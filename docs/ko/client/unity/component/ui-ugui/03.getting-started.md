# 빠른 시작

이 문서는 UGUI 컴포넌트의 기본 구성을 완료하고 UI 인터페이스를 생성하고 표시하는 방법을 안내합니다.

## 사전 요구 사항

- Unity 2019.4+
- GameFrameX Core, UI, Asset, Event 패키지 설치됨
- UGUI 패키지 설치됨

## 단계

### 1. UI 폼 만들기

1. Unity 에디터에서 Hierarchy를 마우스 오른쪽 버튼으로 클릭 -> `GameFrameX` -> `Create UI Form`
2. UGUI 템플릿 선택
3. UI 폼 이름과 소속 UI 그룹 구성

### 2. UI 로직 작성

```csharp
using UnityEngine.UI;
using GameFrameX.UI;

public class MyUIForm : UIForm
{
    private Button _closeButton;

    protected override void OnInit(object userData)
    {
        base.OnInit(userData);
        _closeButton = GetComponentInChildren<Button>();
        _closeButton.onClick.AddListener(OnCloseButtonClick);
    }

    protected override void OnOpen(object userData)
    {
        base.OnOpen(userData);
        Debug.Log("UI 폼이 열렸습니다");
    }

    private void OnCloseButtonClick()
    {
        Close();
    }
}
```

### 3. UI 폼 열기

```csharp
var uiComponent = GameFrameworkEntry.GetComponent<UIComponent>();
uiComponent.OpenUIForm("MyUIForm");
```

## 다음 단계

- [UI 만들기](02.create-ui.md)를 읽고 더 많은 UI 생성 방법을 확인하십시오
- [코드 생성기](07.code-generator.md)를 읽고 자동 코드 생성 기능을 확인하십시오
