# よくある

ドキュメントた GlobalConfig コンポーネントのよくある。

## 

GlobalConfigComponent は GameFrameX フレームワークのグローバルコンポーネント，にゲームのグローバル，バージョン、リソースバージョン、。コンポーネント Unity コンポーネントの，と。

> コンポーネントのリファレンス [GlobalConfig コンポーネント](./1-overview.1-introduction)。
> コンポーネントの API リファレンス [GlobalConfigComponent](./4-core-component.1-global-config-component)。

## インストールと

### Q1: インストール GlobalConfig コンポーネント？

GlobalConfig コンポーネントをじてインストール：

1. **をじて manifest.json インストール**：にプロジェクトの `manifest.json` ファイル：
   ```json
   {"com.gameframex.unity.globalconfig": "https://github.com/GameFrameX/com.gameframex.unity.globalconfig.git"}
   ```

2. **をじて Unity Package Manager**：に Unity  `Window > Package Manager`， `+`  Git URL：
   ```
   https://github.com/GameFrameX/com.gameframex.unity.globalconfig.git
   ```

3. **インストール**：リポジトリ Unity プロジェクトの `Packages` ディレクトリ。

> のインストールステップリファレンス [インストールメソッド](./2-installation.1-install-methods)。

### Q2: インストール "Assembly has reference to missing assembly" エラー？

は GlobalConfig コンポーネント：
- `com.gameframex.unity` (バージョン 1.1.1)
- `com.gameframex.unity.web` (バージョン 1.1.2)

****：インストール。に `manifest.json` ：

```json
{
  "dependencies": {
    "com.gameframex.unity": "1.1.1",
    "com.gameframex.unity.globalconfig": "https://github.com/GameFrameX/com.gameframex.unity.globalconfig.git",
    "com.gameframex.unity.web": "1.1.2"
  }
}
```

### Q3: Unity バージョン？

 package.json，コンポーネントサポートの Unity バージョン **2017.1**。バージョンの Unity，。

---

## コンポーネント

### Q4:  GlobalConfigComponent ？

1. に Unity エディター， GameObject
2. に Inspector ， **Add Component** 
3.  **Game Framework > Global Config**
4. コンポーネントの GameObject 

> のステップリファレンス [](./3-getting-started.1-add-to-scene)。

### Q5: に GlobalConfigComponent ？

****。コンポーネントた `[DisallowMultipleComponent]` ，た GameObject 。コンポーネントの GameObject，フレームワークシングルトン，にグローバルコンポーネント。

### Q6: にコード GlobalConfigComponent？

コンポーネントた `IsAutoRegister = false`，コンポーネント。のはにをじて：

```csharp
// を通じて FindObjectOfType 查找
var globalConfig = FindObjectOfType<GlobalConfigComponent>();
if (globalConfig != null)
{
    string appVersionUrl = globalConfig.CheckAppVersionUrl;
    string resourceVersionUrl = globalConfig.CheckResourceVersionUrl;
}
```

> のコードリファレンス [コード](./3-getting-started.3-code-access)。

### Q7: をじてコード？

にをじてコードプロパティ：

```csharp
var globalConfig = FindObjectOfType<GlobalConfigComponent>();

// 設定各种 URL
globalConfig.CheckAppVersionUrl = "https://example.com/check-app-version";
globalConfig.CheckResourceVersionUrl = "https://example.com/check-resource-version";
globalConfig.HostServerUrl = "https://example.com/server";

// 設定附加内容
globalConfig.Content = "{\"key\": \"value\"}";

// 設定グローバル設定信息
ResponseGlobalInfo globalInfo = new ResponseGlobalInfo
{
    // 設定プロパティ
};
globalConfig.SetGlobalConfig(globalInfo);
```

---

## プロパティ

### Q8: プロパティ？

| プロパティ |  |
|------|------|
| ** (HostServerUrl)** | の，にゲーム |
| **Appバージョンインターフェース (CheckAppVersionUrl)** | バージョンの URL  |
| **リソースバージョンインターフェース (CheckResourceVersionUrl)** | ゲームリソースの URL  |
| ** (AOTCodeList)** | AOT コンパイルの（JSON ） |
| ** (Content)** | の |
| **データ (AOTCodeLists)** | AOT データ |

> のプロパティリファレンス [プロパティ](./3-getting-started.2-configure-properties)。

### Q9: AOTCodeList と AOTCodeLists ？

- **AOTCodeList** (タイプ)：にに Inspector  JSON  AOT コード
- **AOTCodeLists** (タイプ)：の AOT コード，

 AOTCodeList ， JSON  AOTCodeLists：

```csharp
// 設定 JSON 格式の AOT コード列表
globalConfig.AOTCodeList = "[\"Assembly1\", \"Assembly2\", \"Assembly3\"]";

// 自动解析，可能に AOTCodeLists 中获取解析结果
List<string> aotCodeLists = globalConfig.AOTCodeLists;
```

---

## AOT 

### Q10: は AOT コード？？

AOT (Ahead-of-Time) コンパイルは .NET のコンパイル，に IL2CPP プラットフォーム。タイプとコードに AOT コンパイル，をじて AOT コードの。

> の AOT リファレンス [AOT コード](./7-aot-configuration.1-aot-code-list)。

### Q11:  AOTCodeList  JSON エラー？

に AOTCodeList エラー，のと：

1. **JSON エラー**： JSON 
   ```csharp
   // エラー例
   globalConfig.AOTCodeList = "Assembly1, Assembly2"; // エラーの格式

   // 正确例
   globalConfig.AOTCodeList = "[\"Assembly1\", \"Assembly2\"]"; // 正确の JSON 数组格式
   ```

2. ****：む，

### Q12: AOT コード？

 AOT コード，のに IL2CPP プラットフォーム。：
- タイプ
- タイプ
- コード

---

## エディター

### Q13: に Play プロパティ？

は。Inspector のプロパティに **Play **はの，はた。

```csharp
// GlobalConfigComponentInspector.cs 中の实现
EditorGUI.BeginDisabledGroup(EditorApplication.isPlayingOrWillChangePlaymode & Application.isPlaying);
{
    // プロパティ编辑区域
}
EditorGUI.EndDisabledGroup();
```

### Q14: データ (OriginalData) はの？

はの，データプロパティに Inspector はの。プロパティにのデータ，にをじてコード：

```csharp
// 設定原始データ
globalConfig.SetOriginalData(jsonString);

// 读取原始データ
string originalData = globalConfig.OriginalData;
```

---

## 

### Q15: コンポーネントの Awake メソッド？

コンポーネントた `Awake` メソッド， `IsAutoRegister`  `false`：

```csharp
protected override void Awake()
{
    IsAutoRegister = false;
    base.Awake();
}
```

コンポーネントフレームワークのコンポーネント，をじて。

### Q16: グローバル？

のフロー：

1. の URL リクエスト
2. すデータ（JSON ）
3. レスポンスデータ ResponseGlobalInfo 
4. びし SetGlobalConfig グローバル

```csharp
// 伪コード例
var globalConfig = FindObjectOfType<GlobalConfigComponent>();
string url = globalConfig.HostServerUrl;

// 发送ネットワークリクエスト获取設定
// ...
ResponseGlobalInfo response = ParseServerResponse(data);
globalConfig.SetGlobalConfig(response);

// 访问設定信息
ResponseGlobalInfo info = globalConfig.GlobalInfo;
```

---

## とバージョン

### Q17: GlobalConfig コンポーネントコンポーネント？

 package.json，コンポーネント：
- `com.gameframex.unity` (>= 1.1.1) - GameFrameX コアフレームワーク
- `com.gameframex.unity.web` (>= 1.1.2) - Web ネットワークモジュール

### Q18: インストールのコンポーネントバージョン？

に Unity の Package Manager インストールのバージョン，に `Packages/manifest.json` ファイルバージョン。

---

## トラブルシューティング

### Q19: よくあるステップ

1. ****：インストール
2. ** Unity バージョン**： Unity 2017.1 バージョン
3. **コンポーネント**： GlobalConfigComponent 
4. **プロパティ**：の URL プロパティ
5. **ログ**： Unity はエラー

### Q20:  GlobalConfigComponent？

 `FindObjectOfType<GlobalConfigComponent>()` す null，：
1. コンポーネント GameObject
2. コンポーネントたの GameObject
3. ロード

****：
- コンポーネントの GameObject
-  GlobalConfig にのロード

---

## リンク

- [GlobalConfig コンポーネント](./1-overview.1-introduction)
- [インストールメソッド](./2-installation.1-install-methods)
- [](./3-getting-started.1-add-to-scene)
- [プロパティ](./3-getting-started.2-configure-properties)
- [コード](./3-getting-started.3-code-access)
- [GlobalConfigComponent](./4-core-component.1-global-config-component)
- [リクエストクラス](./5-data-structures.1-request-classes)
- [レスポンスクラス](./5-data-structures.2-response-classes)
- [カスタマイズInspector](./6-editor-configuration.1-custom-inspector)
- [AOTコード](./7-aot-configuration.1-aot-code-list)

## ファイルロード

****：に Inspector  GlobalConfigComponent ファイルパス。

****：に Unity エディター GlobalConfigComponent の GameObject，に Inspector は。

## バージョン URL 

****：エラーネットワーク。

****： `AppVersionCheckUrl` と `ResourceVersionCheckUrl` はの HTTP/HTTPS ，ネットワーク。
