---
title: "事件收不到或不触发怎么办"
order: 1
autoGenerated:
  source:
    repo: com.gameframex.unity.event
    commit: a5f8cf0edf8e3925da36478adc798c737af4c682
    generator: OpenDeepWiki
---
# 事件收不到或不触发怎么办

订阅了事件却收不到回调，通常是**订阅与抛出的事件 id 不一致**、**Fire 的"下一帧分发"语义被误解**，或**场景缺少 `EventComponent`** 导致的。按下面的诊断表对号入座，再进入对应修复步骤即可定位。

## 快速诊断

| 症状 | 原因 | 修复 |
|------|------|------|
| 回调从不执行 | 订阅的 `id` 与 `Fire` 传入的事件编号不是同一个字符串（拼写、大小写、一边用 `typeof(X).FullName` 一边用手写字面量） | 统一用常量或 `typeof(X).FullName`，见[修复步骤 1](#1-核对事件-id-完全一致) |
| 回调从不执行，但 id 确认一致 | 订阅发生在 `Fire` 之后，`EventPoolMode.AllowNoHandler` 下事件被静默丢弃 | 先 `Subscribe` 再 `Fire`，见[修复步骤 2](#2-先订阅再抛出并自检订阅是否成功) |
| 事件在 Fire 当帧收不到，下一帧才到 | `Fire` 线程安全但延迟到下一帧分发，属于设计行为 | 需要立刻回调改用 `FireNow`，见[修复步骤 3](#3-理解-fire-的下一帧分发语义) |
| 偶发丢事件 | 抛出事件后同帧销毁对象 / 游戏暂停，`EventManager.Update` 未再执行，队列中的事件未分发 | 避免抛出后立即销毁订阅者；或改用 `FireNow` |
| 控制台输出 `Event manager is invalid.`，所有事件失效 | 场景中缺少 `EventComponent`，`Awake` 中取不到 `IEventManager` 模块 | 在场景挂上 GameFrameX/Event 组件，见[修复步骤 4](#4-确认场景中存在-eventcomponent) |
| 调 `Fire(sender, "")` 或传 null 事件编号直接抛异常 | `GameFrameworkGuard.NotNullOrEmpty(eventId, nameof(eventId))` 参数校验 | 传入非空事件编号字符串 |
| 无人订阅的事件想打日志排查 | 未设置默认处理函数 | 调用 `SetDefaultHandler` 兜底，见[修复步骤 5](#5-用默认处理函数兜底排查无人订阅的事件) |
| 取消订阅时报 handler 不存在 | `Unsubscribe` 传入了未订阅的 handler | 改用 `CheckUnsubscribe` |

## 修复步骤

### 1. 核对事件 id 完全一致

`Subscribe` 与 `Fire` 匹配的唯一依据是 `string` 类型的事件编号，两侧不一致时事件池不会报错（`EventManager` 以 `EventPoolMode.AllowNoHandler | EventPoolMode.AllowMultiHandler` 模式创建），事件被静默丢弃：

```csharp
m_EventPool = new EventPool<GameEventArgs>(EventPoolMode.AllowNoHandler | EventPoolMode.AllowMultiHandler);
```

Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L46-L49)

推荐做法：把事件编号收敛为同一份常量或 `typeof(X).FullName`，订阅与抛出两侧引用同一来源。使用无参数事件时，`Fire(sender, eventId)` 内部会创建 `EmptyEventArgs` 并写入该编号：

```csharp
public void Fire(object sender, string eventId)
{
    GameFrameworkGuard.NotNullOrEmpty(eventId, nameof(eventId));
    m_EventManager.Fire(sender, EmptyEventArgs.Create(eventId));
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L157-L161)

```csharp
public static EmptyEventArgs Create(string eventId)
{
    var eventArgs = ReferencePool.Acquire<EmptyEventArgs>();
    eventArgs.SetEventId(eventId);
    return eventArgs;
}
```

Source: [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs#L33-L38)

注意 `EmptyEventArgs` 回收时会把编号重置为 `typeof(EmptyEventArgs).FullName`，不要依赖事件参数对象在分发后仍保留自定义编号。

### 2. 先订阅再抛出，并自检订阅是否成功

事件必须先订阅后抛出。怀疑订阅链路有问题时，用 `Count(id)` / `Check(id, handler)` 直接确认事件池当前状态：

```csharp
public int Count(string id)
{
    return m_EventManager.Count(id);
}

public bool Check(string id, EventHandler<GameEventArgs> handler)
{
    return m_EventManager.Check(id, handler);
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L84-L98)

重复初始化容易造成同一 handler 被订阅多次（`AllowMultiHandler` 允许重复订阅，会重复回调）。框架提供了防重复版本：

```csharp
public void CheckSubscribe(string id, EventHandler<GameEventArgs> handler)
{
    if (!Check(id, handler))
    {
        m_EventManager.Subscribe(id, handler);
    }
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L105-L111)

对称地，取消订阅不存在的 handler 时用 `CheckUnsubscribe`，它会在 handler 不存在时直接跳过：

```csharp
public void CheckUnsubscribe(string id, EventHandler<GameEventArgs> handler)
{
    m_EventManager.CheckUnsubscribe(id, handler);
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L128-L131)

### 3. 理解 Fire 的"下一帧"分发语义

`Fire` 是线程安全的，但事件进入队列后在 `EventManager.Update` 中于下一帧分发；`FireNow` 立即分发但非线程安全：

```csharp
/// 抛出事件，这个操作是线程安全的，即使不在主线程中抛出，也可保证在主线程中回调事件处理函数，但事件会在抛出后的下一帧分发。
public void Fire(object sender, GameEventArgs e)
{
    m_EventPool.Fire(sender, e);
}

/// 抛出事件立即模式，这个操作不是线程安全的，事件会立刻分发。
public void FireNow(object sender, GameEventArgs e)
{
    m_EventPool.FireNow(sender, e);
}
```

Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L158-L175)

队列中的事件依赖每帧的轮询被消费：

```csharp
protected override void Update(float elapseSeconds, float realElapseSeconds)
{
    m_EventPool.Update(elapseSeconds, realElapseSeconds);
}
```

Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L81-L84)

由此推出两个排查点：
1. 在 `Fire` 之后当帧就断言回调已执行会失败——等下一帧或改用 `FireNow`。
2. 抛出事件后立刻销毁订阅对象、或游戏暂停导致 `Update` 停止，队列中事件可能永远不分发。

### 4. 确认场景中存在 EventComponent

`EventComponent` 在 `Awake` 中通过 `GameFrameworkEntry.GetModule<IEventManager>()` 获取模块，失败会打 `Log.Fatal` 并直接返回，之后所有事件 API 均不可用：

```csharp
protected override void Awake()
{
    ImplementationComponentType = Utility.Assembly.GetType(componentType);
    InterfaceComponentType = typeof(IEventManager);
    base.Awake();
    m_EventManager = GameFrameworkEntry.GetModule<IEventManager>();
    if (m_EventManager == null)
    {
        Log.Fatal("Event manager is invalid.");
        return;
    }
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L66-L77)

修复：在场景里添加 `GameFrameX/Event` 组件（`[AddComponentMenu("GameFrameX/Event")]`），并确认初始化顺序满足模块优先级（`EventManager.Priority` 为 7）。

### 5. 用默认处理函数兜底排查无人订阅的事件

设置默认处理函数后，所有没有订阅者的事件都会走到该回调，适合在排查阶段打印"事件被抛出但无人处理"：

```csharp
public void SetDefaultHandler(EventHandler<GameEventArgs> handler)
{
    m_EventManager.SetDefaultHandler(handler);
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L137-L140)

## 验证

1. 订阅一个只打日志的 handler，记录 `Count(eventId)`，期望返回值 ≥ 1。
2. `Fire(sender, eventId)` 后等待下一帧（或直接 `FireNow`），确认日志输出。
3. `Check(eventId, handler)` 应返回 `true`；执行 `CheckUnsubscribe` 后 `Count(eventId)` 应回到 0，且再次 `Fire` 时事件进入默认处理函数（若已设置）。
4. 在编辑器中选中挂有 `EventComponent` 的对象，可通过 Inspector 观察事件与处理函数数量（`EventHandlerCount` / `EventCount`）。

## 相关链接

- 事件订阅与抛出 API 速查：见《事件系统参考》
- 事件系统整体介绍与快速开始：见《事件系统概述》
- 源码入口：[EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs)、[EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs)、[GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs#L34-L40)
