# クイックスタート

本文書は、UGUIコンポーネントの基本設定，引导您完成を作成し、UI画面を表示する手順を説明します。

## 前提条件

- Unity 2019.4以上
- GameFrameX Core、UI、Asset、Eventパッケージがインストール済み
- UGUIパッケージがインストール済み

## 手順

### 1. UIフォームを作成

1. UnityエディターのHierarchyで右クリック → `GameFrameX` → `Create UI Form`
2. UGUIテンプレートを選択
3. UIフォームの名前と所属するUIグループを設定

### 2. UIロジックを記述

```csharp
using UnityEngine.UI;
using GameFrameX.UI;

public class MyUIForm : UIForm
{
    private Button _closeButton;

    protected override void OnInit(object userData)
    {
        base.OnInit(userData);
        _closeButton = GetComponentInChildren<Button>();
        _closeButton.onClick.AddListener(OnCloseButtonClick);
    }

    protected override void OnOpen(object userData)
    {
        base.OnOpen(userData);
        Debug.Log("UIフォームが開かれました");
    }

    private void OnCloseButtonClick()
    {
        Close();
    }
}
```

### 3. UIフォームを開く

```csharp
var uiComponent = GameFrameworkEntry.GetComponent<UIComponent>();
uiComponent.OpenUIForm("MyUIForm");
```

## 次のステップ

- [UI作成](02.create-ui.md)を阅读して、より多くのUI作成方法を學習
- [コードジェネレーター](07.code-generator.md)を阅读して、自動コード生成機能を學習