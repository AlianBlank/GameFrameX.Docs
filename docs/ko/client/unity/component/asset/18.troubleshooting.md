# 자주 묻는 질문

이 문서는 GameFrameX Asset 자원 컴포넌트 사용 과정에서 발생할 수 있는 일반적인 문제와 그 해결책을汇总합니다.

## 개요

Asset 자원 컴포넌트는 YooAsset을 기반으로 구축되어 통일된 자원 로드 및 관리 인터페이스를 제공합니다. 실제 사용에서 플랫폼 차이, 구성 오류 또는 자원 경로 문제로 인해 다양한 오류 상황이 발생할 수 있습니다. 이 문서는 개발자가 이러한 문제를 빠르게 위치 파악하고 해결할 수 있도록 도와줍니다.

## 초기화 문제

### 문제: 자원 시스템 초기화 실패

**증상:** 게임 시작 시 "Asset manager is invalid" 또는 초기화 관련 오류 발생.

**가능한 원인:**
1. AssetComponent가 장면에 올바르게 추가되지 않음
2. IAssetManager 모듈이 등록되지 않음

**해결책:**

Unity 장면에서 AssetComponent가 올바르게 구성되었는지 확인합니다:

```csharp
// Unity 장면에 AssetComponent 추가
// 경로: GameObject -> GameFrameX -> Asset
```

> Source: [AssetComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetComponent.cs#L18-L64)

GameFrameworkEntry가 올바르게 초기화되었는지 확인합니다:

```csharp
// 시작 시 AssetManager 모듈이 등록되었는지 확인
GameFrameworkEntry.RegisterModule<AssetManager>();
```

### 문제: 자원 패키지 초기화 반환값 false

**증상:** `InitPackageAsync` 메서드가 false를 반환하거나 예외 발생.

**가능한 원인:**
1. 실행 모드 (EPlayMode)가 올바르게 설정되지 않음
2. Host 서버 URL이 구성되지 않음
3. YooAssets 내부 초기화 실패

**해결책:**

초기화 전에 실행 모드가 올바르게 설정되었는지 확인합니다:

```csharp
var assetComponent = GameEntry.GetComponent<AssetComponent>();
assetComponent.GamePlayMode = EPlayMode.HostPlayMode;
```

> Source: [AssetManager.Initialization.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetManager.Initialization.cs#L18-L47)

## 자원 로드 문제

### 문제: 자원 경로无效

**증상:** 자원 로드 시 경로无效 또는 자원未找到 오류.

**가능한 원인:**
1. 자원 경로가 잘못 입력됨
2. 자원이 빌드清单에 추가되지 않음
3. 자원 태그 구성 오류

**해결책:**

`HasAssetPath` 메서드를 사용하여 경로가有效的인지 확인합니다:

```csharp
var assetComponent = GameEntry.GetComponent<AssetComponent>();
if (assetComponent.HasAssetPath("Prefabs/Player"))
{
    var handle = await assetComponent.LoadAssetAsync<GameObject>("Prefabs/Player");
}
```

> Source: [AssetComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetComponent.cs#L570-L573)

### 문제: 비동기 로드 자원 반환값 null

**증상:** 비동기 자원 로드 후 null 또는 Handle 무효 획득.

**가능한 원인:**
1. 자원 경로가 존재하지 않음
2. 자원 타입 불일치
3. 로드 과정에서 예외 발생

**해결책:**

항상 Handle의 상태를 확인합니다:

```csharp
var handle = await assetComponent.LoadAssetAsync<GameObject>("Prefabs/Player");
if (handle.Status == YooAsset.EOperationStatus.Succeed)
{
    var player = handle.AssetObject as GameObject;
}
else
{
    Log.Error($"자원 로드 실패: {handle.Error}");
}
```

### 문제: 동기 로드가 Editor模式下卡死

**증상:** Editor 모의模式下에서 동기 로드 사용 시 편집기卡死.

**원인 분석:** EditorSimulateMode는 편집기 내 자원을 사용하며, 동기 로드는 메인 스레드를 차단합니다.

**해결책:**

가능하면 비동기 로드 방식을 우선使用합니다:

```csharp
// 권장하지 않음
var handle = assetComponent.LoadAssetSync<GameObject>("Prefabs/Player");

// 권장
var handle = await assetComponent.LoadAssetAsync<GameObject>("Prefabs/Player");
```

## 메모리 관리 문제

### 문제: 메모리占用 지속 증가

**증상:** 게임 실행一段时间 후 메모리占用不断增加.

**가능한 원인:**
1. 로드된 자원이 올바르게 해제되지 않음
2. 자원 핸들러를 자주 생성하지만 Release를 호출하지 않음
3. 무용 자원을 정기적으로 정리하지 않음

**해결책:**

자원 핸들을 올바르게 해제합니다:

```csharp
var handle = await assetComponent.LoadAssetAsync<GameObject>("Prefabs/Player");
// 사용 완료 후 해제
handle.Release();
```

> Source: [AssetComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetComponent.cs#L622-L628)

정기적으로 무용 자원을 정리합니다:

```csharp
// 적절한 타이밍에 호출
assetComponent.UnloadUnusedAssetsAsync();
assetComponent.ClearUnusedBundleFilesAsync();
```

### 문제: 자원 언로드 효과 없음

**증상:** UnloadAsset 호출 후에도 자원이仍然存在.

**가능한 원인:**
1. 여전히 해당 자원을 참조하는 객체가 있음
2. 자원 패키지가 잠겨 있음

**해결책:**

모든 자원을 강제로 회수합니다:

```csharp
// 모든 자원 강제 회수
assetComponent.UnloadAllAssetsAsync(packageName);
```

## 플랫폼 특정 문제

### 문제: WebGL 플랫폼 자원 로드 실패

**증상:** WebGL 플랫폼에서 자원 로드 또는 다운로드 불가.

**가능한 원인:**
1. WebGL模式下에서는 반드시 WebPlayMode를 사용해야 함
2. 서버 CORS 구성 문제
3. 메모리 제한

**해결책:**

비편집기 환경에서 AssetComponent는 자동으로 WebPlayMode를 설정합니다:

```csharp
#if UNITY_WEBGL && !UNITY_EDITOR
GamePlayMode = EPlayMode.WebPlayMode;
#endif
```

> Source: [AssetComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetComponent.cs#L44-L52)

### 문제:小游戏 플랫폼 컴파일 오류

**증상:** 위챗, 데챠, 쿠아이쇼우小游戏 플랫폼에서 컴파일 오류 발생.

**历史 수정 기록:**
- 쿠아이쇼우小游戏 플랫폼 컴파일 오류 수정 (v2.4.0)
- 위챗小游戏 플랫폼 조건 컴파일 오류 제거 (v2.3.0)
- 위챗小游戏 환경 초기화 수정 (v2.2.2)

**해결책:**

최신 버전을 사용하고小游戏 플랫폼을 올바르게 구성했는지 확인합니다:

```csharp
#if ENABLE_WECHAT_MINI_GAME
// 위챗小游戏 특정 구성
GameEntry.GetComponent<AssetComponent>().gameObject
    .GetOrAddComponent<WeChatConfigHandler>();
#endif
```

> Source: [AssetManager.Initialization.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetManager.Initialization.cs#L105-L119)

### 문제: EditorSimulateMode가 패키지 후无效

**증상:** 개발 시 Editor 모드では정상, 패키지 후 자원 로드 불가.

**원인:** EditorSimulateMode는 편집기 환경에서만有效합니다.

**해결책:**

패키지 전에 올바른 실행 모드를 설정했는지 확인합니다:

```csharp
#if UNITY_EDITOR
assetComponent.GamePlayMode = EPlayMode.EditorSimulateMode;
#else
assetComponent.GamePlayMode = EPlayMode.HostPlayMode; // 또는 기타 적절한 모드
#endif
```

## 다운로드 업데이트 문제

### 문제: 자원 다운로드 실패

**증상:** 패치 업데이트 시 파일 다운로드 실패.

**가능한 원인:**
1. 네트워크 연결 문제
2. 서버 URL 구성 오류
3. 파일 검증 실패

**해결책:**

다운로드 실패 이벤트를 리스닝합니다:

```csharp
// 다운로드 실패 이벤트 구독
GameEntry.Event.Subscribe(AssetWebFileDownloadFailedEventArgs.EventId, OnDownloadFailed);

private void OnDownloadFailed(object sender, GameEventArgs e)
{
    var args = e as AssetWebFileDownloadFailedEventArgs;
    Log.Error($"다운로드 실패: {args.FileName}, 오류: {args.Error}");
}
```

### 문제: 패치清单 업데이트 실패

**증상:** 패치清单 업데이트 시 오류 발생.

**가능한 원인:**
1. Manifest 파일 손상
2. 네트워크 문제
3. 버전 불일치

**해결책:**

清单 업데이트 실패 이벤트를 리스닝합니다:

```csharp
GameEntry.Event.Subscribe(AssetPatchManifestUpdateFailedEventArgs.EventId, OnManifestUpdateFailed);

private void OnManifestUpdateFailed(object sender, GameEventArgs e)
{
    var args = e as AssetPatchManifestUpdateFailedEventArgs;
    Log.Error($"清单 업데이트 실패: {args.PackageName}, 오류: {args.Error}");
}
```

> Source: [AssetPatchManifestUpdateFailedEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Update/EventArgs/AssetPatchManifestUpdateFailedEventArgs.cs#L36-L48)

## 디버깅 기법

### 이벤트 시스템을 사용한 디버깅

Asset 컴포넌트는 디버깅을 위한 완전한 이벤트 시스템을 제공합니다:

```csharp
// 상태 변화 이벤트 구독
GameEntry.Event.Subscribe(AssetPatchStatesChangeEventArgs.EventId, OnPatchStatesChange);

// 다운로드 진행률 이벤트 구독
GameEntry.Event.Subscribe(AssetDownloadProgressUpdateEventArgs.EventId, OnDownloadProgress);

// 업데이트 파일 발견 이벤트 구독
GameEntry.Event.Subscribe(AssetFoundUpdateFilesEventArgs.EventId, OnFoundUpdateFiles);
```

> Source: [EPatchStates.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Update/EPatchStates.cs#L6-L33)

### 실행 모드 확인

로그에서 현재 실행 모드를 확인합니다:

```csharp
// AssetManager.Initialize()는 현재 모드를 출력
Log.Info($"자원 시스템 실행 모드: {PlayMode}");
```

> Source: [AssetManager.cs](https://github.com/GameFrameX/com.gameframex.unity.asset/blob/main/Runtime/Asset/AssetManager.cs#L49)

### 자원 패키지 상태 확인

```csharp
var package = assetComponent.GetAssetsPackage("DefaultPackage");
if (package != null)
{
    var version = package.GetPackageVersion();
    Log.Info($"현재 패키지 버전: {version}");
}
```

## 관련 링크

- [자원 로드 인터페이스](./5-api-reference.loading-api)
- [자원 패키지 관리](./5-api-reference.package-management)
- [업데이트 이벤트](./5-api-reference.update-events)
- [실행 모드](./6-configuration.play-modes)
- [컴포넌트 구성](./6-configuration.component-settings)
- [플랫폼 지원 개요](./7-platforms.index)
