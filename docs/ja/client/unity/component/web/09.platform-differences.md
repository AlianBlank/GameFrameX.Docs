# 平台差异説明

本文档详细介绍 GameFrameX Web コンポーネント在不同 Unity 平台上的実装差异，帮助開発者理解各平台的特点并正确使用コンポーネント。

## 概要

GameFrameX Web コンポーネントサポート三大平台クラス别：

- **WebGL 平台**：适に使用浏览器环境发布的 WebGL 构建
- **非 WebGL 平台**：包括 PC（Windows、Mac、Linux）、iOS、Android 等原生平台

这两种平台在网络お願いします求実装机制上存在本质差异，主要体现在：

| 特性 | WebGL 平台 | 非 WebGL 平台 |
|------|------------|---------------|
| 网络库 | UnityWebRequest | System.Net.HttpWebRequest |
| 异步模式 | 回调式（Callback） | 异步等待（async/await） |
| 多线程サポート | 不サポート | サポート |
| 证书处理 | 自定义 CertificateHandler | .NET 标准证书验证 |

## 架构差异

```mermaid
flowchart TD
    subgraph GameFrameX_Web["GameFrameX Web コンポーネント"]
        IWM[IWebManager インターフェース]
        WM[WebManager]
        WC[WebComponent]
    end

    subgraph WebGL_Platform["WebGL 平台 (UNITY_WEBGL)"]
        UWR[UnityWebRequest]
        BC[DefaultBypassCertificate]
    end

    subgraph NonWebGL_Platform["非 WebGL 平台"]
        HWR[HttpWebRequest]
        NC[.NET 证书验证]
    end

    WC --> IWM
    IWM --> WM
    WM -->|编译条件 #if UNITY_WEBGL| UWR
    UWR --> BC
    WM -->|其他平台| HWR
    HWR --> NC

    style WebGL_Platform fill:#f9f,stroke:#333
    style NonWebGL_Platform fill:#bbf,stroke:#333
```

### 设计意图

插件采用条件编译（`#if UNITY_WEBGL`）実装平台差异，这种设计带来了以下优势：

1. **统一インターフェース**：上层呼び出し代码无需关心底层実装细节
2. **性能优化**：每个平台都能使用最适合的网络库
3. **機能一致**：尽管実装不同，对外提供的 API 完全一致

## コア実装差异

### お願いします求发送方法

**WebGL 平台実装：**

在 WebGL 平台上，お願いします求を通じて `UnityWebRequest` 发送，使用イベント回调处理完成通知。

```csharp
// Source: Runtime/Web/WebManager.cs#L213-L258
#if UNITY_WEBGL
UnityWebRequest unityWebRequest;
if (webJsonData.IsGet)
{
    unityWebRequest = UnityWebRequest.Get(webJsonData.URL);
}
else
{
    unityWebRequest = UnityWebRequest.Post(webJsonData.URL, string.Empty);
}

unityWebRequest.certificateHandler = new DefaultBypassCertificate();
unityWebRequest.timeout = (int)RequestTimeout.TotalSeconds;
// ... 設定お願いします求头和お願いします求体

var asyncOperation = unityWebRequest.SendWebRequest();
asyncOperation.completed += (asyncOperation2) =>
{
    m_SendingNormalList.Remove(webJsonData);
    if (unityWebRequest.isNetworkError || unityWebRequest.isHttpError || unityWebRequest.error != null)
    {
        webJsonData.UniTaskCompletionStringSource.TrySetException(new Exception(unityWebRequest.error));
        return;
    }
    webJsonData.UniTaskCompletionStringSource.SetResult(new WebStringResult(webJsonData.UserData, unityWebRequest.downloadHandler.text));
};
#endif
```

**非 WebGL 平台実装：**

在非 WebGL 平台上，お願いします求を通じて `HttpWebRequest` 发送，サポート真正的异步等待。

```csharp
// Source: Runtime/Web/WebManager.cs#L260-L328
#else
try
{
    HttpWebRequest request = WebRequest.CreateHttp(webJsonData.URL);
    request.Method = webJsonData.IsGet ? WebRequestMethods.Http.Get : WebRequestMethods.Http.Post;
    request.Timeout = (int)RequestTimeout.TotalMilliseconds;
    // ... 設定お願いします求头和お願いします求体

    using (HttpWebResponse response = (HttpWebResponse)await request.GetResponseAsync())
    {
        using (StreamReader reader = new StreamReader(response.GetResponseStream()))
        {
            string content = await reader.ReadToEndAsync();
            webJsonData.UniTaskCompletionStringSource.SetResult(new WebStringResult(webJsonData.UserData, content));
        }
    }
}
catch (WebException e)
{
    if (e.Status == WebExceptionStatus.Timeout)
    {
        webJsonData.UniTaskCompletionStringSource.SetException(new TimeoutException(e.Message));
        return;
    }
    webJsonData.UniTaskCompletionStringSource.SetException(e);
}
finally
{
    m_SendingNormalList.Remove(webJsonData);
}
#endif
```

### 证书处理差异

**WebGL 平台：**

WebGL 平台使用自定义的证书处理器来绕过证书验证，这对于开发调试和自签名证书环境非常有用。

```csharp
// Source: Runtime/Web/DefaultBypassCertificate.cs
public sealed class DefaultBypassCertificate : CertificateHandler
{
    protected override bool ValidateCertificate(byte[] certificateData)
    {
        return true;
    }
}
```

> Source: [DefaultBypassCertificate.cs](https://github.com/GameFrameX/com.gameframex.unity.web/blob/main/Runtime/Web/DefaultBypassCertificate.cs#L41-L44)

WebGL 平台在作成お願いします求时应用此证书处理器：

```csharp
// Source: Runtime/Web/WebManager.cs#L224
unityWebRequest.certificateHandler = new DefaultBypassCertificate();
```

**非 WebGL 平台：**

非 WebGL 平台使用 .NET 标准证书验证机制，在生产环境中提供更安全的 HTTPS 保护。

### 超时处理差异

```mermaid
sequenceDiagram
    participant Client as 呼び出し方
    participant WebGL as WebGL 平台
    participant Net as .NET 平台
    participant SRV as 服务器

    Client->>WebGL: GetToString(url)
    WebGL->>SRV: UnityWebRequest
    SRV-->>WebGL: 响应/超时
    alt 超时情况
        WebGL-->>Client: Exception via SetException
    end

    Client->>Net: GetToString(url)
    Net->>SRV: HttpWebRequest
    SRV-->>Net: 响应/超时
    alt 超时情况
        Net-->>Client: TimeoutException
    end
```

两个平台对超时異常的处理方法略有不同：

- **WebGL**：を通じて检测 `unityWebRequest.error` 判断超时和其他错误
- **非 WebGL**：を通じて捕获 `WebExceptionStatus.Timeout` 異常并转换为 `TimeoutException`

## 各平台使用注意事项

### WebGL 平台

1. **单线程限制**：WebGL 不サポート多线程，所有网络お願いします求都在主线程を通じて回调处理
2. **跨域お願いします求（CORS）**：浏览器环境强制执行同源策略，确保服务器正确配置 CORS 头
3. **HTTPS 证书**：使用默认的证书绕过处理器，生产环境需考虑替换
4. **异步模式**：虽然インターフェース返回 `Task`，但内部実装是回调式的

### 非 WebGL 平台（PC/iOS/Android）

1. **真异步サポート**：使用 .NET 的 `async/await` 模式，真正実装非阻塞
2. **标准证书验证**：使用系统默认的证书验证机制，更安全可靠
3. **超时精度**：使用毫秒级超时控制
4. **连接池管理**：を通じて `MaxConnectionPerServer` 配置控制并发连接数

## 平台检测与条件编译

在开发过程中，如果必要に基づいて平台执行不同逻辑，可能使用以下方法：

```csharp
#if UNITY_WEBGL
    // WebGL 平台特定代码
    Debug.Log("Running on WebGL platform");
#else
    // 其他平台代码
    Debug.Log("Running on native platform");
#endif
```

### 脚本定义符号

插件提供します两个に使用调试的脚本定义符号：

| 符号 | 説明 |
|------|------|
| `ENABLE_GAMEFRAMEX_WEB_SEND_LOG` | 启用お願いします求发送日志 |
| `ENABLE_GAMEFRAMEX_WEB_RECEIVE_LOG` | 启用响应接收日志 |

> Source: [WebLogScriptingDefineSymbols.cs](https://github.com/GameFrameX/com.gameframex.unity.web/blob/main/Editor/WebLogScriptingDefineSymbols.cs#L42-L43)

可能を通じて Unity 菜单 `GameFrameX/Scripting Define Symbols` 快速开关这些日志。

## 常见平台问题

### WebGL 平台 CORS 错误

**问题**：浏览器控制台显示 "Access to fetch ... has been blocked by CORS policy"

**解決策**：
1. 确保服务器正确設定 CORS 响应头：
   ```
   Access-Control-Allow-Origin: *
   Access-Control-Allow-Methods: GET, POST, OPTIONS
   Access-Control-Allow-Headers: Content-Type, Authorization
   ```
2. 服务器必要处理 OPTIONS 预检お願いします求

### 超时設定不生效

**问题**：設定的超时时间在 WebGL 平台似乎无效

**原因分析**：
- WebGL 平台的 `UnityWebRequest.timeout` プロパティ設定的是整秒数
- 非 WebGL 平台使用毫秒级精度

**建议**：在 WebGL 平台使用较长超时时间（建议 10 秒以上）

### HTTPS 证书警告

**问题**：WebGL 平台お願いします求 HTTPS 地址时出现证书警告

**原因**：使用了默认的证书绕过处理器

**解決策**：生产环境建议実装自定义 `CertificateHandler` 进行实际证书验证

## 関連リンク

- [插件介绍](./01.overview)
- [基础用法](./03.getting-started)
- [WebComponent コンポーネント](./08.web-component)
- [IWebManager インターフェース](./04.iwebmanager-interface)
- [超时配置](./10.timeout-settings)
- [常见问题](./11.troubleshooting)
