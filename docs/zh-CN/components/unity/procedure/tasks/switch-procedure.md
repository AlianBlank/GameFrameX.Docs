---
title: "如何在流程之间切换"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.procedure
    commit: e289726a851a3844e51037341ea0b711fa82b0b3
    generator: OpenDeepWiki
---
# 如何在流程之间切换

切换流程通过 `IProcedureManager.StartProcedure` 完成,游戏任何位置(流程类内部、外部系统、UI 按钮回调)只要拿到 `ProcedureComponent.Procedure` 都能调用。本页给出从获取管理器到完成切换的最小可运行步骤。

## 前置条件

- 已创建继承自 `ProcedureBase` 的多个流程类(如 `ProcedureLogin`、`ProcedureMain`)。
- `ProcedureComponent` 已在场景中挂载,并在 Inspector 的 `Available Procedure Type Names` 中填入这些流程类型名称。
- 已了解整体架构,见《概述》。

## 操作步骤

### 1. 获取流程管理器

通过 `ProcedureComponent` 的 `Procedure` 属性拿到 `IProcedureManager`:

```csharp
using GameFrameX.Procedure.Runtime;
using UnityEngine;

ProcedureComponent procedureComponent = GameFrameX.Runtime.GameFrameworkEntry.GetComponent<ProcedureComponent>();
IProcedureManager procedureManager = procedureComponent.Procedure;
```

Source: [ProcedureComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureComponent.cs#L70-L74)

### 2. 在当前流程的 `OnUpdate` 中发起切换

最常见的切换方式:在某个流程的轮询中检测到条件满足时调用 `StartProcedure`,管理器会触发当前流程的 `OnLeave` 与目标流程的 `OnEnter`。

```csharp
using GameFrameX.Procedure.Runtime;

public class ProcedureLogin : ProcedureBase
{
    protected override void OnUpdate(IFsm<IProcedureManager> procedureOwner, float elapseSeconds, float realElapseSeconds)
    {
        base.OnUpdate(procedureOwner, elapseSeconds, realElapseSeconds);

        if (登录完成的条件满足)
        {
            procedureOwner.Owner.StartProcedure<ProcedureMain>();
        }
    }
}
```

`IProcedureManager.StartProcedure` 的两个重载定义见 [IProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/IProcedureManager.cs#L64-L73),底层会调用状态机的状态切换。

### 3. 在外部代码中切换

不一定要在流程内部切换。游戏里任何持有 `IProcedureManager` 引用的地方都可以调用:

```csharp
using GameFrameX.Procedure.Runtime;

procedureManager.StartProcedure(typeof(ProcedureMain));
// 或泛型版本
procedureManager.StartProcedure<ProcedureMain>();
```

泛型版本源码签名:

```csharp
public interface IProcedureManager
{
    void StartProcedure<T>() where T : ProcedureBase;
    void StartProcedure(Type procedureType);
}
```

Source: [IProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/IProcedureManager.cs#L60-L73)

### 4. 校验切换成功

切换后读取 `CurrentProcedure` 与 `CurrentProcedureTime`,确认新流程已生效、计时已重置为 0。单元测试中的写法可作参考:

```csharp
m_ProcedureManager.Initialize(m_FsmManager, new ProcedurePreload(), new ProcedureMain());
m_ProcedureManager.StartProcedure<ProcedurePreload>();

Assert.IsNotNull(m_ProcedureManager.CurrentProcedure);
Assert.IsInstanceOf<ProcedurePreload>(m_ProcedureManager.CurrentProcedure);
Assert.AreEqual(0f, m_ProcedureManager.CurrentProcedureTime);
```

Source: [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Tests/UnitTests.cs#L153-L173)

## 常见变体

### 仅查询不切换

切换前可用 `HasProcedure` 确认目标已注册,避免状态机内部抛异常:

```csharp
if (procedureManager.HasProcedure<ProcedureMain>())
{
    procedureManager.StartProcedure<ProcedureMain>();
}
```

Source: [IProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/IProcedureManager.cs#L78-L89)

### 获取已注册的实例而非切换

有时只是读取当前流程实例做条件判断,不必切换:

```csharp
ProcedureBase main = procedureManager.GetProcedure<ProcedureMain>();
```

Source: [IProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/IProcedureManager.cs#L94-L105)

## 常见错误

- **调用时机过早**:流程管理器 `Initialize` 之前调用任何方法会抛异常。单元测试 `CurrentProcedure_BeforeInitialize_Throws`、`HasProcedure_BeforeInitialize_Throws` 就是验证这一点,见 [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Tests/UnitTests.cs#L96-L128)。确保先完成 `Initialize` 再 `StartProcedure`。
- **目标流程未注册**:`StartProcedure` 要求流程已通过 `Initialize(..., params ProcedureBase[] procedures)` 或 `ReinitializeProcedures` 注册过。可用 `HasProcedure<T>()` 预先判断。
- **在 `OnEnter` / `OnInit` 内切换**:状态机初始化阶段调用 `StartProcedure` 可能与自身生命周期冲突,推荐放到 `OnUpdate` 中或外部驱动。

## 背景(可选)

`ProcedureComponent` 持有 `m_ProcedureManager`,启动时按 Inspector 配置的 `m_AvailableProcedureTypeNames` 通过反射创建流程实例并依次调用 `StartProcedure`,此后 `m_EntranceProcedure` 成为初始流程。切换时由底层状态机执行 `OnLeave`/`OnEnter` 流转。`m_UseStartupRunner = true` 时,组件本身不再 `Initialize`,由 `ApplicationStartupEntry` + `StartupRunner.Run` 接管,避免重复 `Initialize`。