# 安装

GameFrameX Get Channel 插件的完整安装指南，支持 Unity 2017.1 及以上版本。

## 概述

`com.gameframex.unity.getchannel` 是一个 Unity 插件，用于在多平台项目中获取分发渠道号。该插件支持 iOS、tvOS、visionOS、Android、Editor、PC（Windows/Mac/Linux）、WebGL、UWP、PS4、PS5、Xbox One、Nintendo Switch 等平台。

本节将详细介绍三种安装方式，帮助您快速将插件集成到 Unity 项目中。

## 系统要求

| 要求项 | 最低版本 |
|--------|----------|
| Unity 编辑器 | 2017.1+ |
| .NET 运行时 | .NET Standard 2.0 |

## 安装方式

### 方式一：通过 manifest.json 安装（推荐）

这是最推荐的安装方式，方便版本管理和依赖解析。

**操作步骤：**

1. 打开 Unity 项目中的 `Packages` 文件夹
2. 找到并编辑 `manifest.json` 文件
3. 在 `dependencies` 节点中添加以下内容：

```json
{
  "dependencies": {
    "com.gameframex.unity.getchannel": "https://github.com/gameframex/com.gameframex.unity.getchannel.git",
    // ... 其他依赖
  }
}
```

4. 保存文件后，Unity 会自动下载并安装插件

> Source: [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/README.zh-CN.md#L36-L45)

**优势：**
- 自动解析依赖
- 方便版本控制
- 可通过 Unity Package Manager 统一管理

---

### 方式二：通过 Unity Package Manager 安装

使用 Unity 内置的 Package Manager 界面来安装插件。

**操作步骤：**

1. 在 Unity 编辑器中打开 `Window -> Package Manager`
2. 点击左上角的 `+` 号按钮
3. 选择 `Add package from git URL...` 选项
4. 在输入框中填入以下 Git URL：

```
https://github.com/gameframex/com.gameframex.unity.getchannel.git
```

5. 点击 `Add` 按钮，等待安装完成

> Source: [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/README.zh-CN.md#L47-L53)

**优势：**
- 无需手动编辑文件
- 图形化操作界面
- 适合不熟悉 JSON 格式的用户

---

### 方式三：直接下载或克隆仓库

直接将插件文件复制到项目中。

**操作步骤：**

1. 下载或克隆插件仓库：
   ```bash
   git clone https://github.com/GameFrameX/com.gameframex.unity.getchannel.git
   ```

2. 将下载的文件夹复制到 Unity 项目的 `Packages` 目录下

3. Unity 会自动识别并加载插件

> Source: [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/README.zh-CN.md#L55-L56)

**适用场景：**
- 需要修改插件源码
- 网络环境无法访问 Git
- 离线环境安装

## 安装验证

安装完成后，可以通过以下方式验证：

1. **检查 Package Manager**：在 `Window -> Package Manager` 中查看是否显示 `Game Frame X Get Channel`

2. **检查项目结构**：确认以下文件已正确安装：
   - `Runtime/BlankGetChannel.cs` - 核心运行时脚本
   - `Editor/PostProcessBuildHandler.cs` - iOS 构建后处理脚本

3. **运行示例代码**：导入 Samples 示例场景进行测试

## 包结构说明

安装后的插件包含以下核心组件：

| 目录/文件 | 说明 |
|-----------|------|
| `Runtime/BlankGetChannel.cs` | 核心渠道获取类 |
| `Runtime/BlankGetChannel.Runtime.asmdef` | 运行时程序集定义 |
| `Runtime/link.xml` | 代码链接配置，防止代码裁剪 |
| `Runtime/com.alianhome.getchannel.zip` | Android 渠道 SDK |
| `Plugins/Android/com.alianhome.getchannel.jar` | Android 原生插件 |
| `Plugins/iOS/BlankGetChannel/` | iOS 原生插件 |
| `Editor/PostProcessBuildHandler.cs` | iOS 构建后处理器 |
| `Sample~/BlankGetChannelExample.cs` | 使用示例 |

## 卸载插件

如需卸载插件，只需：

1. 在 `manifest.json` 中移除对应依赖行，或
2. 直接删除 `Packages/com.gameframex.unity.getchannel` 文件夹

## 常见问题

### Q: 安装后提示找不到 BlankGetChannel 类？

确保 `Packages` 文件夹下存在正确的目录结构，Unity 会自动扫描并导入程序集定义文件（.asmdef）。

### Q: Unity 版本过低无法安装？

本插件要求 Unity 2017.1 及以上版本。请升级您的 Unity 编辑器。

### Q: 依赖下载失败怎么办？

- 检查网络连接
- 尝试使用代理
- 使用方式三直接下载插件文件

## 相关链接

- [快速开始](./3-quick-start) - 了解如何使用插件获取渠道号
- [API 参考](./4-api-reference) - BlankGetChannel 类完整 API 文档
- [平台配置](./5-platform-configuration) - 各平台渠道配置详细说明
- [示例](./6-samples) - 更多使用示例代码
- [GitHub 仓库](https://github.com/GameFrameX/com.gameframex.unity.getchannel) - 官方源码仓库
