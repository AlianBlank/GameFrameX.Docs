---
title: "如何使用 OptionsBuilder 解析命令列引數"
order: 1
autoGenerated:
  source:
    repo: GameFrameX.Foundation
    commit: 326e5381987fd10eeefa4e00dc8372061d1e7f1e
    generator: OpenDeepWiki
---
# 使用 OptionsBuilder 解析命令列引數的方法

`OptionsBuilder<T>` 與 `OptionsBuilder`(靜態輔助類別)可從 `string[] args` 與環境變數建構強型別設定物件，支援 bool・int・double・string 等多種型別。屬性會依照「命令列 > 環境變數 > 預設值」的優先順序進行覆寫。

## 前置條件

- 在專案中參考 `GameFrameX.Foundation.Options` 組件。
- 撰寫 `using GameFrameX.Foundation.Options;`。
- 準備具有 `public` setter 的設定類別(需符合 `T : class, new()` 條件約束)。

## 操作步驟

### 1. 定義設定類別

```csharp
public class AppConfig
{
    public string Host { get; set; } = "localhost";
    public int Port { get; set; } = 8080;
    public bool Debug { get; set; }
    public string LogLevel { get; set; } = "info";
    public double Timeout { get; set; } = 30.5;
}
```

Source: [GameFrameX.Foundation.Options/README.md](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/README.md#L22-L31)

### 2. 使用靜態方法一行建構(建議的進入點)

`OptionsBuilder.Create<TOptions>(args)` 是最常使用的進入點，內部會 new 出 `OptionsBuilder<T>` 並呼叫 `Build()`。

```csharp
using GameFrameX.Foundation.Options;

static void Main(string[] args)
{
    var config = OptionsBuilder.Create<AppConfig>(args);
    Console.WriteLine($"{config.Host}:{config.Port}, debug={config.Debug}");
}
```

來源定義：

```csharp
public static TOptions Create<TOptions>(string[] args, bool skipValidation = false) where TOptions : class, new()
{
    var builder = new OptionsBuilder<TOptions>(args ?? Array.Empty<string>());
    return builder.Build(skipValidation);
}
```

Source: [GameFrameX.Foundation.Options/OptionsBuilder.cs](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L63-L67)

### 3. 手動具現化建構器(更精細的控制)

若想控制 bool 的格式、強制前綴、環境變數的讀取等，可直接建構泛型版本：

```csharp
var builder = new OptionsBuilder<AppConfig>(
    args,
    boolFormat: BoolArgumentFormat.Flag,
    ensurePrefixedKeys: true,
    useEnvironmentVariables: true);
var config = builder.Build();
```

建構函式簽章：

```csharp
public OptionsBuilder(string[] args, BoolArgumentFormat boolFormat = BoolArgumentFormat.Flag, bool ensurePrefixedKeys = true, bool useEnvironmentVariables = true)
```

Source: [GameFrameX.Foundation.Options/OptionsBuilder.cs](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L236-L240)

### 4. 視需要選擇其他靜態輔助方法

| 方法 | 用途 | 來源 |
|------|------|------|
| `Create<T>(args, skipValidation)` | 以預設設定建構，失敗時擲回例外 | [OptionsBuilder.cs#L63-L67](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L63-L67) |
| `Create<T>(args, boolFormat, ensurePrefixedKeys, useEnvironmentVariables, skipValidation)` | 控制所有參數 | [OptionsBuilder.cs#L82-L91](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L82-L91) |
| `CreateFromArgsOnly<T>(args, skipValidation)` | 僅使用命令列，忽略環境變數 | [OptionsBuilder.cs#L103-L107](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L103-L107) |
| `CreateFromEnvironmentOnly<T>(skipValidation)` | 僅使用環境變數，忽略 args | [OptionsBuilder.cs#L118-L122](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L118-L122) |
| `CreateDefault<T>()` | 僅使用預設值，略過驗證 | [OptionsBuilder.cs#L132-L136](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L132-L136) |
| `TryCreate<T>(args, out result, out error)` | 失敗時回退至預設設定 | [OptionsBuilder.cs#L149-L163](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L149-L163) |
| `CreateWithDebug<T>(args, skipValidation)` | 建構後呼叫 `OptionsDebugger.PrintParsedOptions` | [OptionsBuilder.cs#L175-L184](https://github.com/GameFrameX/GameFrameX.Foundation/blob/main/GameFrameX.Foundation.Options/OptionsBuilder.cs#L175-L184) |

### 5. 啟動並傳入引數

```bash
myapp.exe --host example.com --port 9090 --debug
```

主控台會依屬性順序顯示原始引數、選項對應、解析後的物件以及 JSON 表示(由 `OptionsDebugger.PrintParsedOptions` 產生)。

## 常見變化

- **僅使用命令列，不讀取環境變數**：

  ```csharp
  var config = OptionsBuilder.CreateFromArgsOnly<AppConfig>(args);
  ```
- **僅使用環境變數，不讀取 args**：

  ```csharp
  var config = OptionsBuilder.CreateFromEnvironmentOnly<AppConfig>();
  ```
- **不擲回例外的容錯建構**(失敗時會回傳預設執行個體，錯誤會放入 `error`):

  ```csharp
  if (!OptionsBuilder.TryCreate<AppConfig>(args, out var cfg, out var err))
  {
      Console.WriteLine($"解析に失敗したためデフォルトを使用します: {err}");
  }
  ```
- **需要偵錯輸出時**：

  ```csharp
  var config = OptionsBuilder.CreateWithDebug<AppConfig>(args);
  ```

## 常見錯誤

| 症狀 | 原因 | 修正方式 |
|------|------|------|
| 必要屬性未設定時擲回例外 | 已啟用必要選項驗證 | 呼叫 `Build(skipValidation: true)` 或 `Create<T>(args, skipValidation: true)` |
| `--verbose=true` 被視為字串 | bool 引數的格式為預設的 `Flag`(僅判斷有無) | 變更為 `BoolArgumentFormat.KeyValue` 或 `Separated` |
| bool 旗標無效 | 使用 `Flag`,卻以 `--verbose true`(中間有空格)的方式傳入 | 變更為 `BoolArgumentFormat.Separated`,或改寫為 `--verbose=true` |
| 環境變數無效 | 環境變數的讀取已停用 | 確認 `useEnvironmentVariables: true`(預設值) |
| `args` 為 `null` | 呼叫端未傳入 | 內部會以 `args ?? Array.Empty<string>()` 進行回退，因此也可以自行加入 null 檢查 |

## 背景(選用)

`OptionsBuilder` 會透過反映讀取 `T` 的 `PropertyInfo`(結果以 `ConcurrentDictionary` 快取以提升效能)，將 `--key value`、`--key=value`、`--flag` 等形式的命令列引數與環境變數對應至屬性名稱並設定到屬性。優先順序如下：命令列 > 環境變數 > 型別欄位的預設值。bool 支援由 `BoolArgumentFormat` 列舉控制的三種解析格式。