---
title: "如何从服务器获取并写入全局配置信息"
order: 5
autoGenerated:
  source:
    repo: com.gameframex.unity.globalconfig
    commit: e9d07be9bf26f9cf8ac767be220c813116f78b84
    generator: OpenDeepWiki
---
# 如何从服务器获取并写入全局配置信息

本指南说明如何通过 `GlobalConfigComponent` 从服务器拉取全局配置(JSON),反序列化为 `ResponseGlobalInfo`,并写入组件,供游戏其他模块读取 `HostServerUrl`、`CheckAppVersionUrl`、`AOTCodeLists` 等字段。

## 前置条件

- 已安装 `com.gameframex.unity.globalconfig` 包,场景中存在 `GameFrameX/Global Config` 组件。
- 服务端提供返回 JSON 的 HTTP 接口,JSON 结构可对应 `ResponseGlobalInfo` 字段。
- 项目中已有可用的 HTTP/WebRequest 工具(本指南示例使用 `UnityWebRequest`)。

## 操作步骤

1. 获取组件引用。组件继承自 `GameFrameworkComponent`,可通过 `GameEntry.GetComponent<GlobalConfigComponent>()` 取得。

   ```csharp
   using GameFrameX.GlobalConfig.Runtime;
   var cfg = GameEntry.GetComponent<GlobalConfigComponent>();
   ```

   Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L13-L14)

2. 构造请求体 `RequestGlobalInfo`,继承自 `RequestBase`,自带语言、平台、包名、渠道等公共字段,可按需扩展。

   ```csharp
   var req = new RequestGlobalInfo
   {
       Language    = "zh-CN",
       AppVersion  = Application.version,
       Platform    = Application.platform.ToString(),
       PackageName = Application.identifier,
       Channel     = "default",
       SubChannel  = string.Empty,
   };
   ```

   Source: [RequestGlobalInfo.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/RequestGlobalInfo.cs#L6-L8), [RequestBase.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/RequestBase.cs#L6-L37)

3. 发起 HTTP 请求并读取 JSON 文本。建议使用协程 + `UnityWebRequest`。

   ```csharp
   string url = "https://your-server/global-info";
   string json = null;

   using (var www = UnityEngine.Networking.UnityWebRequest.Post(url, JsonUtility.ToJson(req)))
   {
       www.SetRequestHeader("Content-Type", "application/json");
       yield return www.SendWebRequest();

       if (www.result == UnityEngine.Networking.UnityWebRequest.Result.Success)
       {
           json = www.downloadHandler.text;
       }
       else
       {
           Debug.LogError(www.error);
       }
   }
   ```

4. 把 JSON 字符串保存为原始数据。组件提供 `SetOriginalData(string)`,传入 `null` 会抛出 `ArgumentNullException`,注意防御。

   ```csharp
   cfg.SetOriginalData(json);
   ```

   Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L126-L134)

5. 反序列化 JSON 为 `ResponseGlobalInfo`,然后调用 `SetGlobalConfig(...)` 写入组件。同样,传入 `null` 会抛 `ArgumentNullException`。

   ```csharp
   var info = JsonUtility.FromJson<ResponseGlobalInfo>(json);
   cfg.SetGlobalConfig(info);
   ```

   Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L160-L168)

6. 读取并使用全局信息。后续模块可通过 `cfg.GlobalInfo`、`cfg.HostServerUrl`、`cfg.CheckAppVersionUrl`、`cfg.AOTCodeLists` 等属性访问;`AOTCodeLists` 会从 `AOTCodeList` 字符串自动反序列化为 `List<string>`。

   ```csharp
   string host        = cfg.HostServerUrl;          // 服务器下发的可选覆盖值
   string checkApp    = cfg.GlobalInfo.CheckAppVersionUrl;
   var   aotList      = cfg.AOTCodeLists;           // List<string>
   string rawSnapshot = cfg.OriginalData;           // 保留的原始 JSON
   ```

   Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L56-L120), [ResponseGlobalInfo.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGlobalInfo.cs#L6-L27)

## 常见变体

- **本地离线缓存**:先读 `cfg.OriginalData` 拿到上次保存的 JSON,若新请求失败则复用旧值,避免每次启动都阻塞在网络上。
- **带重试的请求**:把步骤 3 放进 `for (int i = 0; i < 3; i++)` 重试循环,全部失败再回退到本地缓存。
- **扩展请求字段**:若服务端需要额外参数(如服务器 ID),可新建 `class MyRequest : RequestGlobalInfo { public string ServerId; }`,字段会自动随 JSON 一起序列化。

## 常见错误

| 症状 | 原因 | 处理 |
|------|------|------|
| `SetOriginalData(null)` 抛 `ArgumentNullException` | 传入空引用 | 调用前判断 `json != null`,失败时保留旧值或写入空字符串 |
| `SetGlobalConfig(null)` 抛 `ArgumentNullException` | 反序列化结果为 null(空串/非法 JSON) | 先 `string.IsNullOrEmpty` 判断,失败时记录日志并中止写入 |
| `cfg.AOTCodeLists` 为空 | `AOTCodeList` 字段不是合法 JSON 数组字符串 | 写入时使用 `["A","B"]` 格式;序列化失败组件内部已 `Log.Error` 兜底 |

## 背景(可选)

`GlobalConfigComponent` 是 `GameFrameworkComponent`,通过 `IsAutoRegister = false` 关闭自动注册,需由 `GameEntry` 主动 `GetComponent<T>()` 获取。它同时承担"原始 JSON 缓存"和"反序列化结果"两种状态:前者供本地回退使用,后者供业务模块按字段读取。`AOTCodeList` 在 `set` 时会同步解析为 `List<string>` 缓存,避免每次访问重复反序列化。
