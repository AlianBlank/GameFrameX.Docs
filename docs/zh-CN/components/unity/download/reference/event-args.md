---
title: "事件类型 EventArgs 一览"
order: 3
autoGenerated:
  source:
    repo: com.gameframex.unity.download
    commit: 410b3c76dc5b3a319e551af2f4f6e933bab787fa
    generator: OpenDeepWiki
---
# 事件类型 EventArgs 一览

下载模块通过 `GameEventArgs` 在内部各阶段向业务层抛出事件。本页汇总 `GameFrameX.Download.Runtime` 命名空间下全部 8 个事件参数类的字段、`Create` 工厂方法、参数校验规则与 `EventId`,便于订阅时快速查阅。

> 所有事件参数均继承自 [GameEventArgs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadAgentHelperCompleteEventArgs.cs#L38),统一通过 `ReferencePool.Acquire` 池化复用,使用完由框架自动回收,**不要直接 `new`**。

## 速查表

| 类型 | EventId (FullName) | 关键字段 | 触发时机 |
|------|--------------------|----------|----------|
| `DownloadStartEventArgs` | `GameFrameX.Download.Runtime.DownloadStartEventArgs` | (见源码) | 下载任务开始排队 / 实际下载时 |
| `DownloadUpdateEventArgs` | `GameFrameX.Download.Runtime.DownloadUpdateEventArgs` | (见源码) | 下载进度更新 |
| `DownloadSuccessEventArgs` | `GameFrameX.Download.Runtime.DownloadSuccessEventArgs` | (见源码) | 下载成功完成 |
| `DownloadFailureEventArgs` | `GameFrameX.Download.Runtime.DownloadFailureEventArgs` | (见源码) | 下载失败 |
| `DownloadAgentHelperUpdateLengthEventArgs` | `GameFrameX.Download.Runtime.DownloadAgentHelperUpdateLengthEventArgs` | `int DeltaLength` | 底层代理报告增量字节数 |
| `DownloadAgentHelperUpdateBytesEventArgs` | `GameFrameX.Download.Runtime.DownloadAgentHelperUpdateBytesEventArgs` | `byte[]`(通过 `GetBytes()`) / `int Offset` / `int Length` | 底层代理推送一段字节流 |
| `DownloadAgentHelperCompleteEventArgs` | `GameFrameX.Download.Runtime.DownloadAgentHelperCompleteEventArgs` | `long Length` | 底层代理完成下载 |
| `DownloadAgentHelperErrorEventArgs` | `GameFrameX.Download.Runtime.DownloadAgentHelperErrorEventArgs` | `bool DeleteDownloading` / `string ErrorMessage` | 底层代理出错 |

## DownloadAgentHelperCompleteEventArgs

底层代理辅助器完成下载时抛出。`Create(long length)` 校验 `length >= 0`,否则抛 `GameFrameworkException("Length is invalid.")`。

```csharp
public sealed class DownloadAgentHelperCompleteEventArgs : GameEventArgs
{
    public static readonly string EventId = typeof(DownloadAgentHelperCompleteEventArgs).FullName;
    public long Length { get; private set; }
    public static DownloadAgentHelperCompleteEventArgs Create(long length);
    public override void Clear(); // Length = 0L
    public override string Id { get { return EventId; } }
}
```

Source: [DownloadAgentHelperCompleteEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadAgentHelperCompleteEventArgs.cs#L38-L84)

## DownloadAgentHelperErrorEventArgs

底层代理辅助器出错时抛出。`Create(bool deleteDownloading, string errorMessage)` 不校验入参;`Clear()` 将 `DeleteDownloading` 置 `false`、`ErrorMessage` 置 `null`。

```csharp
public sealed class DownloadAgentHelperErrorEventArgs : GameEventArgs
{
    public static readonly string EventId = typeof(DownloadAgentHelperErrorEventArgs).FullName;
    public bool DeleteDownloading { get; private set; }
    public string ErrorMessage { get; private set; }
    public static DownloadAgentHelperErrorEventArgs Create(bool deleteDownloading, string errorMessage);
    public override void Clear(); // DeleteDownloading=false, ErrorMessage=null
    public override string Id { get { return EventId; } }
}
```

Source: [DownloadAgentHelperErrorEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadAgentHelperErrorEventArgs.cs#L38-L88)

## DownloadAgentHelperUpdateBytesEventArgs

底层代理辅助器推送一段已下载字节流时抛出。`Create(byte[] bytes, int offset, int length)` 三段校验,任一不通过即抛 `GameFrameworkException`;字节通过 `GetBytes()` 取出。

| 校验项 | 失败条件 | 异常消息 |
|--------|----------|----------|
| `bytes` | 为 `null` | `Bytes is invalid.` |
| `offset` | `< 0` 或 `>= bytes.Length` | `Offset is invalid.` |
| `length` | `<= 0` 或 `offset + length > bytes.Length` | `Length is invalid.` |

```csharp
public sealed class DownloadAgentHelperUpdateBytesEventArgs : GameEventArgs
{
    public static readonly string EventId = typeof(DownloadAgentHelperUpdateBytesEventArgs).FullName;
    public int Offset { get; private set; }
    public int Length { get; private set; }
    public static DownloadAgentHelperUpdateBytesEventArgs Create(byte[] bytes, int offset, int length);
    public byte[] GetBytes();
    public override void Clear(); // m_Bytes=null, Offset=0, Length=0
    public override string Id { get { return EventId; } }
}
```

Source: [DownloadAgentHelperUpdateBytesEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadAgentHelperUpdateBytesEventArgs.cs#L38-L125)

## DownloadAgentHelperUpdateLengthEventArgs

底层代理辅助器报告增量字节数时抛出。`Create(int deltaLength)` 校验 `deltaLength > 0`,否则抛 `GameFrameworkException("Delta length is invalid.")`。

```csharp
public sealed class DownloadAgentHelperUpdateLengthEventArgs : GameEventArgs
{
    public static readonly string EventId = typeof(DownloadAgentHelperUpdateLengthEventArgs).FullName;
    public int DeltaLength { get; private set; }
    public static DownloadAgentHelperUpdateLengthEventArgs Create(int deltaLength);
    public override void Clear(); // DeltaLength = 0
    public override string Id { get { return EventId; } }
}
```

Source: [DownloadAgentHelperUpdateLengthEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadAgentHelperUpdateLengthEventArgs.cs#L38-L84)

## DownloadStartEventArgs / DownloadUpdateEventArgs / DownloadSuccessEventArgs / DownloadFailureEventArgs

这 4 个事件参数由 `DownloadManager` 在任务生命周期各阶段抛出(`DownloadStartEventArgs` 任务开始、`DownloadUpdateEventArgs` 进度更新、`DownloadSuccessEventArgs` 成功、`DownloadFailureEventArgs` 失败),其命名空间、类名、源码与事件 ID 如下:

| 文件 | EventId |
|------|---------|
| [DownloadStartEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadStartEventArgs.cs) | `GameFrameX.Download.Runtime.DownloadStartEventArgs` |
| [DownloadUpdateEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadUpdateEventArgs.cs) | `GameFrameX.Download.Runtime.DownloadUpdateEventArgs` |
| [DownloadSuccessEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadSuccessEventArgs.cs) | `GameFrameX.Download.Runtime.DownloadSuccessEventArgs` |
| [DownloadFailureEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/EventArgs/DownloadFailureEventArgs.cs) | `GameFrameX.Download.Runtime.DownloadFailureEventArgs` |

具体字段与构造逻辑请直接打开上述源文件核对。

## 通用约定

- **不要 `new`**:所有事件实例统一通过静态 `Create(...)` 工厂方法从 `ReferencePool` 获取,使用完由框架归还,业务侧无需调用 `Clear()`。
- **订阅方式**:事件由 `GameFrameX.Event.Runtime` 事件总线分发,订阅与反订阅请使用对应的 `EventId`(即 `typeof(T).FullName`)。
- **类全部为 `sealed`**:8 个 EventArgs 均不允许再继承。
- **命名空间**:所有事件参数均位于 `GameFrameX.Download.Runtime`。

## 相关链接

- [DownloadComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/Download/DownloadComponent.cs) — 组件入口,定义订阅入口
- [DownloadManager.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/Download/Download/DownloadManager.cs) — 任务生命周期与事件触发点
- [IDownloadManager.cs](https://github.com/GameFrameX/com.gameframex.unity.download/blob/main/Runtime/Download/Download/IDownloadManager.cs) — 对外公开的下载 API
