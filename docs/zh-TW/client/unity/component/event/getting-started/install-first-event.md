---
title: "安裝並跑通第一個事件"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.event
    commit: a5f8cf0edf8e3925da36478adc798c737af4c682
    generator: OpenDeepWiki
---
# 安裝並跑通第一個事件

本頁帶你從零完成：安裝 `com.gameframex.unity.event` 套件 → 新增 `EventComponent` 元件 → 定義事件參數類別 → 訂閱 → 拋出事件 → 在 Console 看到回呼輸出。全部照抄即可跑通。

## 前置條件

- Unity 2019.4 及以上(見 README 徽章說明)。
- 專案中已引入 GameFrameX 執行階段基礎套件(`EventComponent` 繼承自 `GameFrameworkComponent`,並引用 `GameFrameX.Runtime` 命名空間)。
- 本套件自身無其他相依性。

## 操作步驟

### 1. 安裝套件

任選一種方式編輯 Unity 專案的 `Packages/manifest.json`:

**方式 A:Scoped Registry(推薦)**

```json
{
  "scopedRegistries": [
    {
      "name": "GameFrameX",
      "url": "https://gameframex.upm.alianblank.uk",
      "scopes": [
        "com.gameframex"
      ]
    }
  ],
  "dependencies": {
    "com.gameframex.unity.event": "1.1.1"
  }
}
```

`scopes` 控制哪些套件會透過此 registry 解析，只有以 `com.gameframex` 開頭的套件才會從這個 registry 取得。

**方式 B:Git URL 直接相依**

```json
{
   "com.gameframex.unity.event": "https://github.com/gameframex/com.gameframex.unity.event.git"
}
```

也可以在 Unity 的 `Packages Manager` 中用 `Git URL` 方式新增位址 `https://github.com/gameframex/com.gameframex.unity.event.git`,或直接下載儲存庫放進專案的 `Packages` 目錄自動識別。

Source: [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/README.zh-CN.md#L40-L69)

### 2. 新增 EventComponent 元件

`EventComponent` 是事件系統的 Unity 進入點，透過元件選單 **Component → GameFrameX → Event** 新增(選單路徑由 `[AddComponentMenu("GameFrameX/Event")]` 定義)。它帶有 `[GameFrameXAutoComponent(-7000)]` 特性，在完整的 GameFrameX 啟動流程中會被自動建立；手動掛到任意常駐 GameObject 上同樣可用。元件在 `Awake` 中透過 `GameFrameworkEntry.GetModule<IEventManager>()` 取得管理器實例，失敗時會印出 `Log.Fatal("Event manager is invalid.")` 並停止運作。

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L39-L77)

### 3. 定義事件參數類別

每個事件用一個繼承 `GameEventArgs` 的類別承載，以字串 `Id` 識別事件型別：

```csharp
public class LevelUpEventArgs : GameEventArgs
{
    public override string Id => "level_up";
    public int Level { get; private set; }

    public static LevelUpEventArgs Create(int level)
    {
        var args = ReferencePool.Acquire<LevelUpEventArgs>();
        args.Level = level;
        return args;
    }

    public override void Clear()
    {
        base.Clear();
        Level = 0;
    }
}
```

Source: [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/README.zh-CN.md#L75-L94)

`GameEventArgs` 是 `GameFrameX.Event.Runtime` 命名空間下的抽象基底類別(繼承 `BaseEventArgs`),自訂事件必須從它衍生:
[GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs#L30-L40)

### 4. 訂閱並拋出事件

新增一個測試腳本掛到場景中，回呼簽章固定為 `void(object sender, GameEventArgs e)`,即 `EventHandler<GameEventArgs>`:

```csharp
using GameFrameX.Event.Runtime;
using UnityEngine;

public class FirstEventDemo : MonoBehaviour
{
    private EventComponent m_EventComponent;

    private void Start()
    {
        m_EventComponent = GetComponent<EventComponent>();
        if (m_EventComponent == null)
        {
            m_EventComponent = FindObjectOfType<EventComponent>();
        }

        // 訂閱:不存在時自動訂閱,重複呼叫安全
        m_EventComponent.CheckSubscribe("level_up", OnLevelUp);

        // 拋出事件:延遲模式,下一幀主執行緒回呼
        m_EventComponent.Fire(this, LevelUpEventArgs.Create(5));
    }

    private void OnLevelUp(object sender, GameEventArgs e)
    {
        var args = (LevelUpEventArgs)e;
        Debug.Log($"升级到 {args.Level} 级");
    }

    private void OnDestroy()
    {
        // 存在時安全取消訂閱,handler 不存在時不執行任何動作
        m_EventComponent.CheckUnsubscribe("level_up", OnLevelUp);
    }
}
```

Sources: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L95-L131) · [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/README.zh-CN.md#L96-L128)

### 5. 驗證

執行場景，Console 應印出 `升级到 5 级`。注意 `Fire` 是延遲分發——事件在拋出後的**下一幀**才回呼，若當幀沒看到日誌，等一幀再判斷。

## 常見變體

**立即分發 `FireNow`**:事件立刻同步分發，但**不是執行緒安全的**，僅限主執行緒呼叫。

```csharp
m_EventComponent.FireNow(this, LevelUpEventArgs.Create(5));
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L163-L171)

**無自訂參數的快速拋法**：不需要攜帶資料時，直接用字串事件 ID,內部會建立 `EmptyEventArgs`:

```csharp
m_EventComponent.Fire(this, "level_up");
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L152-L161)

**預設處理器作為後備**：為未訂閱的事件設定後備回呼，常用於排查「事件發了沒人收」的問題：

```csharp
m_EventComponent.SetDefaultHandler(OnDefaultEvent);

void OnDefaultEvent(object sender, GameEventArgs e)
{
    Debug.Log($"未处理的事件: {e.Id}");
}
```

Sources: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L133-L140) · [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/README.zh-CN.md#L142-L151)

**統計資訊**:`Count(id)` 查單一事件的處理函式數量，`EventHandlerCount` / `EventCount` 查全域數量。
Sources: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L47-L61) · [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L84-L87)

## 常見錯誤

| 現象 | 原因 | 規避方法 |
|------|------|----------|
| Console 出現 `Event manager is invalid.`(Fatal) | 元件未透過 `GameFrameworkEntry.GetModule<IEventManager>()` 取得管理器，通常是 GameFrameX 執行階段基礎套件尚未就緒 | 先確認 GameFrameX 執行階段套件已安裝並正常初始化，再掛載 `EventComponent` |
| `Fire` 後當幀收不到回呼 | `Fire` 是延遲分發，事件在拋出後的下一幀才在主執行緒回呼 | 屬預期行為；需要立即同步回呼時請用 `FireNow` |
| 子執行緒拋出事件後行為異常 | 在非主執行緒呼叫了 `FireNow` | 子執行緒只能用 `Fire`(執行緒安全) |
| 事件參數取到髒資料 | 自訂事件參數忘記覆寫 `Clear()` 重設欄位，複用池化實例時殘留舊值 | 參照第 3 步範例，在 `Clear()` 中逐一重設欄位 |
| 重複訂閱同一回呼 | 多處呼叫 `Subscribe` 且未做存在性檢查 | 使用 `CheckSubscribe`(不存在時才訂閱)/ `CheckUnsubscribe`(存在時才取消)，或先 `Check` 再操作 |

## 相關連結

- [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/README.zh-CN.md) — 完整功能介紹與使用範例
- [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs) — 事件元件全部公開 API
- [IEventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/IEventManager.cs) — 事件管理器介面定義
- [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs) — 事件管理器實作
- [GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs) — 事件參數基底類別
- [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs) — `Fire(sender, eventId)` 捷徑所使用的空參數
- [CHANGELOG.md](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/CHANGELOG.md) — 版本更新記錄