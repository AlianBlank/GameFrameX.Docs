---
title: "設定項目參考(UIGroup、動畫開關等屬性)"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.ui.ugui
    commit: 8635a85817c644de244d7a9d3d868d28886d8da4
    generator: OpenDeepWiki
---
# 設定項目參考（UIGroup、動畫開關等屬性）

本頁整理了 GameFrameX UGUI 實作的核心執行階段設定項目與開關：`UIGroup` 深度、Canvas/Camera 預設參數、設計解析度縮放模式，以及 `UGUIElementPropertyAttribute` 控制項路徑標記。

## UIGroup 輔助器設定

`UGUIUIGroupHelper` 負責建立與管理 UGUI 介面群組，並依深度調整 `localPosition`。

| 名稱 | 型別 / 預設值 | 說明 |
|------|-----------|------|
| `Depth` | `int` | 介面群組深度，以 `SetDepth(int)` 寫入 |
| `SetDepth(int depth)` | `void` | 寫入 `Depth`，並將 `transform.localPosition` 設定為 `(0, 0, depth * 1000)` |
| `Handler(root, groupName, uiGroupHelperTypeName, customUIGroupHelper, depth = 0)` | `IUIGroupHelper` | 建立群組節點並呼叫 `SetDepth(depth)`；回傳的輔助器實例由 `Helper.CreateHelper(...)` 建構 |

```csharp
// 出處：UGUIUIGroupHelper.cs
[Preserve]
public sealed class UGUIUIGroupHelper : UIGroupHelperBase
{
    public override int Depth { get; protected set; }

    public override void SetDepth(int depth)
    {
        Depth = depth;
        transform.localPosition = new Vector3(0, 0, depth * 1000);
    }

    public override IUIGroupHelper Handler(Transform root, string groupName, string uiGroupHelperTypeName, IUIGroupHelper customUIGroupHelper, int depth = 0)
    {
        SetDepth(depth);
        // ... 內部會建立群組 GameObject 並附加至 root
    }
}
```

來源：[UGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIUIGroupHelper.cs#L46-L114)

> 每當新增一層群組，Z 軸便會以 `1000` 為單位偏移，可避免深度衝突。

## UGUI 相機預設參數

`EnsureCamera` 會建立名為 `UGUICamera` 的子節點，並寫入下列值：

| 名稱 | 預設值 |
|------|--------|
| `clearFlags` | `CameraClearFlags.Depth` |
| `orthographic` | `true` |
| `orthographicSize` | `5f` |
| `nearClipPlane` | `-500f` |
| `farClipPlane` | `500f` |
| `depth` | `10f` |
| `cullingMask` | `1 << LayerMask.NameToLayer("UI")` |
| `stereoTargetEye` | `StereoTargetEyeMask.None` (Unity 5.4+) |
| `allowHDR` | `false` (Unity 5.6+) |
| `allowMSAA` | `false` (Unity 5.6+) |

```csharp
// 出處：UGUIUIGroupHelper.cs
uiCamera.clearFlags = CameraClearFlags.Depth;
uiCamera.orthographic = true;
uiCamera.orthographicSize = 5f;
uiCamera.nearClipPlane = -500f;
uiCamera.farClipPlane = 500f;
uiCamera.depth = 10f;
uiCamera.cullingMask = uiLayer >= 0 ? 1 << uiLayer : uiCamera.cullingMask;
uiCamera.stereoTargetEye = StereoTargetEyeMask.None;
uiCamera.allowHDR = false;
uiCamera.allowMSAA = false;
```

來源：[UGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIUIGroupHelper.cs#L148-L174)

## UGUI Canvas 預設參數

`EnsureCanvas` 會建立名為 `UGUICanvas` 的子節點，並寫入下列內容：

| 名稱 | 預設值 |
|------|--------|
| `renderMode` | `RenderMode.ScreenSpaceCamera` |
| `worldCamera` | 前一步驟建立的 `uiCamera` |
| `planeDistance` | `100f` |
| `overrideSorting` | `true` |
| `sortingOrder` | `0` |
| `additionalShaderChannels` | `Normal | Tangent | TexCoord1 | TexCoord2 | TexCoord3` |
| `GraphicRaycaster` | 自動加入 |

```csharp
// 出處：UGUIUIGroupHelper.cs
var canvas = canvasObject.GetOrAddComponent<Canvas>();
canvas.renderMode = RenderMode.ScreenSpaceCamera;
canvas.worldCamera = uiCamera;
canvas.planeDistance = 100f;
canvas.overrideSorting = true;
canvas.sortingOrder = 0;
canvas.additionalShaderChannels = AdditionalCanvasShaderChannels.Normal |
                                  AdditionalCanvasShaderChannels.Tangent |
                                  AdditionalCanvasShaderChannels.TexCoord1 |
                                  AdditionalCanvasShaderChannels.TexCoord2 |
                                  AdditionalCanvasShaderChannels.TexCoord3;
canvasObject.GetOrAddComponent<GraphicRaycaster>();
```

來源：[UGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIUIGroupHelper.cs#L199-L213)

## 設計解析度縮放模式

`ApplyDesignResolution` 會依據 `UIDesignResolutionComponent` 將值寫入 `CanvasScaler`：

| `ScaleMode` | 寫入項目 |
|------|--------|
| `ConstantPixelSize` | `scaler.uiScaleMode = ConstantPixelSize`; `scaler.scaleFactor = ConstantScaleFactor` |
| `ConstantPhysicalSize` | `scaler.uiScaleMode = ConstantPhysicalSize`; `scaler.fallbackScreenDPI = FallbackScreenDPI`; `scaler.defaultSpriteDPI = DefaultSpriteDPI` |
| 其他（預設為 `ScaleWithScreenSize`） | `referenceResolution = (DesignWidth, DesignHeight)`; `screenMatchMode = MatchWidthOrHeight`; `matchWidthOrHeight = GetMatchWidthOrHeight(...)` |

`GetMatchWidthOrHeight` 依據 `ScreenMatchMode` 決定：

| `ScreenMatchMode` | 回傳值 |
|------|--------|
| `MatchWidth` | `0f` |
| `MatchHeight` | `1f` |
| 其他 | `designResolution.MatchWidthOrHeight` |

此外，`scaler.referencePixelsPerUnit` 一律使用 `designResolution.ReferencePixelsPerUnit`。

```csharp
// 出處：UGUIUIGroupHelper.cs
private static void ApplyDesignResolution(CanvasScaler scaler, UIDesignResolutionComponent designResolution)
{
    if (designResolution == null) return;
    scaler.referencePixelsPerUnit = designResolution.ReferencePixelsPerUnit;
    switch (designResolution.ScaleMode)
    {
        case UIDesignResolutionComponent.UIScaleMode.ConstantPixelSize:
            scaler.uiScaleMode = CanvasScaler.ScaleMode.ConstantPixelSize;
            scaler.scaleFactor = designResolution.ConstantScaleFactor;
            break;
        case UIDesignResolutionComponent.UIScaleMode.ConstantPhysicalSize:
            scaler.uiScaleMode = CanvasScaler.ScaleMode.ConstantPhysicalSize;
            scaler.fallbackScreenDPI = designResolution.FallbackScreenDPI;
            scaler.defaultSpriteDPI = designResolution.DefaultSpriteDPI;
            break;
        default:
            scaler.uiScaleMode = CanvasScaler.ScaleMode.ScaleWithScreenSize;
            scaler.referenceResolution = new Vector2(designResolution.DesignWidth, designResolution.DesignHeight);
            scaler.screenMatchMode = CanvasScaler.ScreenMatchMode.MatchWidthOrHeight;
            scaler.matchWidthOrHeight = GetMatchWidthOrHeight(designResolution);
            break;
    }
}
```

來源：[UGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIUIGroupHelper.cs#L217-L243)

## EventSystem 自動生成開關

`EnsureEventSystem` 會在 `EventSystem.current == null` 時，於 `root` 之下建立 `EventSystem` GameObject，並附加 `EventSystem` + `StandaloneInputModule`。

| 觸發條件 | 動作 |
|------|--------|
| `EventSystem.current != null` | 立即返回 |
| `root` 節點無效 | 立即返回 |
| 其他情況 | 新建 `EventSystem` + `StandaloneInputModule` |

```csharp
// 出處：UGUIUIGroupHelper.cs
private static void EnsureEventSystem(Transform root)
{
    if (EventSystem.current != null) return;
    if (!TryGetGameObject(root, out _)) return;

    var eventSystemObject = new GameObject("EventSystem");
    eventSystemObject.transform.SetParent(root, false);
    eventSystemObject.GetOrAddComponent<EventSystem>();
    eventSystemObject.GetOrAddComponent<StandaloneInputModule>();
}
```

來源：[UGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIUIGroupHelper.cs#L258-L274)

## 控制項路徑標記 `UGUIElementPropertyAttribute`

以特性（Attribute）形式宣告 Prefab 內子控制項的路徑，並與程式碼產生器搭配使用。

| 名稱 | 型別 | 說明 |
|------|-----------|------|
| `Path` | `string` | Prefab 內控制項的相對路徑 |
| 建構函式 `UGUIElementPropertyAttribute(string path)` | — | 直接寫入 `Path` |

```csharp
// 出處：UGUIElementPropertyAttribute.cs
[UGUIElementProperty("Content/Button")]
private Button m_Button;
```

來源：[UGUIElementPropertyAttribute.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIElementPropertyAttribute.cs#L40-L64)

## 錯誤記錄（例外情況）

| 觸發點 | 錯誤訊息 |
|------|------|
| `Handler` 中 `EnsureRuntimeRoot(root)` 回傳 `null` | `"UGUI runtime root is invalid."` |
| `EnsureRuntimeRoot` 中解析 `root` 失敗 | `"UGUI root transform is invalid."` |
| `EnsureRuntimeRoot` 中建立相機失敗 | `"UGUI camera is invalid."` |
| `EnsureRuntimeRoot` 中建立 Canvas 失敗 | `"UGUI canvas is invalid."` |

來源：[UGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIUIGroupHelper.cs#L88-L141)

## 相關連結

- 如需 `UIGroup` 的執行階段使用方式，請參閱《UI 框架執行階段》概觀
- 如需設計解析度元件的定義，請參閱 `UIDesignResolutionComponent`（屬於 GameFrameX UI Runtime）
- 如需程式碼產生與 `UGUIElementPropertyAttribute` 的使用方式，請參閱 [UGUIComponentInspector.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Editor/UGUIComponentInspector.cs) 及 [UGUICodeGenerator.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Editor/UGUICodeGenerator.cs)