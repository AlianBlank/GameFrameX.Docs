# 图片裁剪工具

图片裁剪工具（实际名称为"防裁剪代码生成工具"）是 GameFrameX 提供的 Unity 编辑器扩展，用于解决 IL2CPP 构建时代码被错误剥离的问题。

## 概述

在 Unity 项目中使用 IL2CPP 进行构建时，编译器会自动剥离未被直接引用的代码（称为"代码裁剪"）。然而，当代码通过反射或其他动态方式使用时，这些类型可能被错误地剥离，导致运行时出现 `MissingMethodException` 或类型解析失败的问题。

图片裁剪工具通过以下方式解决这一问题：
1. 提供可视化界面搜索程序集中的类型
2. 生成 `typeof(TypeName)` 代码来保持对类型的引用
3. 将生成的代码添加到项目中，防止 IL2CPP 剥离这些类型

## 架构

```mermaid
flowchart TD
    subgraph Editor["编辑器层"]
        Window[CroppingWindow 窗口]
    end

    subgraph Core["核心功能层"]
        Search[类型搜索]
        Filter[类型过滤]
        Generator[代码生成器]
    end

    subgraph Runtime["运行时层"]
        Helper[GameFrameXCroppingHelper]
        Types[类型引用集合]
    end

    Window --> Search
    Search --> Filter
    Filter --> Generator
    Generator --> Helper
    Helper --> Types

    style Window fill:#e1f5fe
    style Helper fill:#e8f5e8
    style Types fill:#fff3e0
```

### 核心组件

| 组件 | 文件路径 | 职责 |
|------|----------|------|
| CroppingWindow | `Editor/Cropping/CroppingWindow.cs` | 编辑器窗口 UI、类型搜索、代码生成 |
| GameFrameXCroppingHelper | `Runtime/GameFrameXCroppingHelper.cs` | 运行时类型引用保持模板 |

## 工作流程

```mermaid
sequenceDiagram
    participant User as 用户
    participant Window as CroppingWindow
    participant Assembly as Utility.Assembly
    participant Output as 生成代码

    User->>Window: 打开工具窗口
    User->>Window: 输入搜索关键词
    User->>Window: 点击 Search 按钮
    Window->>Assembly: 获取所有运行时类型
    Assembly-->>Window: 返回类型列表
    Window->>Window: 过滤系统类型
    Window->>Window: 筛选匹配的类型
    Window-->>User: 显示匹配的类型下拉列表
    User->>Window: 选择目标类型
    User->>Window: 点击 Generate 按钮
    Window->>Window: 生成 typeof() 代码
    Window-->>User: 显示生成代码
    User->>User: 复制代码到项目
```

## 使用指南

### 打开工具窗口

在 Unity 编辑器菜单栏中点击 `GameFrameX` → `Cropping(防止裁剪代码生成)` 即可打开工具窗口。

```csharp
// 菜单项定义
[MenuItem("GameFrameX/Cropping(防止裁剪代码生成)", false, 2001)]
public static void ShowWindow()
{
    var window = GetWindow<CroppingWindow>("Cropping");
    window.minSize = new UnityEngine.Vector2(800, 600);
    window.maxSize = window.minSize;
    window.maximized = false;
    window.Show();
}
```
> Source: [CroppingWindow.cs](https://github.com/GameFrameX/com.gameframex.unity/blob/main/Editor/Cropping/CroppingWindow.cs#L45-L55)

### 搜索类型

1. 在"查询类型"文本框中输入要搜索的类型名称关键词
2. 点击 `Search(查询)` 按钮
3. 工具将扫描所有运行时程序集，排除系统类型（UnityEngine、UnityEditor、System等）
4. 在下拉列表中选择要处理的类型

```csharp
// 类型搜索逻辑
string searchText = _searchText.ToLower();
var types = Utility.Assembly.GetTypes();
var result = new List<string>();
foreach (var type in types)
{
    var fullName = type.FullName.ToLower();
    var isFind = false;
    foreach (var ignoredType in _ignoredTypes)
    {
        if (fullName.Contains(ignoredType))
        {
            isFind = true;
            break;
        }
    }
    if (isFind) continue;
    
    if (fullName.Contains(searchText))
    {
        result.Add(type.FullName);
    }
}
```
> Source: [CroppingWindow.cs](https://github.com/GameFrameX/com.gameframex.unity/blob/main/Editor/Cropping/CroppingWindow.cs#L76-L102)

### 生成防裁剪代码

1. 从下拉列表中选择目标类型（通常选择程序集的根命名空间类型）
2. 点击 `Generate(生成)` 按钮
3. 工具将生成该程序集中所有类型的 `typeof()` 引用代码
4. 将生成的代码复制到项目的 `CroppingHelper.cs` 文件中

```csharp
// 代码生成逻辑
private void Generate(string targetTypeName)
{
    var targetType = Utility.Assembly.GetType(targetTypeName);
    if (targetType != null)
    {
        var types = targetType.Assembly.GetTypes();
        types = types.OrderBy(m => m.FullName).ToArray();
        StringBuilder stringBuilder = new StringBuilder();
        foreach (var type in types)
        {
            if (type.IsNestedPrivate) continue;
            if (type.FullName.Contains("PrivateImplementationDetails")) continue;
            
            stringBuilder.AppendLine(" _ = typeof(" + type.FullName.Replace("+", ".").Replace("`1", "<>").Replace("`2", "<,>") + ");");
        }
        _generatedText = stringBuilder.ToString();
    }
}
```
> Source: [CroppingWindow.cs](https://github.com/GameFrameX/com.gameframex.unity/blob/main/Editor/Cropping/CroppingWindow.cs#L136-L163)

### 使用生成的代码

将生成的代码复制到 Unity 项目的脚本文件中：

```csharp
using UnityEngine;

namespace GameFrameX.Runtime
{
    public class GameFrameXCroppingHelper : MonoBehaviour
    {
        private void Start()
        {
            // 生成的代码示例
            _ = typeof(GameFrameX.Runtime.SomeClass);
            _ = typeof(GameFrameX.Runtime.AnotherClass);
            // ... 更多类型
        }
    }
}
```
> Source: [GameFrameXCroppingHelper.cs](https://github.com/GameFrameX/com.gameframex.unity/blob/main/Runtime/GameFrameXCroppingHelper.cs#L1-L10)

## 排除的系统类型

工具默认排除以下程序集中的类型，避免生成不必要的代码：

| 排除前缀 | 说明 |
|----------|------|
| unityengine | Unity 核心引擎类型 |
| unityeditor | Unity 编辑器类型 |
| mono | Mono 运行时类型 |
| system | .NET System 类型 |
| dnlib | 程序集解析库 |
| unity.hotfix | 热更新类型 |
| unity.baselib | 基础库类型 |
| .editor | 编辑器相关 |
| jetbrains | JetBrains 工具类型 |
| nunit | NUnit 测试框架 |

```csharp
private readonly string[] _ignoredTypes = new string[] { 
    "unityengine".ToLower(), 
    "unityeditor".ToLower(), 
    "mono".ToLower(), 
    "system".ToLower(), 
    "dnlib".ToLower(), 
    "unity.hotfix".ToLower(), 
    "unity.baselib".ToLower(), 
    ".editor".ToLower(), 
    "jetbrains".ToLower(), 
    "nunit".ToLower() 
};
```
> Source: [CroppingWindow.cs](https://github.com/GameFrameX/com.gameframex.unity/blob/main/Editor/Cropping/CroppingWindow.cs#L58)

## 常见使用场景

### 场景一：反射调用类型

当使用反射动态创建对象或调用方法时：

```csharp
// 这种代码可能导致类型被剥离
var type = Type.GetType("GameFrameX.Runtime.DynamicClass");
var instance = Activator.CreateInstance(type);
```

### 场景二：JSON 序列化

使用 JSON 序列化库时，类型信息可能丢失：

```csharp
// Newtonsoft.Json 或其他序列化库
string json = JsonConvert.SerializeObject(someObject);
// 反序列化时需要类型存在
var obj = JsonConvert.DeserializeObject<DynamicType>(json);
```

### 场景三：接口与实现分离

使用依赖注入或工厂模式时：

```csharp
// 通过配置文件或反射加载程序集
var assembly = Assembly.Load("GameFrameX.Plugin");
var type = assembly.GetType("GameFrameX.Plugin.IPlugin");
```

## 配置生成的代码模板

生成的代码会排除以下类型的类型：

1. **嵌套私有类型** (`IsNestedPrivate`) - 通常是内部实现细节
2. **PrivateImplementationDetails** - 编译器生成的辅助类型

```csharp
foreach (var type in types)
{
    if (type.IsNestedPrivate)
    {
        continue;  // 跳过嵌套私有类型
    }
    
    if (type.FullName.Contains("PrivateImplementationDetails"))
    {
        continue;  // 跳过编译器生成细节
    }
    
    // 生成 typeof() 代码
}
```
> Source: [CroppingWindow.cs](https://github.com/GameFrameX/com.gameframex.unity/blob/main/Editor/Cropping/CroppingWindow.cs#L147-L156)

## 相关链接

- [构建工具](./6-editor-tools.build-tools) - Unity 项目构建自动化工具
- [包管理工具](./6-editor-tools.package-management) - Unity 包管理工具
- [检视面板工具](./6-editor-tools.inspector-tools) - 自定义 Inspector 面板工具
- [对象池系统](./5-runtime.object-pool) - 对象池管理系统
- [引用池系统](./5-runtime.reference-pool) - 引用池管理系统
