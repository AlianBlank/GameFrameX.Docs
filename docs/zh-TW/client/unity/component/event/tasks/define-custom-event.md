---
title: "如何定義自訂事件參數"
order: 1
autoGenerated:
  source:
    repo: com.gameframex.unity.event
    commit: a5f8cf0edf8e3925da36478adc798c737af4c682
    generator: OpenDeepWiki
---
# 如何定義自訂事件參數

定義自訂事件參數只需新增一個類別繼承 `GameEventArgs`，覆寫 `Id` 與 `Clear()`，並提供一個用 `ReferencePool.Acquire<T>()` 建立實例的靜態 `Create` 工廠方法，之後即可透過 `EventComponent` 訂閱與擲出該事件。

## 前置條件

- 專案中已安裝 GameFrameX Event 套件，場景中存在 `EventComponent` 元件（選單 `GameFrameX/Event`，框架啟動時會自動掛載）。
- 引用命名空間 `GameFrameX.Event.Runtime` 與 `GameFrameX.Runtime`。

## 操作步驟

1. 新增事件參數類別，繼承 `GameEventArgs`（它是 `BaseEventArgs` 的抽象子類別，並帶有防止程式碼裁剪的 `Preserve` 特性標記）：

```csharp
using GameFrameX.Runtime;

namespace GameFrameX.Event.Runtime
{
    /// <summary>
    /// 遊戲邏輯事件基底類別。
    /// </summary>
    [UnityEngine.Scripting.Preserve]
    public abstract class GameEventArgs : BaseEventArgs
    {
    }
}
```

Source: [GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs#L30-L41)

2. 按照框架內建的 `EmptyEventArgs` 的寫法實作你的事件參數：覆寫 `Id` 回傳事件編號、覆寫 `Clear()` 還原欄位、提供靜態 `Create` 方法從引用池取得物件。下面的 `PlayerLevelUpEventArgs` 是依照同一模式編寫的自訂範例：

```csharp
using GameFrameX.Runtime;

namespace GameFrameX.Event.Runtime
{
    /// <summary>
    /// 玩家升級事件參數
    /// </summary>
    public sealed class PlayerLevelUpEventArgs : GameEventArgs
    {
        public int OldLevel { get; private set; }
        public int NewLevel { get; private set; }

        /// <summary>
        /// 事件編號，預設使用類型全名，訂閱與擲出時使用同一字串
        /// </summary>
        public override string Id
        {
            get { return typeof(PlayerLevelUpEventArgs).FullName; }
        }

        /// <summary>
        /// 引用池回收時會呼叫，必須把所有欄位還原為預設值
        /// </summary>
        public override void Clear()
        {
            OldLevel = 0;
            NewLevel = 0;
        }

        /// <summary>
        /// 建立事件參數，從引用池取得實例
        /// </summary>
        public static PlayerLevelUpEventArgs Create(int oldLevel, int newLevel)
        {
            var eventArgs = ReferencePool.Acquire<PlayerLevelUpEventArgs>();
            eventArgs.OldLevel = oldLevel;
            eventArgs.NewLevel = newLevel;
            return eventArgs;
        }
    }
}
```

上面參考的框架原始實作如下，`Id`、`Clear()`、`Create` 三者的寫法與 `ReferencePool.Acquire<T>()` 的用法皆以此為準：

```csharp
public sealed class EmptyEventArgs : GameEventArgs
{
    private string _eventId = typeof(EmptyEventArgs).FullName;

    public override void Clear()
    {
        _eventId = typeof(EmptyEventArgs).FullName;
    }

    public override string Id
    {
        get { return _eventId; }
    }

    public static EmptyEventArgs Create(string eventId)
    {
        var eventArgs = ReferencePool.Acquire<EmptyEventArgs>();
        eventArgs.SetEventId(eventId);
        return eventArgs;
    }

    private void SetEventId(string eventId)
    {
        _eventId = eventId;
    }
}
```

Source: [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs#L7-L49)

3. 訂閱事件。`EventComponent` 對外的訂閱入口是 `CheckSubscribe`（不存在時才訂閱，避免重複訂閱）：

```csharp
// 簽章：public void CheckSubscribe(string id, EventHandler<GameEventArgs> handler)
EventComponent.CheckSubscribe(typeof(PlayerLevelUpEventArgs).FullName, OnPlayerLevelUp);

private void OnPlayerLevelUp(object sender, GameEventArgs e)
{
    var args = (PlayerLevelUpEventArgs)e;
    Debug.Log($"升級：{args.OldLevel} -> {args.NewLevel}");
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L100-L111)

4. 擲出事件。`Fire` 執行緒安全、下一幀分發；`FireNow` 立即分發但非執行緒安全：

```csharp
// 簽章：public void Fire(object sender, GameEventArgs e)
EventComponent.Fire(this, PlayerLevelUpEventArgs.Create(10, 11));

// 簽章：public void FireNow(object sender, GameEventArgs e)
EventComponent.FireNow(this, PlayerLevelUpEventArgs.Create(10, 11));
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L142-L171)

5. 不再需要時取消訂閱，事件編號必須與訂閱時完全一致：

```csharp
// 簽章：public void Unsubscribe(string id, EventHandler<GameEventArgs> handler)
EventComponent.Unsubscribe(typeof(PlayerLevelUpEventArgs).FullName, OnPlayerLevelUp);

// handler 可能未訂閱時使用安全版本，不存在時不執行任何操作
EventComponent.CheckUnsubscribe(typeof(PlayerLevelUpEventArgs).FullName, OnPlayerLevelUp);
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L113-L131)

## 常見變體

- **不帶資料的事件**：不必新增類別，直接使用框架內建的 `EmptyEventArgs`。`EventComponent` 提供了便利的多載 `Fire(object sender, string eventId)`，內部會自動 `EmptyEventArgs.Create(eventId)`，`eventId` 傳入 `null` 或空字串會觸發參數驗證：

```csharp
public void Fire(object sender, string eventId)
{
    GameFrameworkGuard.NotNullOrEmpty(eventId, nameof(eventId));
    m_EventManager.Fire(sender, EmptyEventArgs.Create(eventId));
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L152-L161)

- **自訂事件編號字串**：如果不想使用 `typeof(X).FullName`，可以像 `EmptyEventArgs` 那樣把編號儲存為私有欄位 `_eventId`，由 `Create(eventId)` 傳入，一個類別即可承載多個邏輯事件。

## 常見錯誤

| 現象 | 原因 | 修復 |
|------|------|------|
| 擲出事件後沒有任何回呼 | 訂閱與擲出使用的 `Id` 字串不一致 | 兩處統一使用 `typeof(PlayerLevelUpEventArgs).FullName` |
| 事件回呼中讀到的資料是髒值 | `Clear()` 沒有把全部欄位還原，實例被引用池重複使用後殘留舊值 | 在 `Clear()` 中重設所有欄位 |
| 同一幀內回呼重複執行多次 | 多次 `Subscribe` 同一 handler | 改用 `CheckSubscribe`，先 `Check` 再訂閱 |
| 物件銷毀後回呼仍觸發、擲出空引用 | 忘記 `Unsubscribe` | 在 `OnDestroy` 中呼叫 `Unsubscribe` 或 `CheckUnsubscribe` |
| 自訂參數類別被程式碼裁剪掉 | 類別未保留 | 加上 `[UnityEngine.Scripting.Preserve]`，與 `GameEventArgs` 一致 |

## 背景

事件參數實例來自 `ReferencePool` 引用池（`ReferencePool.Acquire<T>()`），擲出分發後會被回收並重複使用，因此 `Clear()` 必須把欄位還原為預設值；這也是所有事件參數都透過靜態 `Create` 建立、而不是 `new` 的原因。事件分發與執行緒語意由 `IEventManager` 管理，`Fire` 保證回呼發生在主執行緒。

## 相關連結

- [GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs) — 事件參數基底類別
- [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs) — 框架內建空事件參數，自訂類別的標準範本
- [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs) — 訂閱 / 擲出事件的元件入口
- [IEventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/IEventManager.cs) — 事件管理器介面