# 添加单次任务

添加单次任务機能允许開発者添加一个只执行一次的定时任务，适に使用延迟执行、一次性イベント触发等シーン。

## 概要

`AddOnce` 是 Timer コンポーネント提供的コアメソッド之一，に使用添加一个只执行一次的定时任务。该メソッド会在指定的延迟时间后触发回调函数，执行完成后自动从定时器列表中移除。

**使用シーン：**
- 延迟执行某个操作（如几秒后显示ヒント）
- 一次性イベント触发（如ゲーム开始倒计时）
- 延迟初期化某些コンポーネント
- 定时提醒機能

## コア実装

`AddOnce` メソッド的内部実装非常简洁，它复用 `Add` メソッド，将重复次数設定为 1：

```csharp
// TimerManager.cs 第 197-200 行
public void AddOnce(float interval, Action<object> callback, object callbackParam = null)
{
    Add(interval, 1, callback, callbackParam);
}
```

> Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L197-L200)

**设计意图：** を通じて复用 `Add` メソッド，代码更加简洁，同时利用已有的オブジェクトプール和线程安全机制，减少重复代码和潜在 bug。

## 工作原理

```mermaid
flowchart TD
    subgraph 作成阶段["作成阶段"]
        A1[呼び出し AddOnce] --> A2[作成 TimerItem]
        A2 --> A3[設定 Repeat = 1]
        A3 --> A4[加入待添加队列]
    end
    
    subgraph 执行阶段["执行阶段"]
        B1[每帧 Update 检测] --> B2{elapsed >= interval?}
        B2 -->|否| B1
        B2 -->|是| B3[执行回调函数]
        B3 --> B4[Repeat 减 1]
        B4 --> B5{Repeat == 0?}
        B5 -->|是| B6[标记为 Deleted]
        B6 --> B7[从定时器列表移除]
    end
    
    A4 --> 执行阶段
```

## 使用例

### 基础用法

添加一个 3 秒后执行的任务：

```csharp
// 取得 TimerComponent コンポーネント
var timerComponent = GetComponent<TimerComponent>();

// 添加一个 3 秒后执行一次的任务
timerComponent.AddOnce(3f, OnTimerComplete);

// 回调メソッド
private void OnTimerComplete(object param)
{
    Debug.Log("定时任务执行完成！");
}
```

> Source: [README.md](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/README.md#L44-L52)

### 传递参数

可能为回调函数传递自定义参数：

```csharp
// 添加带参数的单次任务
timerComponent.AddOnce(5f, OnDelayedAction, "自定义参数");

// 回调メソッド接收参数
private void OnDelayedAction(object param)
{
    string message = param as string;
    Debug.Log($"收到参数: {message}");
}
```

### 在管理器级别使用

直接使用 `ITimerManager` インターフェース：

```csharp
// 取得 TimerManager 模块
var timerManager = GameFrameworkEntry.GetModule<ITimerManager>();

// 添加单次任务
timerManager.AddOnce(2f, OnManagerTimer, null);
```

> Source: [ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs#L26)

## API 参考

### `TimerComponent.AddOnce`

```csharp
public void AddOnce(float interval, Action<object> callback, object callbackParam = null)
```

**機能説明：** 添加一个只执行一次的定时任务。

**参数説明：**

| 参数 | クラス型 | 必填 | 默认值 | 説明 |
|------|------|------|--------|------|
| `interval` | float | 是 | - | 延迟时间，以秒为单位 |
| `callback` | Action\<object\> | 是 | - | 任务触发时执行的回调函数 |
| `callbackParam` | object | 否 | null | 传递给回调函数的参数 |

**呼び出し示例：**

```csharp
timerComponent.AddOnce(3f, MyCallback, "test");
```

### `ITimerManager.AddOnce`

```csharp
void AddOnce(float interval, Action<object> callback, object callbackParam = null);
```

**機能説明：** 在定时器管理器级别添加单次任务。

**詳細説明：** お願いします参阅 [TimerComponent.AddOnce](#timercomponentaddonce) 的参数説明，两者签名完全一致。

> Source: [ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs#L20-L26)

## 注意事项

1. **时间单位**：参数 `interval` 的单位是**秒**（秒），而非毫秒。例如，传入 `5f` 表示 5 秒后执行。

2. **回调签名**：回调函数必須接受一个 `object` クラス型的参数，即使不使用参数也必須声明：

   ```csharp
   // 正确
   void MyCallback(object param) { }
   
   // 错误 - 签名不匹配
   void MyCallback() { }
   ```

3. **自动移除**：任务执行完成后会自动从定时器列表中移除，无需手动呼び出し `Remove`。

4. **重复添加**：如果使用相同的回调函数再次呼び出し `AddOnce`，会更新现有任务的参数（间隔时间和回调参数）。

5. **线程安全**：`AddOnce` メソッド内部使用了锁机制，可能安全地在多线程环境中呼び出し。

## 関連リンク

- [添加定时任务](./4-core-functions.add-timer) - 了解如何添加重复执行的定时任务
- [添加帧更新任务](./4-core-functions.add-update) - 了解如何添加每帧执行的任务
- [检查和移除任务](./4-core-functions.exists-remove) - 了解如何检查和移除任务
- [TimerComponent API 参考](./5-api-reference.timer-component) - 完整的 TimerComponent API 文档
- [ITimerManager API 参考](./5-api-reference.itimer-manager) - 完整的 ITimerManager インターフェース文档
- [TimerManager API 参考](./5-api-reference.timer-manager) - 完整的 TimerManager 実装文档
