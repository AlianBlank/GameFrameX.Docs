# IDataTable 数据表接口

IDataTable 是 GameFrameX Config 模块的核心接口，提供了对配置数据表的统一访问方式。该接口支持多种主键类型（int、long、string），并提供了丰富的数据查询和聚合操作。

## 概述

IDataTable 接口是配置数据表的基础抽象层，设计用于在游戏开发中管理静态配置数据。它采用泛型设计，支持强类型的数据访问，同时保持了灵活的查询能力。

该接口采用双层结构设计：
- **IDataTable**：非泛型基础接口，定义了通用的加载和数据计数功能
- **`IDataTable<T>`**：泛型接口，继承自 IDataTable，提供了丰富的数据访问和查询方法

### 设计意图

IDataTable 接口的设计遵循以下原则：

1. **类型安全**：通过泛型接口确保编译时的类型安全
2. **统一访问**：提供多种主键类型的访问方式，适应不同的配置数据结构
3. **查询灵活**：支持 LINQ 风格的条件查询和聚合计算
4. **异步加载**：支持异步加载数据，提升游戏启动性能

## 架构

以下是 IDataTable 在 Config 系统中的架构关系：

```mermaid
classDiagram
    class IDataTable {
        <<interface>>
        +LoadAsync() Task
        +Count int
    }
    
    class IDataTable~T~ {
        <<interface>>
        +TryGet(int id, out T value) bool
        +TryGet(long id, out T value) bool
        +TryGet(string id, out T value) bool
        +FirstOrDefault T
        +LastOrDefault T
        +All T[]
        +Find(Func~T, bool~) T
        +FindListArray(Func~T, bool~) T[]
        +FindList(Func~T, bool~) List~T~
        +ForEach(Action~T~) void
        +Max(Func~T, Tk~) Tk
        +Min(Func~T, Tk~) Tk
        +Sum(Func~T, int~) int
        +Sum(Func~T, long~) long
        +Sum(Func~T, float~) float
        +Sum(Func~T, double~) double
        +Sum(Func~T, decimal~) decimal
    }
    
    class BaseDataTable~T~ {
        <<abstract>>
        #LongDataMaps Dictionary~long, T~
        #StringDataMaps Dictionary~string, T~
        #DataList List~T~
        +LoadAsync() Task*
    }
    
    class ConfigManager {
        +AddConfig(string, IDataTable) void
        +GetConfig(string) IDataTable
        +HasConfig(string) bool
        +RemoveConfig(string) bool
    }
    
    class ConfigComponent {
        +GetConfig~T~() T
        +HasConfig~T~() bool
        +Add(string, IDataTable) void
    }
    
    IDataTable <|.. IDataTable~T~
    IDataTable~T~ <|.. BaseDataTable~T~
    ConfigManager --> IDataTable : 存储
    ConfigComponent --> ConfigManager : 访问
```

### 核心组件说明

| 组件 | 职责 |
|------|------|
| **IDataTable** | 非泛型基础接口，定义加载和计数功能 |
| **`IDataTable<T>`** | 泛型接口，提供强类型的数据访问方法 |
| **`BaseDataTable<T>`** | 抽象基类，实现核心存储逻辑（字典+列表） |
| **ConfigManager** | 全局配置管理器，负责存储和管理所有 IDataTable 实例 |
| **ConfigComponent** | Unity 组件封装，提供面向用户的便捷访问方法 |

## 数据表继承结构

`IDataTable<T>` 接口继承自 IDataTable，形成完整的接口体系：

```mermaid
erDiagram
    IDataTable ||..|| IDataTable_Generic : "继承"
    IDataTable_Generic ||..|| BaseDataTable : "实现"
    
    IDataTable {
        Task LoadAsync "异步加载"
        int Count "数据数量"
    }
    
    IDataTable_Generic {
        bool TryGet "安全获取"
        T FirstOrDefault "首个元素"
        T LastOrDefault "末位元素"
        T[] All "所有元素"
    }
```

## 使用示例

### 基础使用

以下示例展示如何定义一个继承自 BaseDataTable 的数据表类：

```csharp
using GameFrameX.Config.Runtime;

// 定义数据表元素类型
public class ItemData
{
    public int Id { get; set; }
    public string Name { get; set; }
    public int Level { get; set; }
    public int Price { get; set; }
}

// 定义数据表实现类
public class ItemDataTable : BaseDataTable<ItemData>
{
    public override async Task LoadAsync()
    {
        // 在此处实现数据加载逻辑
        // 示例：从配置文件中加载数据并填充到 LongDataMaps 和 StringDataMaps
        var dataList = new List<ItemData>
        {
            new ItemData { Id = 1, Name = "武器", Level = 1, Price = 100 },
            new ItemData { Id = 2, Name = "护甲", Level = 1, Price = 80 },
            new ItemData { Id = 3, Name = "药水", Level = 1, Price = 10 }
        };
        
        foreach (var data in dataList)
        {
            LongDataMaps[data.Id] = data;
            StringDataMaps[data.Name] = data;
            DataList.Add(data);
        }
    }
}
```
> Source: [IDataTable.cs](https://github.com/GameFrameX/com.gameframex.unity.config/blob/main/Runtime/Config/Config/IDataTable.cs#L46-L77)
> Source: [BaseDataTable.cs](https://github.com/GameFrameX/com.gameframex.unity.config/blob/main/Runtime/Config/Config/BaseDataTable.cs#L46-L70)

### 数据查询

数据表加载后，可以通过多种方式查询数据：

```csharp
// 获取数据表实例（通过 ConfigComponent）
var configComponent = UnityEngine.GameObject.FindObjectOfType<ConfigComponent>();
var itemDataTable = configComponent.GetConfig<ItemDataTable>();

// 通过整数主键获取（推荐使用 TryGet）
ItemData item1 = itemDataTable[1];

// 通过字符串键获取
ItemData item2 = itemDataTable["武器"];

// 安全获取（推荐，避免空引用异常）
if (itemDataTable.TryGet(1, out ItemData item3))
{
    UnityEngine.Debug.Log($"找到物品: {item3.Name}");
}

// 获取第一个和最后一个元素
ItemData first = itemDataTable.FirstOrDefault;
ItemData last = itemDataTable.LastOrDefault;

// 获取所有元素
ItemData[] allItems = itemDataTable.All;
```
> Source: [BaseDataTable.cs](https://github.com/GameFrameX/com.gameframex.unity.config/blob/main/Runtime/Config/Config/BaseDataTable.cs#L119-L204)

### 条件查询

使用 LINQ 风格的条件查询：

```csharp
// 查找第一个匹配的元素
ItemData found = itemDataTable.Find(item => item.Level == 1);

// 查找所有匹配的元素（数组）
ItemData[] level1Items = itemDataTable.FindListArray(item => item.Level == 1);

// 查找所有匹配的元素（列表）
List<ItemData> cheapItems = itemDataTable.FindList(item => item.Price < 50);

// 遍历所有元素
itemDataTable.ForEach(item => 
{
    UnityEngine.Debug.Log($"物品: {item.Name}, 价格: {item.Price}");
});
```
> Source: [BaseDataTable.cs](https://github.com/GameFrameX/com.gameframex.unity.config/blob/main/Runtime/Config/Config/BaseDataTable.cs#L296-L349)

### 聚合操作

数据表支持多种聚合计算：

```csharp
// 计算总和
int totalPrice = itemDataTable.Sum(item => item.Price);

// 获取最大值
int maxLevel = itemDataTable.Max(item => item.Level);

// 获取最小值
int minPrice = itemDataTable.Min(item => item.Price);

// 获取元素数量
int count = itemDataTable.Count;
```
> Source: [BaseDataTable.cs](https://github.com/GameFrameX/com.gameframex.unity.config/blob/main/Runtime/Config/Config/BaseDataTable.cs#L351-L449)

## API 参考

### IDataTable 接口

非泛型基础接口，定义数据表的基本功能。

#### `Task LoadAsync()`

异步加载数据表。

**返回：** 表示异步操作的任务

```csharp
await itemDataTable.LoadAsync();
```

#### `int Count { get; }`

获取数据表中对象的数量。

**返回：** 数据表中对象的数量

---

### `IDataTable<T>` 接口

泛型接口，提供强类型的数据访问和查询功能。

#### `bool TryGet(int id, out T value)`

尝试根据整数 ID 获取对象。

**参数：**
- `id` (int): 要获取的对象的整数 ID
- `value` (out T): 当找到对应 ID 的对象时，返回该对象；否则返回默认值

**返回：** 如果找到对应 ID 的对象则返回 `true`；否则返回 `false`

#### `bool TryGet(long id, out T value)`

尝试根据长整数 ID 获取对象。

**参数：**
- `id` (long): 要获取的对象的长整数 ID
- `value` (out T): 当找到对应 ID 的对象时，返回该对象；否则返回默认值

**返回：** 如果找到对应 ID 的对象则返回 `true`；否则返回 `false`

#### `bool TryGet(string id, out T value)`

尝试根据字符串 ID 获取对象。

**参数：**
- `id` (string): 要获取的对象的字符串 ID
- `value` (out T): 当找到对应 ID 的对象时，返回该对象；否则返回默认值

**返回：** 如果找到对应 ID 的对象则返回 `true`；否则返回 `false`

#### `T this[int id] { get; }`

根据整数主键获取数据表中的对象。

**参数：**
- `id` (int): 要获取的对象的整数主键

**返回：** 与指定主键关联的数据对象；如果找不到则返回 `null`

#### `T this[long id] { get; }`

根据长整数主键获取数据表中的对象。

**参数：**
- `id` (long): 要获取的对象的长整数主键

**返回：** 与指定主键关联的数据对象；如果找不到则返回 `null`

#### `T this[string id] { get; }`

根据字符串键获取数据表中的对象。

**参数：**
- `id` (string): 要获取的对象的字符串键

**返回：** 与指定键关联的数据对象；如果找不到则返回 `null`

#### `T FirstOrDefault { get; }`

获取数据表中第一个对象。

**返回：** 如果数据表为空，则返回 `null`；否则返回第一个对象

#### `T LastOrDefault { get; }`

获取数据表中最后一个对象。

**返回：** 如果数据表为空，则返回 `null`；否则返回最后一个对象

#### `T[] All { get; }`

获取数据表中所有对象。

**返回：** 包含数据表中所有对象的数组

#### `T[] ToArray()`

将数据表中所有对象复制到新数组。

**返回：** 包含数据表中所有对象的新数组

#### `List<T> ToList()`

将数据表中所有对象复制到新列表。

**返回：** 包含数据表中所有对象的新列表

#### `T Find(Func<T, bool> func)`

根据条件查找第一个匹配的对象。

**参数：**
- `func` (Func<T, bool>): 用于定义匹配条件的函数

**返回：** 如果找到匹配的对象，则返回该对象；否则返回 `null`

#### `T[] FindListArray(Func<T, bool> func)`

根据条件查找所有匹配的对象并返回数组。

**参数：**
- `func` (Func<T, bool>): 用于定义匹配条件的函数

**返回：** 包含所有匹配对象的数组

#### `List<T> FindList(Func<T, bool> func)`

根据条件查找所有匹配的对象并返回列表。

**参数：**
- `func` (Func<T, bool>): 用于定义匹配条件的函数

**返回：** 包含所有匹配对象的列表

#### `void ForEach(Action<T> func)`

对数据表中的每个元素执行指定的操作。

**参数：**
- `func` (`Action<T>`): 要对每个元素执行的操作

#### `Tk Max<Tk>(Func<T, Tk> func) where Tk : IComparable<Tk>`

获取数据表中指定属性的最大值。

**参数：**
- `func` (Func<T, Tk>): 用于获取比较值的函数

**类型参数：**
- `Tk`: 比较值的类型，必须实现 `IComparable<Tk>` 接口

**返回：** 指定属性的最大值

#### `Tk Min<Tk>(Func<T, Tk> func) where Tk : IComparable<Tk>`

获取数据表中指定属性的最小值。

**参数：**
- `func` (Func<T, Tk>): 用于获取比较值的函数

**类型参数：**
- `Tk`: 比较值的类型，必须实现 `IComparable<Tk>` 接口

**返回：** 指定属性的最小值

#### `int Sum(Func<T, int> func)`

计算数据表中指定 `int` 属性的总和。

**参数：**
- `func` (Func<T, int>): 用于获取求和值的函数

**返回：** 指定属性的总和

#### `long Sum(Func<T, long> func)`

计算数据表中指定 `long` 属性的总和。

**参数：**
- `func` (Func<T, long>): 用于获取求和值的函数

**返回：** 指定属性的总和

#### `float Sum(Func<T, float> func)`

计算数据表中指定 `float` 属性的总和。

**参数：**
- `func` (Func<T, float>): 用于获取求和值的函数

**返回：** 指定属性的总和

#### `double Sum(Func<T, double> func)`

计算数据表中指定 `double` 属性的总和。

**参数：**
- `func` (Func<T, double>): 用于获取求和值的函数

**返回：** 指定属性的总和

#### `decimal Sum(Func<T, decimal> func)`

计算数据表中指定 `decimal` 属性的总和。

**参数：**
- `func` (Func<T, decimal>): 用于获取求和值的函数

**返回：** 指定属性的总和

## 相关链接

- [ConfigComponent 配置组件](./4-core-concepts.config-component)
- [ConfigManager 配置管理器](./4-core-concepts.config-manager)
- [API 参考](./5-api-reference.interfaces)
- [数据表定义](./3-getting-started.data-table-definition)
