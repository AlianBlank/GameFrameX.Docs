---
title: "如何异步显示实体"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.entity
    commit: de90f8d7162fa6db458a88881883d01232939a39
    generator: OpenDeepWiki
---
# 如何异步显示实体

调用 `IEntityManager.ShowEntityAsync(...)` 即可异步加载并显示实体;它基于 `UniTask<IEntity>` 实现,资源加载完成后会自动完成实体实例化并返回。

## 前置条件

- 已通过 `EntityComponent` 初始化实体系统,并注册好实体组(`EntityGroup`)和实体资源辅助器(`IEntityHelper`)。
- 实体资源已配置在 YooAsset(或框架使用的资源管理器)中,确保 `IAssetManager.LoadAssetAsync` 能找到 `entityAssetName`。

## 操作步骤

1. 通过 `GameFrameworkEntry.GetModule<IEntityManager>()` 或自定义组件拿到 `IEntityManager` 实例。
2. 选择合适的重载,使用 `await` 等待实体显示完成。

```csharp
using Cysharp.Threading.Tasks;
using GameFrameX.Entity;
using UnityEngine;

public class MyEntityLoader : MonoBehaviour
{
    private IEntityManager _entityManager;

    private void Awake()
    {
        _entityManager = GameFrameworkEntry.GetModule<IEntityManager>();
    }

    public async UniTaskVoid LoadPlayerAsync(int entityId)
    {
        IEntity entity = await _entityManager.ShowEntityAsync(
            entityId,
            "Assets/Game/Prefabs/Player.prefab",
            "PlayerGroup");

        // 实体已显示,可继续后续逻辑
        Debug.Log($"实体 {entity.Id} 显示完成");
    }
}
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L296-L299)

3. 带优先级的加载(资源紧张时使用):

```csharp
IEntity entity = await _entityManager.ShowEntityAsync(
    entityId: 1001,
    entityAssetName: "Assets/Game/Prefabs/Player.prefab",
    entityGroupName: "PlayerGroup",
    priority: 10);
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L308-L311)

4. 携带自定义 `userData` 加载实体,可在 `OnShow` 中通过 `Entity.UserData` 取回:

```csharp
var data = new MySpawnData { Level = 5 };
IEntity entity = await _entityManager.ShowEntityAsync(
    entityId: 1002,
    entityAssetName: "Assets/Game/Prefabs/NPC.prefab",
    entityGroupName: "NPCGroup",
    userData: data);
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L320-L323)

5. 同时指定优先级与 `userData`(完整重载):

```csharp
IEntity entity = await _entityManager.ShowEntityAsync(
    entityId: 1003,
    entityAssetName: "Assets/Game/Prefabs/Boss.prefab",
    entityGroupName: "BossGroup",
    priority: 1,
    userData: bossData);
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L333-L396)

## 常见变体

### 异步隐藏实体

显示完成后,通过 `HideEntity` 配合 `await` 风格的帧切换即可隐藏;若需要 `OnHide` 回调也使用 `userData`:

```csharp
// 与异步显示搭配使用时,HideEntity 是同步调用
_entityManager.HideEntity(entityId: 1001, userData: hideData);
```

`Source:` [IEntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/IEntityManager.cs#L257-L264)

### 同时显示多个实体

```csharp
var tasks = new[]
{
    _entityManager.ShowEntityAsync(1, "Player.prefab", "PlayerGroup"),
    _entityManager.ShowEntityAsync(2, "Enemy.prefab", "EnemyGroup"),
    _entityManager.ShowEntityAsync(3, "NPC.prefab",   "NPCGroup"),
};
IEntity[] entities = await UniTask.WhenAll(tasks);
```

## 常见错误

| 错误现象 | 原因 | 处理方式 |
|---------|------|---------|
| `You must set resource manager first.` | 未注入 `IAssetManager` | 在 `EntityComponent` 初始化前调用 `SetResourceManager` |
| `You must set entity helper first.` | 未注入 `IEntityHelper` | 调用 `SetEntityHelper` 注册辅助器 |
| `Entity asset name is invalid.` | `entityAssetName` 为空 | 传入非空字符串 |
| `Entity group name is invalid.` | `entityGroupName` 为空或不存在 | 调用 `HasEntityGroup` 校验后再传入 |
| `Entity id '{0}' is already exist.` | 同一 `entityId` 已存在 | 使用新 ID,或先 `HideEntity` 旧实体 |
| `Entity '{0}' is already being loaded.` | 重复触发同一 `entityId` 的异步加载 | 等待上一次 `ShowEntityAsync` 完成后再发起 |

## 相关链接

- [IEntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/IEntityManager.cs#L223-L251) — 接口定义与四个重载
- [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L296-L397) — `ShowEntityAsync` 完整实现
- [EntityComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/EntityComponent.cs) — 实体组件入口