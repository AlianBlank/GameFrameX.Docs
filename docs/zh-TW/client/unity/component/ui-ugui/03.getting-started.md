# 快速開始

本文將引導你完成 UGUI 組件的基本配置，建立並顯示一個 UI 介面。

## 前置條件

- Unity 2019.4+
- GameFrameX Core、UI、Asset、Event 包已安裝
- UGUI 包已安裝

## 步驟

### 1. 建立 UI 窗體

1. 在 Unity 編輯器中右鍵 Hierarchy → `GameFrameX` → `Create UI Form`
2. 選擇 UGUI 模板
3. 配置 UI 窗體名稱和所屬 UI 組

### 2. 編寫 UI 邏輯

```csharp
using UnityEngine.UI;
using GameFrameX.UI;

public class MyUIForm : UIForm
{
    private Button _closeButton;

    protected override void OnInit(object userData)
    {
        base.OnInit(userData);
        _closeButton = GetComponentInChildren<Button>();
        _closeButton.onClick.AddListener(OnCloseButtonClick);
    }

    protected override void OnOpen(object userData)
    {
        base.OnOpen(userData);
        Debug.Log("UI 窗體已開啟");
    }

    private void OnCloseButtonClick()
    {
        Close();
    }
}
```

### 3. 開啟 UI 窗體

```csharp
var uiComponent = GameFrameworkEntry.GetComponent<UIComponent>();
uiComponent.OpenUIForm("MyUIForm");
```

## 下一步

- 閱讀 [UI 建立](02.create-ui.md) 瞭解更多 UI 建立方式
- 閱讀 [程式碼生成器](07.code-generator.md) 瞭解自動程式碼生成功能
