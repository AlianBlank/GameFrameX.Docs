---
title: "在定时器完成或移除时执行回调"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.timer
    commit: bb529b138aac40d0a198f9cd2fe46f55fb94844d
    generator: OpenDeepWiki
---
# 在定时器完成或移除时执行回调

在 `Add(...)` 调用时传入最后一个可选参数 `onComplete`（`System.Action`），定时器就会在**完成（重复次数耗尽）或被移除**时自动调用它。签名如下：

```csharp
int Add(float interval, int repeat, Action<object> callback, object callbackParam = null,
        string tag = null, TimerTimeScale timeScale = TimerTimeScale.Unscaled,
        Action onComplete = null);
```

Source: [ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs#L14-L22)

## 前置条件

- 场景中已创建并初始化 `TimerComponent`（它内部持有 `ITimerManager` 并转发 `Add`）。
Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L44-L47)

## 操作步骤

1. 准备一个普通的定时回调（`Action<object>`），它负责每次触发时的工作。
2. 在 `Add` 调用中用命名参数 `onComplete:` 传入完成回调（推荐用命名参数，因为它位于一串可选参数之后）。
3. 等待重复次数耗尽或手动移除，`onComplete` 会被自动调用一次。

```csharp
bool completed = false;

// 每 1 秒触发一次，共 3 次；第 3 次结束后 completed 变为 true
int id = timerComponent.Add(1f, 3, _ => { Debug.Log("tick"); },
                            onComplete: () => completed = true);
```

Source: [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Tests/UnitTests.cs#L660-L664)

手动移除同样会触发回调——例如通过回调函数移除：

```csharp
var callback = new Action<object>(_ => { });
_timerManager.Add(1f, 0, callback, onComplete: () => completed = true);
_timerManager.Remove(callback);   // 移除后 completed == true
```

Source: [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Tests/UnitTests.cs#L677-L684)

## 触发时机速查

| 场景 | `onComplete` 是否触发 | 依据 |
|------|----------------------|------|
| 有限次重复（`repeat > 0`）次数耗尽 | 触发 | [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L114-L122) |
| 通过 `Remove(Action<object> callback)` 移除 | 触发 | [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Tests/UnitTests.cs#L683-L684) |
| 无限重复（`repeat = 0`）一直运行 | 不触发 | [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Tests/UnitTests.cs#L688-L699) |
| `callback` 传入 null | 定时器创建失败，返回 0 | [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L221-L225) |

## 常见变体

- **与标签分组结合**：`onComplete` 是 `Add` 的参数之一，可与 `tag`、`timeScale` 一起使用，不影响按标签的 `PauseByTag` / `ResumeByTag` / `RemoveByTag` 操作。
  Source: [ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs#L93-L118)
- **复用同一回调重新添加**：对已存在的 `callback` 再次 `Add` 时会原位更新（`t.Set(interval, repeat, callback, callbackParam, tag, timeScale, onComplete)` 并重置计时），新传入的 `onComplete` 会覆盖旧的。
  Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L227-L243)

## 常见错误

- **无限重复的定时器上期望回调触发**：`repeat = 0` 表示无限重复，定时器永远不会"完成"，`onComplete` 不会触发；需要结束时机就显式设置有限次数或调用 `Remove`。
  Source: [UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Tests/UnitTests.cs#L688-L699)
- **回调为 null 导致定时器未创建**：`callback == null` 时仅输出警告 `Timer callback error` 前的日志 `"timer callback is null, " + interval + "," + repeat` 并返回 0，`onComplete` 自然也不会有触发机会；主回调 `callback` 必须非空（`onComplete` 本身可以为 null）。
  Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L221-L225)
- **参数顺序写错**：`onComplete` 是第 7 个参数，位置传参容易错位；建议始终使用命名参数 `onComplete: () => ...`。
  Source: [ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs#L22)

## 背景

`onComplete` 存储在内部 `TimerItem` 的 `OnComplete` 字段上（`public Action OnComplete;`），调度线程在把定时器标记为删除并真正移出字典时调用 `i.OnComplete?.Invoke()`；标签批量移除路径同样在移出标签索引后调用 `t.OnComplete?.Invoke()`，因此自然完成与被移除都会走这一个出口。

Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L134-L148)（自然完成路径）
Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L332-L334)（移除路径）

## 相关链接

- 关于定时器整体架构与快速上手，见概述页
- 关于暂停/恢复、标签分组管理，见《标签分组与批量操作》
- 接口完整定义：[ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs)
- 实现与调度逻辑：[TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs)
- Unity 组件封装：[TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs)
- 回调行为单元测试：[UnitTests.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Tests/UnitTests.cs#L657-L700)
