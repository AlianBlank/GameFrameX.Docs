# ProcedureManager

ProcedureManager 是 GameFrameX フロー管理系统的コア実装クラス，负责管理ゲーム中的各种フローステータス及其切换。

## 概要

ProcedureManager 実装了 `IProcedureManager` インターフェース，を継承 `GameFrameworkModule` 基クラス，是フロー管理模块的主要実装。它基づく有限状態機械（FSM）来実装フロー的作成、切换和管理機能。

**设计意图：**
- 提供统一的フロー管理机制，サポートゲーム在不同阶段（如启动、菜单、ゲーム中、暂停、结算等）之间平滑切换
- 利用 FSM 的特性，确保フロー切换的可靠性和可追踪性
- 作为 GameFramework 的コア模块，サポートホットアップデート和模块化扩展

## 架构

```mermaid
graph TD
    subgraph External["外部呼び出し"]
        Client[客户端/Unityコンポーネント]
    end
    
    subgraph GameFramework["Game Framework コア"]
        GFE["GameFrameworkEntry"]
        GFM["GameFrameworkModule (基クラス)"]
    end
    
    subgraph ProcedureModule["Procedure 模块"]
        PC["ProcedureComponent"]
        PM["ProcedureManager"]
        IPM["IProcedureManager インターフェース"]
        PB["ProcedureBase"]
    end
    
    subgraph FsmModule["FSM 模块 (依赖)"]
        FSM["IFsmManager"]
        FSMInst["IFsm&lt;IProcedureManager&gt;"]
    end
    
    Client -->|使用| PC
    PC -->|取得| IPM
    PM -->|実装| IPM
    PM -->|继承| GFM
    PM -->|CreateFsm| FSMInst
    FSMInst -->|管理ステータス| PB
    GFE -->|GetModule| PM
    GFE -->|GetModule| FSM
    
    style PM fill:#f9f,stroke:#333,stroke-width:2px
    style IPM fill:#ff9,stroke:#333,stroke-width:1px
```

### コンポーネント职责

| コンポーネント | 职责 |
|------|------|
| **ProcedureManager** | コア実装クラス，管理フロー的作成、切换、查询 |
| **IProcedureManager** | インターフェース定义，抽象フロー管理操作 |
| **ProcedureComponent** | Unity コンポーネント，に使用在シーン中配置和初期化フロー |
| **ProcedureBase** | フロー基クラス，每个具体フロー继承此クラス |
| **IFsmManager** | 有限状態機械管理器（外部依赖） |

## コア機能

### 1. 初期化フロー管理器

在使用フロー管理器之前，必要先进行初期化。初期化时必要提供 FSM 管理器和所有可用的フロークラス型。

```csharp
public void Initialize(IFsmManager fsmManager, params ProcedureBase[] procedures)
```

> Source: [ProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureManager.cs#L104-L110)

**参数説明：**
- `fsmManager`: 有限状態機械管理器，负责作成和管理 FSM インスタンス
- `procedures`: 可用的フローインスタンス数组

### 2. 启动フロー

初期化完成后，必要指定一个エントリーポイントフロー来启动フロー管理器。

```csharp
// 泛型方法
public void StartProcedure<T>() where T : ProcedureBase

// Type方法
public void StartProcedure(Type procedureType)
```

> Source: [ProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureManager.cs#L116-L138)

### 3. 查询フローステータス

提供多种方法查询当前フロー信息和检查特定フロー是否存在。

```csharp
// 取得当前フロー
public ProcedureBase CurrentProcedure { get; }

// 取得当前フロー持续时间
public float CurrentProcedureTime { get; }

// 检查是否存在指定フロー（泛型）
public bool HasProcedure<T>() where T : ProcedureBase

// 检查是否存在指定フロー（Type）
public bool HasProcedure(Type procedureType)
```

> Source: [ProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureManager.cs#L44-L71), [L145-L168](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureManager.cs#L145-L168)

### 4. 取得フローインスタンス

可能在运行时取得任意已注册的フローインスタンス进行操作。

```csharp
// 泛型方法取得
public ProcedureBase GetProcedure<T>() where T : ProcedureBase

// Type方法取得
public ProcedureBase GetProcedure(Type procedureType)
```

> Source: [ProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureManager.cs#L175-L198)

## フロー切换フロー

```mermaid
sequenceDiagram
    participant Unity as Unity引擎
    participant PC as ProcedureComponent
    participant PM as ProcedureManager
    participant FSM as IFsmManager
    participant PB as ProcedureBase

    Unity->>PC: Awake()
    PC->>PM: GameFrameworkEntry.GetModule&lt;IProcedureManager&gt;()
    PC->>PC: Start() コルーチン
    PC->>PM: Initialize(fsmManager, procedures)
    PM->>FSM: CreateFsm(this, procedures)
    FSM-->>PM: IFsm&lt;IProcedureManager&gt;
    PM-->>PC: 初期化完成
    PC->>PM: StartProcedure&lt;T&gt;()
    PM->>FSM: Start&lt;T&gt;()
    FSM->>PB: OnEnter() - 进入フロー
    Note over FSM,PB: フロー运行中...
    PB->>FSM: 触发フロー切换
    FSM->>PB: OnLeave() - 离开旧フロー
    FSM->>PB: OnEnter() - 进入新フロー
```

## 使用例

### 基本使用フロー

1. **作成自定义フロークラス**

```csharp
using GameFrameX.Procedure.Runtime;

public class LoginProcedure : ProcedureBase
{
    protected override void OnEnter(IFsm<IProcedureManager> procedureOwner)
    {
        base.OnEnter(procedureOwner);
        // 初期化登录フロー
    }

    protected override void OnUpdate(IFsm<IProcedureManager> procedureOwner, float elapseSeconds, float realElapseSeconds)
    {
        base.OnUpdate(procedureOwner, elapseSeconds, realElapseSeconds);
        // 检查是否满足切换条件
        // 如果满足，切换到下一个フロー
    }
}

public class GameProcedure : ProcedureBase
{
    protected override void OnEnter(IFsm<IProcedureManager> procedureOwner)
    {
        base.OnEnter(procedureOwner);
        // 开始ゲーム
    }
}
```

2. **配置 ProcedureComponent**

在 Unity シーン中添加 `ProcedureComponent` コンポーネント，并配置：
- `Available Procedure Type Names`: 添加所有フロー的クラス型名称
- `Entrance Procedure Type Name`: 設定エントリーポイントフロークラス型名称

3. **在代码中切换フロー**

```csharp
// 假设在某个 Procedure 中切换到下一个フロー
public class LoginProcedure : ProcedureBase
{
    protected override void OnUpdate(IFsm<IProcedureManager> procedureOwner, float elapseSeconds, float realElapseSeconds)
    {
        base.OnUpdate(procedureOwner, elapseSeconds, realElapseSeconds);
        
        // 登录完成后，切换到ゲームフロー
        if (IsLoginSuccess)
        {
            procedureOwner.ChangeState<GameProcedure>();
        }
    }
}
```

> Source: [ProcedureBase.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureBase.cs#L1-L77)

### 直接使用 ProcedureManager

如果必要直接使用 ProcedureManager 而非を通じて ProcedureComponent：

```csharp
// 取得フロー管理器
IProcedureManager procedureManager = GameFrameworkEntry.GetModule<IProcedureManager>();

// 取得 FSM 管理器
IFsmManager fsmManager = GameFrameworkEntry.GetModule<IFsmManager>();

// 初期化
procedureManager.Initialize(fsmManager, new LoginProcedure(), new GameProcedure());

// 启动エントリーポイントフロー
procedureManager.StartProcedure<LoginProcedure>();

// 在ゲーム循环中查询
ProcedureBase current = procedureManager.CurrentProcedure;
float time = procedureManager.CurrentProcedureTime;
```

## API 参考

### プロパティ

| プロパティ | クラス型 | 説明 |
|------|------|------|
| **CurrentProcedure** | `ProcedureBase` | 取得当前正在执行的フローインスタンス |
| **CurrentProcedureTime** | `float` | 取得当前フロー已持续的时间（秒） |

### メソッド

#### `Initialize(IFsmManager fsmManager, params ProcedureBase[] procedures)`

初期化フロー管理器。

**参数：**
- `fsmManager` (IFsmManager): 有限状態機械管理器
- `procedures` (params ProcedureBase[]): 可用的フローインスタンス数组

**異常：**
- `GameFrameworkException`: 当 fsmManager 为 null 时抛出

---

#### `StartProcedure<T>() where T : ProcedureBase`

开始指定的フロー（泛型方法）。

**参数：**
- 无

**異常：**
- `GameFrameworkException`: 当フロー管理器未初期化时抛出

---

#### `StartProcedure(Type procedureType)`

开始指定的フロー（Type 方法）。

**参数：**
- `procedureType` (Type): 要开始的フロークラス型

**異常：**
- `GameFrameworkException`: 当フロー管理器未初期化时抛出

---

#### `HasProcedure<T>() where T : ProcedureBase`

检查是否存在指定的フロー。

**返回：** 
- `bool`: 如果存在返回 true，否则返回 false

**異常：**
- `GameFrameworkException`: 当フロー管理器未初期化时抛出

---

#### `GetProcedure<T>() where T : ProcedureBase`

取得指定クラス型的フローインスタンス。

**返回：** 
- `ProcedureBase`: フローインスタンス

**異常：**
- `GameFrameworkException`: 当フロー管理器未初期化时抛出

---

### 生命周期メソッド

ProcedureManager を継承 `GameFrameworkModule`，主要生命周期メソッド：

| メソッド | 説明 |
|------|------|
| `Update(elapseSeconds, realElapseSeconds)` | 轮询メソッド，当前実装为空（由 FSM 自行处理） |
| `Shutdown()` | 关闭并清理フロー管理器，销毁 FSM |

> Source: [ProcedureManager.cs](https://github.com/GameFrameX/com.gameframex.unity.procedure/blob/main/Runtime/Procedure/ProcedureManager.cs#L78-L97)

## 依赖关系

ProcedureManager 依赖以下コンポーネント：

| 依赖项 | 説明 | 来源 |
|--------|------|------|
| **IFsmManager** | 有限状態機械管理器 | GameFrameX.Fsm.Runtime |
| **IFsm\<IProcedureManager\>** | フローステータス机インスタンス | GameFrameX.Fsm.Runtime |
| **GameFrameworkModule** | GameFramework 模块基クラス | GameFrameX.Runtime |
| **ProcedureBase** | フロー基クラス | 本模块 |

## 関連リンク

- [IProcedureManager](./3-core-concepts.iprocedure-manager) - フロー管理器インターフェース定义
- [ProcedureBase](./3-core-concepts.procedure-base) - フロー基クラス
- [ProcedureComponent](./3-core-concepts.procedure-component) - Unity フローコンポーネント
- [FSM 模块](https://github.com/GameFrameX/com.alianblank.gameframex.unity.fsm) - 有限状態機械模块
- [自定义フロー](./4-custom-procedures) - 如何作成自定义フロー
