# クイックスタート

本文将引导你完成フローコンポーネント的基本設定，作成并切换自定義フロー。

## 前置条件

- Unity 2019.4+
- GameFrameX Core パッケージ已インストール
- Procedure パッケージ已インストール
- FSM パッケージ已インストール（フローコンポーネント基于 FSM 実装）

## ステップ

### 1. 作成自定義フロー

```csharp
using GameFrameX.Procedure;

public class ProcedureLaunch : ProcedureBase
{
    protected override void OnEnter(ProcedureOwner procedureOwner)
    {
        base.OnEnter(procedureOwner);
        Debug.Log("进入启动流程");
    }

    protected override void OnUpdate(ProcedureOwner procedureOwner, float elapseSeconds, float realElapseSeconds)
    {
        base.OnUpdate(procedureOwner, elapseSeconds, realElapseSeconds);
        // 加载完成后切换到菜单流程
        ChangeState<ProcedureMenu>(procedureOwner);
    }
}

public class ProcedureMenu : ProcedureBase
{
    protected override void OnEnter(ProcedureOwner procedureOwner)
    {
        base.OnEnter(procedureOwner);
        Debug.Log("进入菜单流程");
    }
}
```

### 2. 設定可用フロー

在 Inspector 的 `ProcedureComponent` 中追加所有自定義フロー类。

### 3. 設定入口フロー

```csharp
var procedureComponent = GameFrameworkEntry.GetComponent<ProcedureComponent>();
// 入口流程为 Inspector 中配置的第一个流程
```

## 完全な例

```csharp
using UnityEngine;
using GameFrameX.Procedure;

// 启动流程
public class ProcedureLaunch : ProcedureBase
{
    protected override void OnEnter(ProcedureOwner procedureOwner)
    {
        Debug.Log("启动流程 - 加载资源...");
    }

    protected override void OnUpdate(ProcedureOwner procedureOwner, float elapseSeconds, float realElapseSeconds)
    {
        // 模拟加载完成后切换
        ChangeState<ProcedureMenu>(procedureOwner);
    }
}

// 菜单流程
public class ProcedureMenu : ProcedureBase
{
    protected override void OnEnter(ProcedureOwner procedureOwner)
    {
        Debug.Log("菜单流程 - 显示主界面");
    }
}
```

## 次のステップ

- 阅读 [自定義フロー](08.custom-procedures.md) 理解する更多フローモード
- 阅读 [エディタ使用](09.editor-usage.md) 理解する Inspector 設定メソッド
