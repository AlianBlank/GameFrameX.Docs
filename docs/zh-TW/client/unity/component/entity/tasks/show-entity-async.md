---
title: "如何非同步顯示實體"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.entity
    commit: de90f8d7162fa6db458a88881883d01232939a39
    generator: OpenDeepWiki
---
# 如何非同步顯示實體

呼叫 `IEntityManager.ShowEntityAsync(...)` 來非同步載入並顯示實體；它基於 `UniTask<IEntity>` 實作，當資源載入完成後，實體的實例化會自動完成並返回。

## 前置條件

- 已透過 `EntityComponent` 初始化實體系統，並已註冊實體群組（`EntityGroup`）與實體資源輔助器（`IEntityHelper`）。
- 已在 YooAsset（或框架所使用的資源管理器）中設定實體資源，確保 `IAssetManager.LoadAssetAsync` 能找到 `entityAssetName`。

## 步驟

1. 透過 `GameFrameworkEntry.GetModule<IEntityManager>()` 或自訂元件取得 `IEntityManager` 實例。
2. 選擇合適的多載方法，並使用 `await` 等待實體顯示完成。

```csharp
using Cysharp.Threading.Tasks;
using GameFrameX.Entity;
using UnityEngine;

public class MyEntityLoader : MonoBehaviour
{
    private IEntityManager _entityManager;

    private void Awake()
    {
        _entityManager = GameFrameworkEntry.GetModule<IEntityManager>();
    }

    public async UniTaskVoid LoadPlayerAsync(int entityId)
    {
        IEntity entity = await _entityManager.ShowEntityAsync(
            entityId,
            "Assets/Game/Prefabs/Player.prefab",
            "PlayerGroup");

        // 實體已顯示完成，可繼續後續邏輯
        Debug.Log($"Entity {entity.Id} has been displayed");
    }
}
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L296-L299)

3. 帶優先順序的載入（在資源吃緊時使用）：

```csharp
IEntity entity = await _entityManager.ShowEntityAsync(
    entityId: 1001,
    entityAssetName: "Assets/Game/Prefabs/Player.prefab",
    entityGroupName: "PlayerGroup",
    priority: 10);
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L308-L311)

4. 載入帶有自訂 `userData` 的實體，可在 `OnShow` 中透過 `Entity.UserData` 取得：

```csharp
var data = new MySpawnData { Level = 5 };
IEntity entity = await _entityManager.ShowEntityAsync(
    entityId: 1002,
    entityAssetName: "Assets/Game/Prefabs/NPC.prefab",
    entityGroupName: "NPCGroup",
    userData: data);
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L320-L323)

5. 同時指定優先順序與 `userData`（完整多載）：

```csharp
IEntity entity = await _entityManager.ShowEntityAsync(
    entityId: 1003,
    entityAssetName: "Assets/Game/Prefabs/Boss.prefab",
    entityGroupName: "BossGroup",
    priority: 1,
    userData: bossData);
```

`Source:` [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L333-L396)

## 常見變化

### 非同步隱藏實體

實體顯示完成後，可結合 `await` 式的幀切換，使用 `HideEntity` 將其隱藏；若需要 `OnHide` 回呼，也可以傳入 `userData`：

```csharp
// 與非同步顯示搭配使用時，HideEntity 為同步呼叫
_entityManager.HideEntity(entityId: 1001, userData: hideData);
```

`Source:` [IEntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/IEntityManager.cs#L257-L264)

### 一次顯示多個實體

```csharp
var tasks = new[]
{
    _entityManager.ShowEntityAsync(1, "Player.prefab", "PlayerGroup"),
    _entityManager.ShowEntityAsync(2, "Enemy.prefab", "EnemyGroup"),
    _entityManager.ShowEntityAsync(3, "NPC.prefab",   "NPCGroup"),
};
IEntity[] entities = await UniTask.WhenAll(tasks);
```

## 常見錯誤

| 錯誤訊息 | 原因 | 解決方式 |
|---------|------|---------|
| `You must set resource manager first.` | 未注入 `IAssetManager` | 在初始化 `EntityComponent` 之前呼叫 `SetResourceManager` |
| `You must set entity helper first.` | 未注入 `IEntityHelper` | 呼叫 `SetEntityHelper` 註冊輔助器 |
| `Entity asset name is invalid.` | `entityAssetName` 為空 | 傳入非空字串 |
| `Entity group name is invalid.` | `entityGroupName` 為空或不存在 | 傳入前先用 `HasEntityGroup` 進行驗證 |
| `Entity id '{0}' is already exist.` | 已存在相同 `entityId` 的實體 | 使用新的 ID，或先將舊實體 `HideEntity` |
| `Entity '{0}' is already being loaded.` | 同一個 `entityId` 被重複觸發非同步載入 | 等待前一個 `ShowEntityAsync` 完成後再重新發起 |

## 相關連結

- [IEntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/IEntityManager.cs#L223-L251) — 介面定義與四個多載方法
- [EntityManager.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/Entity/EntityManager.cs#L296-L397) — `ShowEntityAsync` 的完整實作
- [EntityComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.entity/blob/main/Runtime/Entity/EntityComponent.cs) — 實體元件入口