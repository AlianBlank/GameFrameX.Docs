# 界面组辅助器

`FairyGUIUIGroupHelper` 是 GameFrameX UI FairyGUI 插件的コアコンポーネント之一，负责在 FairyGUI 渲染引擎中作成和管理 UI 界面组。界面组是组织和管理多个界面表单（窗口）的容器，を通じて深度（Depth）控制界面的显示层级。

## 概要

界面组辅助器是 GameFrameX UI 系统与 FairyGUI 渲染引擎之间的桥梁コンポーネント。它を継承 `UIGroupHelperBase` 抽象クラス，提供します FairyGUI 特定的界面组実装。在 UI 系统中，界面组に使用将関連的界面表单组织在一起，并を通じて深度值控制界面的遮挡关系和显示顺序。

**コア职责：**
- 作成 FairyGUI 界面组容器（GComponent）
- 管理界面组的深度（Z 顺序）
- 設定界面组全屏显示
- 与 GameFrameX コア系统集成

> Source: [FairyGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.fairygui/blob/main/Runtime/FairyGUIUIGroupHelper.cs#L36-L97)

## アーキテクチャ設計

### クラス图结构

```mermaid
classDiagram
    class UIGroupHelperBase {
        <<abstract>>
        +Depth: int { get; protected set; }
        +Handler(root, groupName, helperTypeName, customHelper, depth) IUIGroupHelper
        +SetDepth(depth) void
    }
    
    class FairyGUIUIGroupHelper {
        +Depth: int { get; protected set; }
        +Handler(root, groupName, helperTypeName, customHelper, depth) IUIGroupHelper
        +SetDepth(depth) void
    }
    
    class GComponent {
        +name: string
        +opaque: bool
        +displayObject: DisplayObject
        +AddRelation(target, type) void
        +MakeFullScreen() void
    }
    
    class GRoot {
        +inst: GRoot
    }
    
    UIGroupHelperBase <|-- FairyGUIUIGroupHelper
    FairyGUIUIGroupHelper --> GComponent : creates
    GComponent --> GRoot : added as child
```

### 系统集成架构

```mermaid
flowchart TD
    subgraph UI系统["GameFrameX UI 系统"]
        UIManager[UI 管理器]
        UIComponent[UI コンポーネント]
    end
    
    subgraph FairyGUI插件["GameFrameX UI FairyGUI"]
        FairyGUIUIGroupHelper["界面组辅助器<br/>FairyGUIUIGroupHelper"]
        FairyGUIFormHelper["界面辅助器<br/>FairyGUIFormHelper"]
        FairyGUIPackageComponent["包コンポーネント<br/>FairyGUIPackageComponent"]
    end
    
    subgraph FairyGUI引擎["FairyGUI 引擎"]
        GRoot[GRoot 根容器]
        GComponent[GComponent コンポーネント]
    end
    
    UIComponent -->|作成组| FairyGUIUIGroupHelper
    FairyGUIUIGroupHelper -->|作成容器| GComponent
    GComponent -->|添加到| GRoot
    FairyGUIFormHelper -->|管理界面| FairyGUIUIGroupHelper
```

### 界面组层级关系

```mermaid
graph TD
    GRoot["GRoot (根容器)"] --> UIGroup1["界面组 1 (Depth=0)"]
    GRoot --> UIGroup2["界面组 2 (Depth=1)"]
    GRoot --> UIGroup3["界面组 3 (Depth=2)"]
    
    UIGroup1 --> FormA["表单 A"]
    UIGroup1 --> FormB["表单 B"]
    UIGroup2 --> FormC["表单 C"]
    UIGroup3 --> FormD["表单 D"]
    
    style GRoot fill:#f9f,color:#000
    style UIGroup1 fill:#bfb,color:#000
    style UIGroup2 fill:#bfb,color:#000
    style UIGroup3 fill:#bfb,color:#000
```

## コア機能

### 作成界面组

`Handler` メソッド是界面组辅助器的コアメソッド，负责作成并初期化一个新的 FairyGUI 界面组。

```csharp
public override IUIGroupHelper Handler(Transform root, string groupName, string uiGroupHelperTypeName, IUIGroupHelper customUIGroupHelper, int depth = 0)
{
    SetDepth(depth);
    GComponent component = new GComponent();
    GRoot.inst.AddChild(component);
    var comName = groupName;
    component.displayObject.name = comName;
    component.gameObjectName = comName;
    component.name = comName;
    component.opaque = false;
    component.AddRelation(GRoot.inst, RelationType.Width);
    component.AddRelation(GRoot.inst, RelationType.Height);
    component.MakeFullScreen();
    return GameFrameX.Runtime.Helper.CreateHelper(component.displayObject.gameObject, uiGroupHelperTypeName, (UIGroupHelperBase)customUIGroupHelper, 0);
}
```

> Source: [FairyGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.fairygui/blob/main/Runtime/FairyGUIUIGroupHelper.cs#L82-L96)

**作成フロー説明：**

1. **設定深度**：呼び出し `SetDepth(depth)` 設定界面组的 Z 顺序
2. **作成コンポーネント**：使用 `new GComponent()` 作成 FairyGUI 容器コンポーネント
3. **添加到根容器**：将コンポーネント添加到 `GRoot.inst`（FairyGUI 全局根容器）
4. **設定名称**：設定コンポーネント的 `name`、`gameObjectName` 和 `displayObject.name`
5. **設定透明**：設定 `opaque = false` 使界面组容器本身透明
6. **全屏設定**：添加宽度和高度关联，使用 `MakeFullScreen()` 使コンポーネント占满全屏
7. **作成辅助器**：使用 `GameFrameX.Runtime.Helper.CreateHelper` 作成实际的辅助器インスタンス

### 設定界面深度

`SetDepth` メソッドに使用設定界面组的深度值，该值决定了界面的显示层级。

```csharp
public override void SetDepth(int depth)
{
    Depth = depth;
    transform.localPosition = new Vector3(0, 0, depth * 100);
}
```

> Source: [FairyGUIUIGroupHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.fairygui/blob/main/Runtime/FairyGUIUIGroupHelper.cs#L63-L67)

**深度設定原理：**
- FairyGUI 使用 Z 轴坐标控制显示层级
- 每个深度等级对应 100 单位的 Z 轴距离
- 深度值越大，界面显示越靠前（覆盖深度值较小的界面）

## 界面组与表单的关系

界面组与界面表单（Form）之间是一对多的包含关系：

```mermaid
sequenceDiagram
    participant UIComp as UIComponent
    participant GroupHelper as FairyGUIUIGroupHelper
    participant FormHelper as FairyGUIFormHelper
    participant GR as GRoot
    participant GC as GComponent

    UIComp->>GroupHelper: 作成界面组
    GroupHelper->>GC: 作成 GComponent
    GC->>GR: 添加为子对象
    GroupHelper-->>UIComp: 返回组辅助器
    
    UIComp->>FormHelper: 作成界面表单
    FormHelper->>GC: 取得界面组容器
    GC->>GC: 添加表单为子对象
    FormHelper-->>UIComp: 返回表单インスタンス
```

### 界面组分配

在 FairyGUI 系统中，界面表单を通じて `OptionUIGroupAttribute` 特性分配到指定的界面组：

```csharp
[OptionUIGroup("MainUI")]
public class MainMenuForm : FUI
{
    // 界面表单逻辑
}
```

> Source: [FairyGUIFormHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.fairygui/blob/main/Runtime/FairyGUIFormHelper.cs#L108-L115)

系统会に基づいて `OptionUIGroup` 特性中指定的组名称取得对应的界面组インスタンス，并将界面表单添加到该界面组的 GComponent 容器中。

## 使用例

### 基本配置

在使用界面组辅助器之前，必要在 GameFrameX 启动配置中注册 FairyGUI 関連的コンポーネント：

```csharp
// 在 GameFrameworkComponent 中配置
// 使用 FairyGUIUIGroupHelper 作为界面组辅助器実装
```

### 界面组定义示例

```csharp
// 界面组を通じて UIManager 在运行时自动作成
// 以下是系统内部的作成逻辑

// 1. 当必要作成新界面时，检查是否存在对应的界面组
// 2. 如果不存在，使用 FairyGUIUIGroupHelper 作成新的界面组
// 3. 界面组的深度由系统に基づいて配置或自动递增分配
```

## API 参考

### `FairyGUIUIGroupHelper` クラス

**名前空間：** `GameFrameX.UI.FairyGUI.Runtime`

**继承：** `UIGroupHelperBase`

---

### プロパティ

#### `Depth`

```csharp
public override int Depth { get; protected set; }
```

取得界面组的深度值。

- **クラス型：** `int`
- **説明：** 表示界面组的 Z 轴深度，に使用控制界面的显示层级

---

### メソッド

#### `Handler`

```csharp
public override IUIGroupHelper Handler(
    Transform root,
    string groupName,
    string uiGroupHelperTypeName,
    IUIGroupHelper customUIGroupHelper,
    int depth = 0
)
```

作成并初期化一个 FairyGUI 界面组。

**参数：**
- `root` (Transform): 根节点（当前実装未使用）
- `groupName` (string): 界面组的名称
- `uiGroupHelperTypeName` (string): 界面组辅助器クラス型名称
- `customUIGroupHelper` (IUIGroupHelper): 自定义的界面组辅助器インスタンス
- `depth` (int): 界面组的深度值，默认为 0

**返回值：** 
- `IUIGroupHelper`: 作成的界面组辅助器インスタンス

**実装説明：**
1. 設定界面组深度
2. 作成 `GComponent` 作为界面组容器
3. 将容器添加到 `GRoot.inst`
4. 配置全屏显示プロパティ
5. 作成并返回辅助器インスタンス

---

#### `SetDepth`

```csharp
public override void SetDepth(int depth)
```

設定界面组的深度。

**参数：**
- `depth` (int): 界面组的深度值

**実装説明：**
を通じて修改 `transform.localPosition` 的 Z 坐标来実装深度設定，Z 坐标计算公式为：`depth * 100`。这种设计确保不同深度的界面之间有足够的 Z 轴间隔，避免渲染冲突。

---

## 関連リンク

- [FUI 界面基クラス](./4-core-concepts.1-fui-base-class) - FairyGUI 界面表单的基クラス実装
- [界面管理器](./4-core-concepts.2-ui-manager) - 界面系统的コア管理器
- [パッケージ管理器](./4-core-concepts.3-package-manager) - FairyGUI リソースパッケージ管理
- [界面辅助器](./4-core-concepts.4-form-helper) - 界面表单的作成和释放管理
- [界面组配置](./6-configuration.2-ui-group-config) - 界面组的配置选项
- [GitHub 仓库](https://github.com/GameFrameX/com.gameframex.unity.ui.fairygui.git) - 插件源代码
