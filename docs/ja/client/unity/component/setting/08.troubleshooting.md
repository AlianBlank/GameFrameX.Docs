# よくある

ドキュメントた GameFrameX Setting コンポーネントのよくある、エラー。

## 

GameFrameX Setting コンポーネントはゲームフレームワークのコアコンポーネント，ゲームの。に，、データ、プラットフォーム。ドキュメントのと，と。

## 

###  1：Setting manager is invalid

**エラー：**
```
Log.Fatal("Setting manager is invalid.");
```

**：**
エラー `SettingComponent` に `Awake()`  `GameFrameworkEntry`  `ISettingManager` モジュール。は GameFramework コアコンポーネント。

**：**

1. に GameFrameX コアコンポーネント
2.  `GameFrameworkEntry` は
3.  SettingComponent にの GameObject 

```csharp
// 正确の初始化顺序
// 1. 先初始化 GameFrameworkEntry
GameFrameworkEntry.Initialize();

// 2. 然后 SettingComponent 会自动を通じて Awake 获取マネージャー
var settingComponent = gameObject.AddComponent<SettingComponent>();
```

###  2：Can not create setting helper

**エラー：**
```
Log.Error("Can not create setting helper.");
```

**：**
エラーのタイプ。：
- の Helper タイプ
- の Helper クラスに
- Helper タイプ

**：**

1.  `SettingComponent` Inspector の `Setting Helper Type Name` 
2. タイプ（）

```csharp
// デフォルト設定
// Setting Helper Type Name: GameFrameX.Setting.Runtime.PlayerPrefsSettingHelper
// 或
// Setting Helper Type Name: GameFrameX.Setting.Runtime.DefaultSettingHelper
```

3. の Helper タイプ：
   - `GameFrameX.Setting.Runtime.PlayerPrefsSettingHelper` - にづく Unity PlayerPrefs
   - `GameFrameX.Setting.Runtime.DefaultSettingHelper` - にづくローカルファイル

###  3：のコンポーネント

**エラー：**
Unity エラーコンポーネント

**：**
`SettingComponent` た `[RequireComponent(typeof(GameFrameXSettingCroppingHelper))]`， `GameFrameXSettingCroppingHelper` コンポーネント。

**：**

1. SettingComponent  GameFrameXSettingCroppingHelper コンポーネント
2. ， GameFrameXSettingCroppingHelper  GameObject

## データ

###  4：Load settings failure

**エラー：**
```
Log.Error("Load settings failure.");
```

**：**
ロード，：
- ファイル
- 
- ファイルパスに
- /エラー

**：**

1. ** DefaultSettingHelper：**
```csharp
// 检查ファイルは否存に
var helper = settingComponent.GetComponent<DefaultSettingHelper>();
string filePath = helper.FilePath;
Debug.Log($"Settings file path: {filePath}");
```

2. **：**
```csharp
try
{
    settingComponent.Save();
}
catch (Exception ex)
{
    Debug.LogError($"Save failed: {ex.Message}");
}
```

3. **ファイル：**
   -  Application.persistentDataPath 
   - は

###  5：

**：**
びし `Save()` メソッド，にロード

**：**

1. **びし Save()**：に，
2. **PlayerPrefs **：にプラットフォーム PlayerPrefs.Save() びし
3. **WebGL プラットフォーム**：プラットフォームの

**：**

```csharp
// 1. 修改設定后必須呼び出し Save()
settingComponent.SetString("PlayerName", "NewName");
settingComponent.Save();  // 必須呼び出し

// 2. 使用 DefaultSettingHelper 确保データ持久化
// 其会自动保存到 Application.persistentDataPath/GameFrameworkSetting.dat
```

###  6：GetAllSettingNames す null サポート

**エラー：**
```
Log.Warning("GetAllSettingNames is not supported.");
```

**：**
`PlayerPrefsSettingHelper` サポートの， Unity PlayerPrefs の API。

> Source: [PlayerPrefsSettingHelper.cs#L96-L100](https://github.com/GameFrameX/com.gameframex.unity.setting/blob/main/Runtime/Setting/PlayerPrefsSettingHelper.cs#L96-L100)

**：**

1. ** DefaultSettingHelper **： Helper サポートの

```csharp
// に SettingComponent Inspector 中将 Helper タイプ改为：
// GameFrameX.Setting.Runtime.DefaultSettingHelper
```

2. ****： PlayerPrefs，の

###  7：Count プロパティす -1

**：**
びし `settingComponent.Count` す -1

**：**
`PlayerPrefsSettingHelper` の Count プロパティす -1， Unity PlayerPrefs のインターフェース。

> Source: [PlayerPrefsSettingHelper.cs#L52-L55](https://github.com/GameFrameX/com.gameframex.unity.setting/blob/main/Runtime/Setting/PlayerPrefsSettingHelper.cs#L52-L55)

```csharp
public override int Count
{
    get { return -1; }  // PlayerPrefs 不サポート获取数量
}
```

**：**

1.  `DefaultSettingHelper` の
2. 

## プラットフォーム

###  8：WebGL プラットフォーム

**：**
に、、プラットフォーム，

**：**
プラットフォームの API，サポート Unity  PlayerPrefs。フレームワークプラットフォームた：

> Source: [PlayerPrefsSettingHelper.cs#L80-L86](https://github.com/GameFrameX/com.gameframex.unity.setting/blob/main/Runtime/Setting/PlayerPrefsSettingHelper.cs#L80-L86)

```csharp
public override bool Save()
{
#if UNITY_WEBGL && (ENABLE_DOUYIN_MINI_GAME || ENABLE_WECHAT_MINI_GAME || ENABLE_KUAISHOU_MINI_GAME)
    return true;  // 小程序プラットフォーム无需手动保存
#else
    UnityEngine.PlayerPrefs.Save();
    return true;
#endif
}
```

**：**

1. たのプラットフォーム：
   - `ENABLE_DOUYIN_MINI_GAME` - 
   - `ENABLE_WECHAT_MINI_GAME` - 
   - `ENABLE_KUAISHOU_MINI_GAME` - 

2. に Unity Build Settings プラットフォーム

###  9：

**：**
 `SetObject()`  `GetObject()` ，データ

**：**

1. タイプサポート JSON 
2. むの（ delegate、EventHandler）
3. 

**：**

1. クラス `[Serializable]`
2. のデータ
3. 

```csharp
[Serializable]
public class PlayerData
{
    public string Name;
    public int Level;
    public List<Item> Items;
}

[Serializable]
public class Item
{
    public int Id;
    public int Count;
}

// 使用例
var playerData = new PlayerData
{
    Name = "Player1",
    Level = 10,
    Items = new List<Item>()
};

settingComponent.SetObject("PlayerData", playerData);
settingComponent.Save();

// 读取データ
var loadedData = settingComponent.GetObject<PlayerData>("PlayerData");
```

## API 

###  10：Setting name is invalid

**エラー：**
 `GameFrameworkException("Setting name is invalid.")`

**：**
 API は。 null ，。

> Source: [SettingManager.cs#L215-L218](https://github.com/GameFrameX/com.gameframex.unity.setting/blob/main/Runtime/Setting/Setting/SettingManager.cs#L215-L218)

```csharp
if (string.IsNullOrEmpty(settingName))
{
    throw new GameFrameworkException("Setting name is invalid.");
}
```

**：**

```csharp
// エラー用法
settingComponent.SetBool("", true);  // 抛出例外

// 正确用法
settingComponent.SetBool("IsFullScreen", true);
```

###  11：す

**：**
デフォルトのメソッドにの，すの

**：**
のデータタイプにデフォルトパラメータのデフォルトす：

| データタイプ | デフォルトメソッドす |
|----------|-------------------|
| bool | false |
| int | 0 |
| float | 0f |
| string | null |

**：**

1. **デフォルトのメソッド**：
```csharp
// 不推荐
bool isFullScreen = settingComponent.GetBool("IsFullScreen");

// 推荐
bool isFullScreen = settingComponent.GetBool("IsFullScreen", false);
```

2. **はに**：
```csharp
if (settingComponent.HasSetting("PlayerName"))
{
    string name = settingComponent.GetString("PlayerName");
}
else
{
    // 处理不存にの情况
}
```

## デバッグ

### デバッグログ

ログ：

```csharp
// にコード中添加デバッグ信息
Debug.Log($"Settings Count: {settingComponent.Count}");
Debug.Log($"Has PlayerName: {settingComponent.HasSetting("PlayerName")}");

// 获取所有設定名称（仅 DefaultSettingHelper サポート）
string[] allNames = settingComponent.GetAllSettingNames();
foreach (var name in allNames)
{
    Debug.Log($"Setting: {name}");
}
```

###  Helper 

```csharp
// 获取当前使用の Helper 信息
var helper = settingComponent.GetComponent<SettingHelperBase>();
Debug.Log($"Helper Type: {helper.GetType().Name}");
Debug.Log($"Helper FilePath: {(helper is DefaultSettingHelper dh ? dh.FilePath : "N/A")}");
```

## エラー

| エラー |  |  |
|----------|------|----------|
| Setting manager is invalid | GameFramework  |  GameFrameworkEntry  |
| Can not create setting helper | Helper タイプエラー |  Inspector  |
| Load settings failure | ファイルパスエラー | ファイルパスと |
| Save settings failure |  | と |
| GetAllSettingNames is not supported | た PlayerPrefsHelper |  DefaultSettingHelper |
| Setting name is invalid |  |  |
| Count returns -1 | PlayerPrefs サポート |  DefaultSettingHelper |

## リンク

- [](./04.features) - た Setting コンポーネントのコア
- [](./05.helper-implementation) - カスタマイズ Helper 
- [プラットフォーム](./06.platforms) - プラットフォーム
- [API リファレンス](./07.api-reference) - の API メソッドドキュメント
