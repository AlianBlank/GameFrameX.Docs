# 常见问题

本文档收集了使用 GameFrameX Advertisement 模块时常见的问题及其解決策。

## 概要

GameFrameX Advertisement 模块为 Unity プロジェクト提供します统一的广告集成抽象层。由于广告機能涉及多个平台（Android、iOS、WebGL）以及多个广告网络 SDK（AdMob、Unity Ads、IronSource 等），在实际使用过程中可能遇到各种配置和运行时问题。本文档旨在帮助開発者快速定位和解决这些常见问题。

> **ヒント**：在排查问题时，お願いします首先确认您已正确安装并配置了广告模块。如需基础使用説明，お願いします参阅 [快速开始](../3-getting-started) 和 [コア概念](../4-core-concepts) 文档。

## コア架构

在深入排查问题之前，了解模块的コア架构有助于更好地理解问题根源：

```mermaid
flowchart TD
    subgraph Unity["Unity シーン层"]
        GameObj[GameObject + AdvertisementComponent]
    end

    subgraph Framework["GameFrameX フレームワーク层"]
        GFX[GameFrameworkEntry]
        AdComp[AdvertisementComponent]
    end

    subgraph Interface["インターフェース抽象层"]
        IAM[IAdvertisementManager]
        BAM[BaseAdvertisementManager]
    end

    subgraph Impl["具体実装层"]
        ADM[广告管理器実装<br/>AdMob/IronSource/UnityAds...]
    end

    GameObj --> AdComp
    AdComp --> GFX
    GFX -->|GetModule| IAM
    IAM <|.. BAM
    BAM <|-- ADM
```

**工作フロー：**
1. 開発者将 `AdvertisementComponent` 添加到シーン中的 GameObject
2. `AdvertisementComponent` 在 `Awake()` 中を通じて `GameFrameworkEntry.GetModule<IAdvertisementManager>()` 取得广告管理器
3. 在 `Start()` 时呼び出し `Initialize()` 初期化广告管理器
4. を通じて `Load()`、`Show()`、`Play()` メソッド与广告系统交互

---

## よくある質問与解決策

### 问题一：广告管理器初期化失败

#### 症状

运行ゲーム时，控制台显示以下错误：
```
Log.Fatal: Advertisement manager is invalid.
```

#### 原因分析

此错误表示 `GameFrameworkEntry.GetModule<IAdvertisementManager>()` 返回了 `null`，即フレームワーク未能找到 `IAdvertisementManager` インターフェース的実装。

可能的根本原因：
1. **未注册广告管理器実装**：フレームワーク中没有任何模块実装 `IAdvertisementManager` インターフェース
2. **程序集剥离问题**：Unity 的 IL2CPP 或代码剥离导致广告管理器実装クラス被移除
3. **模块加载顺序问题**：广告模块在其他依赖模块之前被加载

#### 解決策

**手順 1：确认実装済み广告管理器**

确保您的プロジェクト中存在 `IAdvertisementManager` インターフェース的具体実装クラス。例如：

```csharp
using GameFrameX.Advertisement.Runtime;
using GameFrameX.Runtime;
using UnityEngine;
using UnityEngine.Scripting;

[Preserve]
public class MyAdsManager : BaseAdvertisementManager
{
    public override void Initialize(string adUnitId, bool debug = false)
    {
        // 実装广告 SDK の初期化論理
    }

    public override void Play(System.Action<bool> playResult, string customData = null)
    {
        // 実装广告播放逻辑
    }

    public override void Show(System.Action<string> success, System.Action<string> fail, 
        System.Action<bool> onShowResult, string customData = null)
    {
        // 実装广告展示逻辑
    }

    public override void Load(System.Action<string> success, System.Action<string> fail, 
        string customData = null)
    {
        // 実装广告加载逻辑
    }
}
```

> Source: [BaseAdvertisementManager.cs]({{file_base_url}}/Runtime/Advertisement/BaseAdvertisementManager.cs#L11-L88)

**手順 2：解决程序集剥离问题**

如果使用了 IL2CPP 或代码裁剪，确保添加 `[Preserve]` プロパティ（Unity 会保留带有此プロパティ的クラス）：

```csharp
[Preserve]
public class MyAdsManager : BaseAdvertisementManager
{
    // ...
}
```

同时在 `link.xml`（位于 `Assets/Plugins/` ディレクトリ下）中添加保留规则：

```xml
<linker>
    <assembly fullname="YourAssemblyName" preserve="all">
        <type fullname="YourNamespace.MyAdsManager" preserve="all"/>
    </assembly>
</linker>
```

**手順 3：检查模块注册**

确保在ゲーム启动时正确注册了广告管理器。具体方法取决于您的 GameFrameX フレームワーク配置方法。

---

### 问题二：广告位 ID 为空

#### 症状

控制台输出警告或错误，表明广告位 ID 未設定：
```
AdvertisementComponent: AdUnitId is empty!
```

#### 原因分析

此问题发生在 `AdvertisementComponent` 尝试に基づいて当前平台选择广告位 ID 时，未能找到对应的配置。

检查代码逻辑（见 `AdvertisementComponent.cs` 第 69-87 行）：

```csharp
AdUnitId =
#if UNITY_WEBGL
#if ENABLE_WECHAT_MINI_GAME
        m_adUnitIdWebGLWeChat
#elif ENABLE_DOUYIN_MINI_GAME
        m_adUnitIdWebGLDouYin
#elif ENABLE_KUAISHOU_MINI_GAME
        m_adUnitIdWebGLKuaiShou
#else
        m_adUnitIdWebGL
#endif
#elif UNITY_ANDROID
        m_adUnitIdAndroid
#elif UNITY_IOS
        m_adUnitIdiOS
#else
        string.Empty
#endif
        ;
```

> Source: [AdvertisementComponent.cs]({{file_base_url}}/Runtime/Advertisement/AdvertisementComponent.cs#L69-L87)

#### 解決策

**手順 1：在 Inspector 中配置广告位 ID**

1. 在 Unity シーン中选择添加了 `AdvertisementComponent` 的 GameObject
2. 在 Inspector 面板中，に基づいて目标平台填写对应的广告位 ID：
   - **Android 广告位ID**：填写 Google AdMob 或其他 Android 广告网络的 ID
   - **IOS 广告位ID**：填写 iOS 广告网络的 ID
   - **WebGL 广告位ID**：填写 Web 平台广告 ID
   - **WebGL 微信/抖音/快手 广告位ID**：填写对应小程序平台的广告 ID

> Source: [AdvertisementComponentInspector.cs]({{file_base_url}}/Editor/Inspector/AdvertisementComponentInspector.cs#L41-L47)

**手順 2：验证平台宏定义**

如果广告位 ID 仍然为空，お願いします检查プロジェクト的编译宏定义：
- 打开 `Edit > Project Settings > Player`
- 在 **Other Settings** 中检查 **Scripting Define Symbols**
- 确保为 WebGL 平台定义了正确的宏：`ENABLE_WECHAT_MINI_GAME`、`ENABLE_DOUYIN_MINI_GAME` 或 `ENABLE_KUAISHOU_MINI_GAME`

**手順 3：使用代码动态設定**

如果必要在运行时动态設定广告位 ID：

```csharp
using GameFrameX.Advertisement.Runtime;
using UnityEngine;

public class AdInitializer : MonoBehaviour
{
    public AdvertisementComponent adComponent;
    
    void Start()
    {
        // 动态設定广告位ID
        string customAdUnitId = "your_ad_unit_id_here";
        adComponent.Initialize(customAdUnitId, isDebug: true);
    }
}
```

> Source: [AdvertisementComponent.cs]({{file_base_url}}/Runtime/Advertisement/AdvertisementComponent.cs#L100-L105)

---

### 问题三：广告加载失败

#### 症状

呼び出し `Load()` メソッド后，`fail` 回调被触发，收到クラス似以下错误信息：
```
广告加载失败: No ad available
广告加载失败: Network error
```

#### 原因分析

广告加载失败可能有多种原因：

1. **广告 SDK 未正确初期化**：未在 `Initialize()` 中正确配置广告 SDK
2. **网络问题**：设备网络连接異常
3. **广告位配置错误**：广告单元 ID 在广告平台后台未正确配置
4. **广告库存为空**：当前广告平台没有可用的广告
5. **SDK 版本问题**：使用的广告 SDK 版本过旧或不兼容

#### 解決策

**手順 1：检查初期化代码**

确保在展示广告前正确初期化了广告 SDK：

```csharp
void Start()
{
    var adComponent = GetComponent<AdvertisementComponent>();
    // AdvertisementComponent 会在 Start 时自动呼び出し Initialize
    // 如果必要手动初期化：
    // adComponent.Initialize("your_ad_unit_id", isDebug: true);
    
    // 建议提前加载广告
    adComponent.Load(
        (message) => Debug.Log("广告加载成功: " + message),
        (error) => Debug.LogError("广告加载失败: " + error)
    );
}
```

> Source: [AdvertisementComponent.cs]({{file_base_url}}/Runtime/Advertisement/AdvertisementComponent.cs#L163-L167)

**手順 2：使用测试模式**

在开发阶段，启用调试模式以便取得测试广告：

```csharp
// 在 Inspector 中勾选"是否是测试模式"
// 或者を通じて代码：
adComponent.Initialize("your_ad_unit_id", isDebug: true);
```

**手順 3：检查网络连接**

确保设备具有有效的网络连接。对于移动设备，注意：
- 蜂窝数据可能被广告 SDK 限制
- 必要在 `AndroidManifest.xml`（Android）或 `Info.plist`（iOS）中声明网络权限

**手順 4：验证广告位配置**

登录广告平台開発者后台，确认：
- 广告位已作成并激活
- 广告位 ID 与代码中配置的一致
- 广告位クラス型（激励视频/插屏/横幅）匹配您的お願いします求

---

### 问题四：广告展示失败

#### 症状

呼び出し `Show()` メソッド后，`fail` 回调被触发，错误信息例如：
```
广告展示失败: Ad not ready
广告展示失败: Show() called before Load()
```

#### 原因分析

1. **广告未预先加载**：在呼び出し `Show()` 前未呼び出し `Load()` 加载广告
2. **加载的广告已过期**：广告加载后长时间未展示，缓存已失效
3. **广告被跳过**：广告 SDK 内部错误

#### 解決策

**正确的广告展示フロー：**

```csharp
using GameFrameX.Advertisement.Runtime;
using UnityEngine;

public class AdCaller : MonoBehaviour
{
    private AdvertisementComponent adComponent;

    void Start()
    {
        adComponent = GetComponent<AdvertisementComponent>();
        
        // 第一步：加载广告
        LoadAd();
    }

    public void LoadAd()
    {
        adComponent.Load(
            (message) => 
            { 
                Debug.Log("广告加载成功，可能呼び出し Show"); 
                // 广告加载成功后可能展示
                ShowAd();
            },
            (error) => { Debug.LogError("广告加载失败: " + error); }
        );
    }

    public void ShowAd()
    {
        adComponent.Show(
            (message) => Debug.Log("广告展示成功: " + message),
            (error) => Debug.LogError("广告展示失败: " + error),
            (reward) => 
            {
                if (reward)
                {
                    Debug.Log("✅ 用户完整观看广告，发放奖励");
                    // 在此处添加奖励发放逻辑
                }
                else
                {
                    Debug.Log("❌ 用户跳过广告，不发放奖励");
                }
            }
        );
    }

    // 使用 Play メソッド的简化版本
    public void PlayAd()
    {
        adComponent.Play(
            (reward) => 
            {
                if (reward)
                {
                    Debug.Log("✅ 广告播放完成，发放奖励");
                }
            },
            "custom_data_for_ad"
        );
    }
}
```

> Source: [AdvertisementComponent.cs]({{file_base_url}}/Runtime/Advertisement/AdvertisementComponent.cs#L121-L151)

**关键点：**
- 必須先呼び出し `Load()` 加载广告，等待 `success` 回调后再呼び出し `Show()`
- 广告加载成功后有有效期限制，应尽快展示
- 推荐在ゲーム启动时预加载广告

---

### 问题五：平台特定问题

#### 5.1 WebGL 平台问题

**问题：WebGL 构建后广告不显示**

可能原因与解決策：

1. **浏览器广告拦截**：某些浏览器或广告拦截插件会阻止广告お願いします求
   - 测试时使用隐私/无痕模式

2. **跨域问题**：广告 SDK 必要正确的 CORS 配置
   - 确保服务器响应头包含正确的 `Access-Control-Allow-Origin`

3. **小ゲーム平台宏定义**：微信/抖音/快手小ゲーム必要定义对应宏
   - 在 Player Settings 中添加：`ENABLE_WECHAT_MINI_GAME` 或 `ENABLE_DOUYIN_MINI_GAME` 或 `ENABLE_KUAISHOU_MINI_GAME`

#### 5.2 Android 平台问题

**问题：Android 构建后应用崩溃**

可能原因与解決策：

1. **权限缺失**：确保 `AndroidManifest.xml` 包含必要权限：
   ```xml
   <uses-permission android:name="android.permission.INTERNET"/>
   <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
   ```

2. **SDK 版本冲突**：检查是否有多个广告 SDK 版本冲突
   - 使用 Gradle 依赖解决工具查看并排除冲突

3. **ProGuard/R8 规则**：如果启用了代码混淆，可能必要添加保留规则：
   ```properties
   -keep class com.your.ad.provider.** { *; }
   -dontwarn com.your.ad.provider.**
   ```

#### 5.3 iOS 平台问题

**问题：iOS 构建后广告不显示**

可能原因与解決策：

1. **广告标识符 (IDFA)**：iOS 14+ 必要お願いします求用户授权跟踪
   - 在 `Info.plist` 中添加 `NSUserTrackingUsageDescription`

2. **App Transport Security (ATS)**：确保允许广告域名的 HTTP 连接
   - 在 `Info.plist` 中配置 `NSAppTransportSecurity`

3. **Swift 版本兼容**：某些广告 SDK 必要特定 Swift 版本
   - 在 Xcode プロジェクト設定中检查 Swift 版本

---

### 问题六：奖励发放时机问题

#### 症状

用户观看完激励视频后，奖励未正确发放；或者用户在广告播放一半时退出了，奖励仍然被发放。

#### 原因分析

激励视频广告的奖励发放逻辑必要に基づいて `onShowResult` 回调的布尔值来决定：

- `true`：用户完整观看广告，应发放奖励
- `false`：用户跳过广告或中断播放，不应发放奖励

> Source: [AdvertisementComponent.cs]({{file_base_url}}/Runtime/Advertisement/AdvertisementComponent.cs#L126-L127)

#### 解決策

确保在 `onShowResult` 回调中正确处理奖励发放：

```csharp
public void ShowRewardAd()
{
    adComponent.Show(
        // success 回调：广告开始展示
        (message) => Debug.Log("广告开始展示: " + message),
        
        // fail 回调：广告展示失败
        (error) => 
        {
            Debug.LogError("广告展示失败: " + error);
            // 不要发放奖励
        },
        
        // onShowResult 回调：广告关闭
        (shouldReward) => 
        {
            if (shouldReward)
            {
                // ✅ 用户完整观看广告，发放奖励
                Debug.Log("发放奖励：金币 + 100");
                AddCoins(100);
            }
            else
            {
                // ❌ 用户未完整观看，不发放奖励
                Debug.Log("用户跳过广告，不发放奖励");
            }
        }
    );
}
```

**安全奖励发放建议：**

1. **在服务器端验证**：对于高价值奖励，建议在服务器端验证奖励发放
2. **使用广告平台服务器回调**：多数广告平台提供服务器到服务器的奖励回调
3. **防止重复发放**：使用标志位防止同一广告多次发放奖励

---

### 问题七：Editor 模式下コンポーネント不工作

#### 症状

在 Unity Editor 中运行时，广告機能无法正常工作。

#### 原因分析

1. **Editor 不サポート某些广告 SDK**：大多数广告 SDK 仅サポート真机或特定构建平台
2. **模块未在 Editor 模式下初期化**

#### 解決策

**作成 Editor 专用的模拟广告管理器：**

```csharp
using GameFrameX.Advertisement.Runtime;
using UnityEngine;
using System;

[CreateAssetMenu(menuName = "GameFrameX/Editor Mock Ads Manager")]
public class EditorMockAdsManager : BaseAdvertisementManager
{
    [SerializeField] private bool simulateSuccess = true;
    [SerializeField] private float simulatedDelay = 1.0f;

    public override void Initialize(string adUnitId, bool debug = false)
    {
        Debug.Log($"[Editor] Mock Ads Manager 初期化: {adUnitId}");
    }

    public override void Play(Action<bool> playResult, string customData = null)
    {
        Debug.Log($"[Editor] Mock Play 被呼び出し, customData: {customData}");
        playResult?.Invoke(simulateSuccess);
    }

    public override void Show(Action<string> success, Action<string> fail, 
        Action<bool> onShowResult, string customData = null)
    {
        Debug.Log($"[Editor] Mock Show 被呼び出し, customData: {customData}");
        
        if (simulateSuccess)
        {
            success?.Invoke("Mock ad shown successfully");
            onShowResult?.Invoke(true);
        }
        else
        {
            fail?.Invoke("Mock ad failed");
        }
    }

    public override void Load(Action<string> success, Action<string> fail, 
        string customData = null)
    {
        Debug.Log($"[Editor] Mock Load 被呼び出し");
        
        if (simulateSuccess)
        {
            success?.Invoke("Mock ad loaded successfully");
        }
        else
        {
            fail?.Invoke("Mock ad failed to load");
        }
    }
}
```

**在 Inspector 中选择 Editor 模拟実装：**

1. 选择シーン中的 AdvertisementComponent 对象
2. 在 Inspector 中找到广告管理器実装クラス型选择器
3. 选择 Editor 模拟管理器

---

## 诊断工具与日志

### 启用调试模式

调试模式会输出详细的广告操作日志，帮助排查问题：

```csharp
// メソッド1：在 Inspector 中勾选"是否是测试模式"

// メソッド2：を通じて代码設定
adComponent.Initialize("your_ad_unit_id", isDebug: true);
```

### 关键日志标签

在控制台中过滤以下标签可能帮助定位问题：

| 日志标签 | 含义 |
|---------|------|
| `Advertisement manager is invalid` | 广告管理器未注册 |
| `广告加载成功/失败` | 加载操作结果 |
| `广告展示成功/失败` | 展示操作结果 |
| `发放奖励` | 奖励发放逻辑 |

---

## 常见错误代码参考

| 错误代码 | 含义 | 解決策 |
|---------|------|---------|
| `No ad available` | 当前无广告库存 | 等待一段时间后重试，或联系广告平台 |
| `Network error` | 网络连接问题 | 检查网络连接 |
| `Ad not ready` | 广告未加载 | 先呼び出し Load() メソッド |
| `Show() called before Load()` | 展示顺序错误 | 确保先 Load 再 Show |
| `Invalid ad unit ID` | 广告位 ID 无效 | 检查广告位 ID 配置 |
| `SDK not initialized` | SDK 未初期化 | 检查 Initialize() 呼び出し |

---

## 関連リンク

- [概述](../1-overview) - 模块整体介绍
- [安装指南](../2-installation) - 安装与配置
- [快速开始](../3-getting-started) - 快速上手教程
- [コア概念](../4-core-concepts) - コア架构与设计
- [平台配置](../5-platforms) - 各平台详细配置
- [API 参考](../6-api-reference) - 完整 API 文档
