# 示例

本文件提供 `BlankGetChannel` 外掛的完整程式碼示例，涵蓋基本用法、平臺特定配置以及實際應用場景。

## 概述

`BlankGetChannel` 外掛支援在 Unity 專案中跨平臺獲取渠道資訊。本節透過多個實際程式碼示例，展示如何在不同的使用場景中正確呼叫渠道獲取功能。

> **注意：** 詳細的 API 說明請參考 [API 參考](./6-api-reference)，平臺配置說明請參考 [平臺配置](./4-platform-configuration)。

## 基礎用法示例

### 最簡單的獲取方式

使用預設引數獲取渠道資訊，`channelKey` 預設為 `"channel"`，`defaultValue` 預設為 `"default"`：

```csharp
using UnityEngine;

public class ChannelDemo1 : MonoBehaviour
{
    void Start()
    {
        // 使用預設引數獲取渠道
        string channel = BlankGetChannel.GetChannelName();
        Debug.Log("當前渠道: " + channel);
    }
}
```

> Source: [BlankGetChannel.cs](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/Runtime/BlankGetChannel.cs#L98-L141)

### 指定鍵名獲取渠道

當需要在同一個應用中獲取多個不同的渠道配置項時：

```csharp
using UnityEngine;

public class ChannelDemo2 : MonoBehaviour
{
    void Start()
    {
        // 獲取主渠道
        string mainChannel = BlankGetChannel.GetChannelName("channel");
        Debug.Log("主渠道: " + mainChannel);

        // 獲取子渠道
        string subChannel = BlankGetChannel.GetChannelName("sub_channel");
        Debug.Log("子渠道: " + subChannel);

        // 獲取自定義渠道
        string customChannel = BlankGetChannel.GetChannelName("custom_key");
        Debug.Log("自定義渠道: " + customChannel);
    }
}
```

### 帶預設值的安全獲取

當渠道配置可能不存在時，提供預設值以確保應用正常執行：

```csharp
using UnityEngine;

public class ChannelDemo3 : MonoBehaviour
{
    void Start()
    {
        // 當 channel 鍵不存在時，返回 "unknown"
        string channel = BlankGetChannel.GetChannelName("channel", "unknown");
        Debug.Log("渠道: " + channel);

        // 當 sub_channel 鍵不存在時，返回 "default"
        string subChannel = BlankGetChannel.GetChannelName("sub_channel", "default");
        Debug.Log("子渠道: " + subChannel);

        // 組合場景：獲取多個渠道配置，每個都有獨立的預設值
        string primaryChannel = BlankGetChannel.GetChannelName("primary", "official");
        string secondaryChannel = BlankGetChannel.GetChannelName("secondary", "none");
        string testChannel = BlankGetChannel.GetChannelName("test", "internal");

        Debug.Log($"渠道配置: 主={primaryChannel}, 次={secondaryChannel}, 測試={testChannel}");
    }
}
```

## GUI 互動示例

以下示例展示瞭如何在遊戲啟動時透過按鈕互動獲取渠道資訊：

```csharp
using UnityEngine;

/// <summary>
/// 示例：使用 GUI 按鈕獲取渠道資訊
/// </summary>
public class BlankGetChannelExample : MonoBehaviour
{
    private string value;

    void OnGUI()
    {
        // 顯示當前渠道值
        GUILayout.Label($"當前渠道: {value}");
        
        // 建立獲取按鈕
        if (GUILayout.Button("獲取渠道", GUILayout.Width(200), GUILayout.Height(100)))
        {
            value = BlankGetChannel.GetChannelName("appchannel");
        }
    }
}
```

> Source: [BlankGetChannelExample.cs](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/Sample~/BlankGetChannelExample.cs#L1-L15)

## 平臺特定示例

### iOS/tvOS/visionOS 平臺

在 iOS 平臺上，渠道資訊儲存在 `Info.plist` 檔案中：

```csharp
using UnityEngine;

public class iOSChannelDemo : MonoBehaviour
{
    void Start()
    {
        // iOS Info.plist 配置示例：
        // <key>channel</key>
        // <string>ios_cn_taptap</string>
        // <key>sub_channel</key>
        // <string>beta</string>
        
        // 獲取主渠道
        string channel = BlankGetChannel.GetChannelName("channel");
        Debug.Log($"iOS 渠道: {channel}");
        
        // 獲取子渠道
        string subChannel = BlankGetChannel.GetChannelName("sub_channel");
        Debug.Log($"iOS 子渠道: {subChannel}");
    }
}
```

> **說明：** iOS 平臺使用原生 Objective-C++ 程式碼讀取 Info.plist，詳見 [BlankGetChannel.mm](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/Plugins/iOS/BlankGetChannel/BlankGetChannel.mm#L25-L36)

### Android 平臺

在 Android 平臺上，渠道資訊透過 `AndroidManifest.xml` 中的 `<meta-data>` 標籤定義：

```csharp
using UnityEngine;

public class AndroidChannelDemo : MonoBehaviour
{
    void Start()
    {
        // Android AndroidManifest.xml 配置示例：
        // <application>
        //     <meta-data
        //         android:name="channel"
        //         android:value="android_cn_taptap" />
        //     <meta-data
        //         android:name="sub_channel"
        //         android:value="beta" />
        // </application>
        
        // 獲取主渠道
        string channel = BlankGetChannel.GetChannelName("channel");
        Debug.Log($"Android 渠道: {channel}");
        
        // 獲取子渠道
        string subChannel = BlankGetChannel.GetChannelName("sub_channel");
        Debug.Log($"Android 子渠道: {subChannel}");
    }
}
```

> **說明：** Android 平臺透過 JAR 外掛呼叫 Java 層程式碼獲取 meta-data，詳見 [BlankGetChannel.cs](https://github.com/GameFrameX/com.gameframex.unity.getchannel/blob/main/Runtime/BlankGetChannel.cs#L131-L136)

### Editor/PC/WebGL/UWP/Console 平臺

在這些平臺上，渠道資訊儲存在 `Resources/application_config.txt` 檔案中：

```csharp
using UnityEngine;

public class EditorChannelDemo : MonoBehaviour
{
    void Start()
    {
        // application_config.txt 檔案內容示例：
        // channel=editor_cn_test
        // sub_channel=beta
        // other_key=other_value
        
        // 獲取主渠道
        string channel = BlankGetChannel.GetChannelName("channel");
        Debug.Log($"Editor/PC 渠道: {channel}");
        
        // 獲取子渠道
        string subChannel = BlankGetChannel.GetChannelName("sub_channel");
        Debug.Log($"Editor/PC 子渠道: {subChannel}");
        
        // 獲取其他配置
        string otherValue = BlankGetChannel.GetChannelName("other_key");
        Debug.Log($"其他配置: {otherValue}");
    }
}
```

## 實際應用場景示例

### 場景一：遊戲啟動時初始化渠道

```csharp
using UnityEngine;

/// <summary>
/// 遊戲啟動管理器 - 在遊戲啟動時獲取並記錄渠道資訊
/// </summary>
public class GameLauncher : MonoBehaviour
{
    [RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
    static void OnGameStart()
    {
        // 獲取渠道資訊
        string channel = BlankGetChannel.GetChannelName("channel", "unknown");
        string subChannel = BlankGetChannel.GetChannelName("sub_channel", "none");
        
        // 輸出日誌
        Debug.Log($"遊戲啟動 - 渠道: {channel}, 子渠道: {subChannel}");
        
        // 根據渠道執行不同邏輯
        HandleChannelInitialization(channel, subChannel);
    }
    
    private static void HandleChannelInitialization(string channel, string subChannel)
    {
        // 示例：根據渠道型別初始化不同的功能
        switch (channel)
        {
            case "ios_cn_taptap":
                Debug.Log("初始化 TapTap 渠道特有功能");
                break;
            case "android_cn_taptap":
                Debug.Log("初始化 Android TapTap 渠道特有功能");
                break;
            case "editor_cn_test":
                Debug.Log("測試環境配置");
                break;
            default:
                Debug.Log("使用預設配置");
                break;
        }
    }
}
```

### 場景二：統計分析系統

```csharp
using UnityEngine;

/// <summary>
/// 渠道統計分析系統
/// </summary>
public class ChannelAnalytics : MonoBehaviour
{
    private string channel;
    private string subChannel;

    void Awake()
    {
        // 獲取渠道資訊
        channel = BlankGetChannel.GetChannelName("channel", "unknown");
        subChannel = BlankGetChannel.GetChannelName("sub_channel", "none");
        
        Debug.Log($"Analytics 初始化 - 渠道: {channel}, 子渠道: {subChannel}");
    }

    void OnApplicationPause(bool pauseStatus)
    {
        if (!pauseStatus)
        {
            // 應用恢復時重新整理渠道資訊
            RefreshChannelInfo();
        }
    }

    private void RefreshChannelInfo()
    {
        channel = BlankGetChannel.GetChannelName("channel", "unknown");
        subChannel = BlankGetChannel.GetChannelName("sub_channel", "none");
        Debug.Log($"渠道資訊已重新整理 - 渠道: {channel}, 子渠道: {subChannel}");
    }

    // 提供公開介面供其他模組查詢當前渠道
    public static string GetCurrentChannel()
    {
        return BlankGetChannel.GetChannelName("channel", "unknown");
    }

    public static string GetCurrentSubChannel()
    {
        return BlankGetChannel.GetChannelName("sub_channel", "none");
    }
}
```

### 場景三：多渠道配置管理

```csharp
using UnityEngine;
using System.Collections.Generic;

/// <summary>
/// 多渠道配置管理器
/// </summary>
public class MultiChannelConfig : MonoBehaviour
{
    // 渠道配置字典
    private Dictionary<string, ChannelConfig> channelConfigs;

    void Start()
    {
        InitializeChannelConfigs();
        ApplyCurrentChannelConfig();
    }

    private void InitializeChannelConfigs()
    {
        channelConfigs = new Dictionary<string, ChannelConfig>
        {
            { "ios_cn_taptap", new ChannelConfig { serverUrl = "https://taptap.example.com", enableAds = true } },
            { "ios_cn_appstore", new ChannelConfig { serverUrl = "https://appstore.example.com", enableAds = false } },
            { "android_cn_taptap", new ChannelConfig { serverUrl = "https://taptap.example.com", enableAds = true } },
            { "android_cn_googleplay", new ChannelConfig { serverUrl = "https://play.google.com", enableAds = true } },
            { "default", new ChannelConfig { serverUrl = "https://default.example.com", enableAds = false } }
        };
    }

    private void ApplyCurrentChannelConfig()
    {
        string channel = BlankGetChannel.GetChannelName("channel", "default");
        
        if (channelConfigs.TryGetValue(channel, out var config))
        {
            Debug.Log($"應用渠道配置 - 渠道: {channel}, 伺服器: {config.serverUrl}, 廣告: {config.enableAds}");
        }
        else
        {
            Debug.LogWarning($"未找到渠道 {channel} 的配置，使用預設配置");
            var defaultConfig = channelConfigs["default"];
            Debug.Log($"預設配置 - 伺服器: {defaultConfig.serverUrl}, 廣告: {defaultConfig.enableAds}");
        }
    }

    /// <summary>
    /// 渠道配置資料類
    /// </summary>
    private class ChannelConfig
    {
        public string serverUrl;
        public bool enableAds;
    }
}
```

### 場景四：除錯與開發輔助

```csharp
using UnityEngine;

/// <summary>
/// 渠道資訊除錯面板 - 在 Editor 和開發構建中顯示渠道資訊
/// </summary>
public class ChannelDebugPanel : MonoBehaviour
{
#if UNITY_EDITOR || DEVELOPMENT_BUILD
    private Vector2 scrollPosition;
    private Dictionary<string, string> allChannels;

    void OnGUI()
    {
        // 建立除錯區域
        GUILayout.BeginArea(new Rect(10, 10, 300, 400));
        GUILayout.Box("渠道除錯面板", GUILayout.Width(280));
        
        scrollPosition = GUILayout.BeginScrollView(scrollPosition, GUILayout.Width(280), GUILayout.Height(350));
        
        // 顯示所有已知渠道鍵
        string[] channelKeys = { "channel", "sub_channel", "custom_key", "appchannel" };
        
        foreach (var key in channelKeys)
        {
            string value = BlankGetChannel.GetChannelName(key, "<未配置>");
            GUILayout.Label($"{key}: {value}");
        }
        
        GUILayout.EndScrollView();
        GUILayout.EndArea();
    }
#endif
}
```

## 快取機制說明

`BlankGetChannel` 內部使用快取機制來避免重複讀取配置檔案，提高效能：

```csharp
// 內部實現虛擬碼
private static readonly Dictionary<string, string> ChannelCache = new Dictionary<string, string>(32);

public static string GetChannelName(string channelKey = "channel", string defaultValue = "default")
{
    // 1. 首先檢查快取
    if (ChannelCache.TryGetValue(channelKey, out var value))
    {
        return value;
    }
    
    // 2. 根據平臺讀取配置
    // ... platform-specific reading logic ...
    
    // 3. 存入快取並返回
    ChannelCache[channelKey] = channelName;
    return channelName;
}
```

> **效能提示：** 由於快取機制存在，首次呼叫 `GetChannelName` 後，後續呼叫相同 `channelKey` 將直接從記憶體返回，無需重複讀取配置檔案。

## 完整專案結構示例

```
Assets/
├── Scripts/
│   ├── GameLauncher.cs           # 遊戲啟動初始化
│   ├── ChannelAnalytics.cs       # 渠道統計分析
│   ├── MultiChannelConfig.cs    # 多渠道配置管理
│   └── UI/
│       └── ChannelDebugPanel.cs  # 除錯面板
├── Resources/
│   └── application_config.txt    # Editor/PC/WebGL 渠道配置
└── Plugins/
    ├── Android/
    │   └── com.alianhome.getchannel.jar  # Android 外掛
    └── iOS/
        └── BlankGetChannel/
            └── BlankGetChannel.mm        # iOS 原生程式碼
```

## 常見錯誤示例與解決方案

### 錯誤一：鍵名不匹配

```csharp
// ❌ 錯誤：配置的鍵名與獲取的鍵名不一致
// Info.plist: <key>channel</key>
// 程式碼: BlankGetChannel.GetChannelName("channelName")

// ✅ 正確：確保鍵名一致
string channel = BlankGetChannel.GetChannelName("channel");
```

### 錯誤二：未處理預設值

```csharp
// ❌ 錯誤：未提供預設值，可能返回 "default" 或 "unknown"
string channel = BlankGetChannel.GetChannelName("nonexistent_key");

// ✅ 正確：根據業務需求提供合理的預設值
string channel = BlankGetChannel.GetChannelName("nonexistent_key", "official");
```

### 錯誤三：平臺判斷遺漏

```csharp
// ❌ 錯誤：假設所有平臺配置方式相同
// Android 使用 meta-data，PC 使用 application_config.txt

// ✅ 正確：參考本文件，根據目標平臺進行正確配置
// 詳見 [平臺配置](./4-platform-configuration)
```

## 相關連結

- [概述](./1-overview) - 專案整體介紹
- [安裝](./2-installation) - 外掛安裝指南
- [快速開始](./3-getting-started) - 快速入門指南
- [平臺配置](./4-platform-configuration) - 各平臺配置詳解
- [API 參考](./6-api-reference) - 完整 API 文件
- [常見問題](./7-troubleshooting) - 常見問題解答
