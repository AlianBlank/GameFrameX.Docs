# Getting Started

This guide will help you get started with the CoroutineComponent in just a few minutes.

## Prerequisites

- Unity 2019.4+
- GameFrameX Core package installed
- Coroutine package installed

## Steps

### 1. Get Coroutine Component

```csharp
var coroutineComponent = GameFrameworkEntry.GetComponent<CoroutineComponent>();
```

### 2. Start Coroutine

```csharp
var coroutine = coroutineComponent.StartCoroutine(MyAsyncTask());
```

### 3. Stop Coroutine

```csharp
coroutineComponent.StopCoroutine(coroutine);
```

### 4. Stop All Coroutines

```csharp
coroutineComponent.StopAllCoroutines();
```

## Complete Example

```csharp
using UnityEngine;
using GameFrameX.Coroutine;

public class CoroutineExample : MonoBehaviour
{
    private CoroutineComponent _coroutineComponent;

    void Start()
    {
        _coroutineComponent = GameFrameworkEntry.GetComponent<CoroutineComponent>();
        _coroutineComponent.StartCoroutine(CountDown());
    }

    System.Collections.IEnumerator CountDown()
    {
        for (int i = 5; i > 0; i--)
        {
            Debug.Log($"Countdown: {i}");
            yield return new WaitForSeconds(1f);
        }
        Debug.Log("Done!");
    }
}
```

## Next Steps

- Read [CoroutineComponent](01.coroutine-component.md) to learn about the complete API interface