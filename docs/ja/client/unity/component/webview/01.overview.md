# 

`Game Frame X Web View` は Game Frame X ゲームフレームワークの WebView コンポーネント， Unity ゲーム。コンポーネントにづく [gree/unity-webview](https://github.com/gree/unity-webview) ，たの API との。

## プロジェクト

GameFrameX WebView は Game Frame X ゲームフレームワークの，に Unity ゲームの。をじてコンポーネント，：

- に Unity ゲーム
-  C# と JavaScript の
- サポート Android と iOS プラットフォーム
- と Game Frame X フレームワークモジュール

## コア

|  |  |
|------|------|
|  | に Unity  Web  |
| JavaScript  | サポート C# と JavaScript コードびし |
|  | サポート |
| プラットフォーム |  Android と iOS プラットフォーム |
| イベント |  WebView とイベント |

## アーキテクチャ

```mermaid
flowchart TD
    subgraph 用户层["用户コード"]
        UserCode["ゲーム业务コード"]
    end

    subgraph コンポーネント层["WebView コンポーネント层"]
        WebViewComponent["WebViewComponent<br/>Unity コンポーネント"]
    end

    subgraph インターフェース层["インターフェース抽象层"]
        IWebViewManager["IWebViewManager<br/>インターフェース定义"]
    end

    subgraph 实现层["プラットフォーム实现层"]
        BaseWebViewManager["BaseWebViewManager<br/>抽象基クラス"]
    end

    subgraph フレームワーク层["Game Frame X フレームワーク"]
        GameFrameworkModule["GameFrameworkModule<br/>フレームワークモジュール基クラス"]
        GameFrameworkEntry["GameFrameworkEntry<br/>フレームワーク入口"]
    end

    subgraph イベント层["イベント系统"]
        WebViewCloseEventArgs["WebViewCloseEventArgs<br/>关闭イベント"]
        WebViewReceivedMessageEventArgs["WebViewReceivedMessageEventArgs<br/>消息イベント"]
    end

    UserCode --> WebViewComponent
    WebViewComponent --> IWebViewManager
    IWebViewManager --> BaseWebViewManager
    BaseWebViewManager --> GameFrameworkModule
    WebViewComponent --> GameFrameworkEntry
    GameFrameworkEntry --> |获取モジュール| IWebViewManager
    BaseWebViewManager --> WebViewCloseEventArgs
    BaseWebViewManager --> WebViewReceivedMessageEventArgs
```

### アーキテクチャ

1. **WebViewComponent**: Unity コンポーネント，の API。をじてコンポーネント GameObject  WebView 。

2. **IWebViewManager**: インターフェース，た WebView マネージャーの，、、、と JavaScript メソッド。

3. **BaseWebViewManager**: クラス， GameFrameworkModule，プラットフォームの。プラットフォームクラス（ AndroidWebViewManager、iOSWebViewManager）クラス。

4. **GameFrameworkModule**: Game Frame X フレームワークのモジュールクラス，モジュールとフレームワーク。

5. **イベント**: をじて GameFrameX のイベント WebView イベント，イベントとイベント。

## モジュール

```mermaid
classDiagram
    class GameFrameworkComponent {
        <<abstract>>
    }
    
    class WebViewComponent {
        +Show(string url)
        +Hide()
        +MakeFullScreen()
        +ExecuteJavaScript(string javaScript)
    }
    
    class IWebViewManager {
        <<interface>>
        +Initialize()
        +Show(string url)
        +Hide()
        +MakeFullScreen()
        +ExecuteJavaScript(string javaScript)
    }
    
    class BaseWebViewManager {
        <<abstract>>
    }
    
    class GameFrameworkModule {
        <<abstract>>
    }
    
    class GameEventArgs {
        <<abstract>>
    }
    
    class WebViewCloseEventArgs {
        +EventId: string
        +Create() WebViewCloseEventArgs
    }
    
    class WebViewReceivedMessageEventArgs {
        +EventId: string
        +Message: string
        +Create(string message) WebViewReceivedMessageEventArgs
    }
    
    GameFrameworkComponent <|-- WebViewComponent
    IWebViewManager <|.. BaseWebViewManager
    GameFrameworkModule <|-- BaseWebViewManager
    GameEventArgs <|-- WebViewCloseEventArgs
    GameEventArgs <|-- WebViewReceivedMessageEventArgs
    WebViewComponent --> IWebViewManager : uses
```

## プラットフォームサポート

| プラットフォーム | サポート |  |
|------|----------|------|
| Android | ✅ サポート | をじて unity-webview Android  |
| iOS | ✅ サポート | をじて unity-webview iOS  |
| Windows | ⚠️ サポート | エディターサポート |
| macOS | ⚠️ サポート | エディターサポート |
| WebGL | ❌ サポート | WebGL プラットフォーム WebView |

## 

```mermaid
flowchart LR
    subgraph 本モジュール["GameFrameX.WebView"]
        WebView["WebView モジュール"]
    end

    subgraph フレームワーク依赖["Game Frame X フレームワーク"]
        Runtime["GameFrameX.Runtime"]
        Event["GameFrameX.Event.Runtime"]
    end

    subgraph 第三方依赖["第三方依赖"]
        UnityWebView["gree/unity-webview"]
    end

    WebView --> Runtime
    WebView --> Event
    WebView --> UnityWebView
```

 `package.json` の，モジュールコンポーネント：

- **GameFrameX.Runtime** (`com.gameframex.unity: 1.0.0`): フレームワークコア
- **GameFrameX.Event.Runtime**: フレームワークイベント
- **gree/unity-webview**:  WebView （インストール）

## シーン

GameFrameX WebView に：

1. **ゲーム**:  WebView ゲーム、
2. ****:  SDK の Web 
3. ****: にゲーム
4. **ドキュメント**: ゲーム
5. ****: 
6. ****:  Web の

## リンク

- [インストールガイド](./2-installation) - インストールステップ
- [クイックスタート](./3-getting-started) - 
- [API リファレンス](./4-api-reference) -  API ドキュメント
- [プラットフォーム](./5-platforms) - Android と iOS プラットフォーム
- [イベント](./6-events) - イベント
- [よくある](./7-troubleshooting) - よくある
- [Game Frame X サイト](https://gameframex.doc.alianblank.com)
- [gree/unity-webview](https://github.com/gree/unity-webview)
