# クイックスタート

本文将引导你完成 GlobalConfig コンポーネント的基本設定和使用。

## 前置条件

- Unity 2019.4+
- GameFrameX Core パッケージ已インストール
- GlobalConfig パッケージ已インストール

## ステップ

### 1. 在シーン中設定

1. 在シーン中作成或找到挂载 GameFramework 的 GameObject
2. 在 Inspector 中找到 `GlobalConfigComponent`
3. 填写全局設定项（应用バージョン確認 URL、リソースバージョン確認 URL 等）

### 2. コード中读取設定

```csharp
var globalConfigComponent = GameFrameworkEntry.GetComponent<GlobalConfigComponent>();

// 获取应用版本检查地址
string appVersionUrl = globalConfigComponent.AppVersionCheckUrl;

// 获取资源版本检查地址
string resourceVersionUrl = globalConfigComponent.ResourceVersionCheckUrl;
```

### 3. ランタイム変更設定

```csharp
// 通过组件接口动态修改配置
globalConfigComponent.SetAppVersionCheckUrl("https://new-url.com/check");
```

## 完全な例

```csharp
using UnityEngine;
using GameFrameX.GlobalConfig;

public class GlobalConfigExample : MonoBehaviour
{
    private GlobalConfigComponent _configComponent;

    void Start()
    {
        _configComponent = GameFrameworkEntry.GetComponent<GlobalConfigComponent>();

        Debug.Log($"App 版本检查 URL: {_configComponent.AppVersionCheckUrl}");
        Debug.Log($"资源版本检查 URL: {_configComponent.ResourceVersionCheckUrl}");
    }
}
```

## 次のステップ

- 阅读 [常见問題](06.troubleshooting.md) 理解する常见設定問題
