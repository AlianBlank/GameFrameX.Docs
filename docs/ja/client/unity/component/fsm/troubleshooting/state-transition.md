---
title: "ステータス切替とステータス操作エラーの早見表"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.fsm
    commit: 65a093c82a2a8ab108bd555b8d9bbb516f587daf
    generator: OpenDeepWiki
---
# 状態切替と状態操作エラーの早見表

`GameFrameworkException` が発生した場合は、まず次のエラー原文(`Fsm.cs` / `FsmManager.cs` で送出)と照らし合わせて根本原因を特定し、その後修復手順に従って対処してください。

## 迅速な診断

| 症状(例外原文) | 送出位置 | 根本原因 | 修復 |
|---|---|---|---|
| `FSM owner is invalid.` | [Fsm.cs#L147-L150](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L147-L150) | FSM 作成時に渡された `owner` が `null` | 所有者オブジェクトが非空であることを保証してから FSM を作成する |
| `FSM states is invalid.` | [Fsm.cs#L152-L155](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L152-L155) | `states` 配列/リストが `null` または要素数が 0 | 少なくとも 1 つの `FsmState<T>` インスタンスを渡す |
| `FSM states is invalid.`(ループ内) | [Fsm.cs#L164-L167](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L164-L167) | 状態集合に `null` 要素が含まれている | 作成前に空参照を取り除く |
| `FSM '{0}' state '{1}' is already exist.` | [Fsm.cs#L170-L173](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L170-L173) | 同一 FSM 内で重複した `FsmState<T>` 型が存在する | 各状態クラスは 1 回だけ登録し、型の重複を避ける |
| `Already exist FSM '{0}'.` | [FsmManager.cs#L322-L325](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L322-L325) | 同じ `TypeNamePair` の FSM が既に存在する | 先に `DestroyFsm` を呼んでから `CreateFsm` するか、異なる `name` を使用する |
| `Already exist FSM '{0}'.` | [FsmManager.cs#L357-L360](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L357-L360) | 同上、`List<FsmState<T>>` オーバーロード使用時のエラー | 同上 |
| `Owner type is invalid.` | [FsmManager.cs#L168-L171](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L168-L171)、[#L197-L200](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L197-L200)、[#L224-L227](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L224-L227)、[#L253-L256](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L253-L256)、[#L387-L389](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L387-L389)、[#L415-L417](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L415-L417) | `HasFsm` / `GetFsm` / `DestroyFsm` の `Type` オーバーロードに `null` が渡された | 有効な `Type` 参照を渡す。ジェネリック版オーバーロードを使用すれば根本的に回避できる |
| `Results is invalid.` | [FsmManager.cs#L285-L288](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L285-L288) | `GetAllFsms(List<FsmBase> results)` に `null` が渡された | 既にインスタンス化された `List<FsmBase>` を渡す |
| `FSM is invalid.` | [FsmManager.cs#L432-L435](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L432-L435)、[#L448-L451](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L448-L451) | `DestroyFsm<T>(IFsm<T>)` / `DestroyFsm(FsmBase)` に `null` が渡された | 取得済みの有効な FSM 参照のみを销毁する |

## 修復手順

### 1. `FSM owner is invalid.`

`Fsm<T>.Create` または `FsmManager.CreateFsm<T>` を呼び出す前に、所有者が非空であることを確認してください:

```csharp
if (owner == null)
{
    // 安全な分岐に戻り、作成を延期するか直接 return する
    return;
}

GameFrameX.Fsm.FsmComponent.FsmManager.CreateFsm("Battle", owner, stateA, stateB);
```

Source: [Fsm.cs#L147-L150](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L147-L150)

### 2. `FSM states is invalid.`

配列/リストには少なくとも 1 つの状態が必要です:

```csharp
var states = new GameFrameX.Fsm.FsmState<MyOwner>[] { new IdleState(), new RunState() };
if (states.Length < 1)
{
    return;
}

var fsm = GameFrameX.Fsm.FsmComponent.FsmManager.CreateFsm("Battle", owner, states);
```

Source: [Fsm.cs#L152-L155](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L152-L155)

### 3. `FSM '{0}' state '{1}' is already exist.`

`Fsm<T>.Create` はループ内で `Type` を基に重複を検出します:

- 同じ型の `FsmState<T>` サブクラスを重複して渡してはいけません。
- 同じ状態インスタンスが複数回追加されていないか、または 2 つのサブクラスが同じ `Type` を宣言していないか(匿名方式で作成したケースなど)を確認してください。
- 解決策:重複した状態を統合するか、余分なインスタンスを削除してください。

Source: [Fsm.cs#L170-L173](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs#L170-L173)

### 4. `Already exist FSM '{0}'.`

`FsmManager` は `TypeNamePair(typeof(T), name)` を主キーとして使用するため、重複作成すると例外が送出されます:

```csharp
if (GameFrameX.Fsm.FsmComponent.FsmManager.HasFsm<MyOwner>("Battle"))
{
    GameFrameX.Fsm.FsmComponent.FsmManager.DestroyFsm<MyOwner>("Battle");
}

var fsm = GameFrameX.Fsm.FsmComponent.FsmManager.CreateFsm<MyOwner>("Battle", owner, states);
```

Source: [FsmManager.cs#L322-L325](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L322-L325)

### 5. `Owner type is invalid.` と `FSM is invalid.`

これらは `Type` ベースのリロードを使用し、かつ `null` が渡された場合にのみ送出されます。対処の推奨事項:

- ジェネリック版リロード `HasFsm<T>()` を使用すれば、`null.Type` の可能性を根本から排除できます。
- `DestroyFsm` を呼ぶ際は、`GetFsm<T>()` が返した有効な参照に対してのみ実行してください。

Source: [FsmManager.cs#L387-L389](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L387-L389)、[#L432-L435](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs#L432-L435)

## 検証

修復後、以下のチェックを実施して問題が解決されたことを確認してください:

1. **シーンに再入場するか、作成フローを再実行** し、Console に対応する例外が表示されないことを確認します。
2. ブレークポイントまたはログで `HasFsm<T>(name)` の結果が `true` であることを出力し、FSM が正常に登録されたことを確認します。
3. `fsm.CurrentState` / 状態切替メソッド(例: `ChangeState<TState>()`)を呼び出し、目的の状態に正常に進めることを確認します。
4. モジュール終了時に `DestroyFsm<T>(name)` を呼び出し、同名の FSM を再作成しても `Already exist FSM '{0}'.` が発生しないことを確認します。

## 関連リンク

- [Runtime/Fsm/Fsm.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/Fsm.cs)
- [Runtime/Fsm/FsmManager.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmManager.cs)
- [Runtime/Fsm/FsmComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmComponent.cs)
- [Runtime/Fsm/FsmBase.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmBase.cs)
- [Runtime/Fsm/IFsm.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/IFsm.cs)
- [Runtime/Fsm/IFsmManager.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/IFsmManager.cs)
- [Runtime/Fsm/FsmState.cs](https://github.com/GameFrameX/com.gameframex.unity.fsm/blob/main/Runtime/Fsm/FsmState.cs)