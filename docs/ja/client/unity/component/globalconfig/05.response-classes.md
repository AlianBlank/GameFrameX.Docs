# レスポンスクラス

レスポンスクラス（Response Classes）は GlobalConfig にすのデータのデータ（DTO）。レスポンスクラスタイプの。

## 

に GlobalConfig ，リクエスト，すのデータ。すのデータをじてレスポンスクラスとタイプ，コードタイプ。

GlobalConfig たのレスポンスクラス：

| レスポンスクラス |  |
|--------|------|
| ResponseGameAppVersion | ゲームバージョン |
| ResponseGameAssetPackageVersion | ゲームリソースバージョン |
| ResponseGlobalInfo | グローバル |

### 

レスポンスクラスの：

1. ****：シンプルの POCO クラス，の
2. **タイプ**：タイプのクラス，タイプ
3. ****：をじてのレスポンスタイプ

## クラス

### ResponseGameAppVersion

ゲームバージョンレスポンスクラス，むの。

```csharp
namespace GameFrameX.GlobalConfig.Runtime
{
    /// <summary>
    /// ゲームバージョン信息
    /// </summary>
    public class ResponseGameAppVersion
    {
        /// <summary>
        /// は否强制升级
        /// </summary>
        public bool IsForce { get; set; }

        /// <summary>
        /// 程序下载地址
        /// </summary>
        public string AppDownloadUrl { get; set; }

        /// <summary>
        /// は否必要更新
        /// </summary>
        public bool IsUpgrade { get; set; }

        /// <summary>
        /// 更新公告
        /// </summary>
        public string UpdateAnnouncement { get; set; }

        /// <summary>
        /// 更新标题
        /// </summary>
        public string UpdateTitle { get; set; }
    }
}
```

> Source: [ResponseGameAppVersion.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGameAppVersion.cs#L1-L33)

**プロパティ：**

| プロパティ | タイプ |  |
|------|------|------|
| IsForce | bool | は，true ゲーム |
| AppDownloadUrl | string | バージョンの URL  |
| IsUpgrade | bool | は，true バージョン |
| UpdateAnnouncement | string | ， |
| UpdateTitle | string | の |

### ResponseGameAssetPackageVersion

ゲームリソースバージョンレスポンスクラス，むリソースの。

```csharp
namespace GameFrameX.GlobalConfig.Runtime
{
    /// <summary>
    /// ゲームリソースバージョン信息
    /// </summary>
    public class ResponseGameAssetPackageVersion
    {
        /// <summary>
        /// 语言
        /// </summary>
        public string Language { get; set; }

        /// <summary>
        /// リソースバージョン
        /// </summary>
        public string Version { get; set; }

        /// <summary>
        /// リソース包名称
        /// </summary>
        public string AssetPackageName { get; set; }

        /// <summary>
        /// リソース包パス
        /// </summary>
        public string AssetPackagePath { get; set; }

        /// <summary>
        /// プラットフォーム
        /// </summary>
        public string Platform { get; set; }

        /// <summary>
        /// 下载根パス
        /// </summary>
        public string RootPath { get; set; }

        /// <summary>
        /// 包名
        /// </summary>
        public string PackageName { get; set; }

        /// <summary>
        /// ゲームバージョン
        /// </summary>
        public string AppVersion { get; set; }

        /// <summary>
        /// 渠道名称
        /// </summary>
        public string Channel { get; set; }
    }
}
```

> Source: [ResponseGameAssetPackageVersion.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGameAssetPackageVersion.cs#L1-L53)

**プロパティ：**

| プロパティ | タイプ |  |
|------|------|------|
| Language | string | ， "zh-CN"、"en-US" |
| Version | string | リソースバージョン， "1.0.0" |
| AssetPackageName | string | リソース |
| AssetPackagePath | string | リソースパス |
| Platform | string | プラットフォーム， "Android"、"iOS"、"Windows" |
| RootPath | string | リソースのディレクトリ URL |
| PackageName | string |  |
| AppVersion | string | バージョン |
| Channel | string |  |

### ResponseGlobalInfo

グローバルレスポンスクラス，むのとグローバルパラメータ。

```csharp
namespace GameFrameX.GlobalConfig.Runtime
{
    /// <summary>
    /// グローバル信息レスポンス对象
    /// </summary>
    public class ResponseGlobalInfo
    {
        /// <summary>
        /// 检测程序バージョン地址
        /// </summary>
        public string CheckAppVersionUrl { get; set; }

        /// <summary>
        /// 检测リソースバージョン地址
        /// </summary>
        public string CheckResourceVersionUrl { get; set; }

        /// <summary>
        /// AOTコード列表
        /// </summary>
        public string AOTCodeList { get; set; }

        /// <summary>
        /// 扩展内容
        /// </summary>
        public string Content { get; set; }
    }
}
```

> Source: [ResponseGlobalInfo.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGlobalInfo.cs#L1-L28)

**プロパティ：**

| プロパティ | タイプ |  |
|------|------|------|
| CheckAppVersionUrl | string | バージョンのインターフェース |
| CheckResourceVersionUrl | string | リソースバージョンのインターフェース |
| AOTCodeList | string | AOT コンパイルコードの JSON  |
| Content | string | の，にカスタマイズ |

## データ

たリクエストクラスとレスポンスクラスの：

```mermaid
erDiagram
    RequestBase ||--o| RequestGameAppVersion : "继承"
    RequestBase ||--o| RequestGameAssetPackageVersion : "继承"
    RequestBase ||--o| RequestGlobalInfo : "继承"

    ResponseGameAppVersion {
        bool IsForce
        string AppDownloadUrl
        bool IsUpgrade
        string UpdateAnnouncement
        string UpdateTitle
    }

    ResponseGameAssetPackageVersion {
        string Language
        string Version
        string AssetPackageName
        string AssetPackagePath
        string Platform
        string RootPath
        string PackageName
        string AppVersion
        string Channel
    }

    ResponseGlobalInfo {
        string CheckAppVersionUrl
        string CheckResourceVersionUrl
        string AOTCodeList
        string Content
    }

    ResponseGlobalInfo ||--o{ ResponseGameAppVersion : "含むURL引用"
    ResponseGlobalInfo ||--o{ ResponseGameAssetPackageVersion : "含むURL引用"
```

## 

### 

にゲームグローバル：

```csharp
using GameFrameX.GlobalConfig.Runtime;
using UnityEngine;

public class ConfigManager : MonoBehaviour
{
    public void LoadGlobalConfig(string jsonData)
    {
        // 解析グローバル設定レスポンス
        var globalInfo = new ResponseGlobalInfo
        {
            CheckAppVersionUrl = "https://api.example.com/check-app-version",
            CheckResourceVersionUrl = "https://api.example.com/check-resource-version",
            AOTCodeList = "[\"Method1\", \"Method2\"]",
            Content = "{\"config\": \"value\"}"
        };

        Debug.Log($"App Version URL: {globalInfo.CheckAppVersionUrl}");
        Debug.Log($"Resource Version URL: {globalInfo.CheckResourceVersionUrl}");
    }
}
```

### ゲームバージョンレスポンス

```csharp
using GameFrameX.GlobalConfig.Runtime;
using UnityEngine;

public class VersionChecker
{
    public void HandleVersionResponse(ResponseGameAppVersion response)
    {
        if (response.IsUpgrade)
        {
            Debug.Log($"发现新バージョン: {response.UpdateTitle}");
            Debug.Log($"更新公告: {response.UpdateAnnouncement}");
            
            if (response.IsForce)
            {
                Debug.LogWarning("当前バージョン已过时，必要强制更新！");
                // 显示强制更新界面
            }
            else
            {
                // 显示オプション更新界面
            }
            
            // 打开下载页面
            Application.OpenURL(response.AppDownloadUrl);
        }
        else
        {
            Debug.Log("当前已は最新バージョン");
        }
    }
}
```

### リソースバージョンレスポンス

```csharp
using GameFrameX.GlobalConfig.Runtime;
using UnityEngine;

public class AssetUpdater
{
    public void HandleAssetVersionResponse(ResponseGameAssetPackageVersion response)
    {
        Debug.Log($"リソースバージョン: {response.Version}");
        Debug.Log($"リソース包: {response.AssetPackageName}");
        Debug.Log($"プラットフォーム: {response.Platform}");
        Debug.Log($"渠道: {response.Channel}");
        Debug.Log($"下载根パス: {response.RootPath}");
        
        // 根据レスポンスビルドリソース下载パス
        string fullAssetPath = $"{response.RootPath}/{response.AssetPackagePath}";
        Debug.Log($"完全リソースパス: {fullAssetPath}");
    }
}
```

## と GlobalConfigComponent の

ResponseGlobalInfo に GlobalConfigComponent ，にのグローバル：

```csharp
namespace GameFrameX.GlobalConfig.Runtime
{
    public sealed class GlobalConfigComponent : GameFrameworkComponent
    {
        /// <summary>
        /// 获取グローバル設定信息
        /// </summary>
        /// <returns>戻すグローバル設定信息对象</returns>
        public ResponseGlobalInfo GlobalInfo
        {
            get { return m_responseGlobalInfo; }
        }

        /// <summary>
        /// グローバル設定信息对象
        /// </summary>
        private ResponseGlobalInfo m_responseGlobalInfo;

        /// <summary>
        /// 設定グローバル設定信息
        /// </summary>
        /// <param name="globalInfo">グローバル設定信息对象</param>
        public void SetGlobalConfig(ResponseGlobalInfo globalInfo)
        {
            m_responseGlobalInfo = globalInfo;
        }
    }
}
```

> Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L131-L158)

## レスポンスクラスとリクエストクラスの

| リクエストクラス | レスポンスクラス |  |
|--------|--------|----------|
| RequestGameAppVersion | ResponseGameAppVersion | ，すバージョン |
| RequestGameAssetPackageVersion | ResponseGameAssetPackageVersion | リソース，すリソースバージョン |
| RequestGlobalInfo | ResponseGlobalInfo | リクエストグローバル，すインターフェース |

## レスポンスクラス

レスポンスクラス，カスタマイズクラスのすデータ：

```csharp
using GameFrameX.GlobalConfig.Runtime;

/// <summary>
/// カスタマイズグローバル信息レスポンスクラス
/// </summary>
[System.Serializable]
public class CustomResponseGlobalInfo : ResponseGlobalInfo
{
    /// <summary>
    /// 服务器时间戳
    /// </summary>
    public long ServerTimestamp { get; set; }

    /// <summary>
    /// 服务器区域
    /// </summary>
    public string ServerRegion { get; set; }

    /// <summary>
    /// 额外設定データ
    /// </summary>
    public Dictionary<string, object> ExtraData { get; set; }
}
```

## Related Links

- [リクエストクラス](./5-data-structures.1-request-classes) - リクエストクラスのドキュメント
- [GlobalConfigComponent](./4-core-component.1-global-config-component) - コアコンポーネントドキュメント
- [](./3-getting-started.1-add-to-scene) - コンポーネント
- [プロパティ](./3-getting-started.2-configure-properties) - プロパティ
- [コード](./3-getting-started.3-code-access) - をじてコード
