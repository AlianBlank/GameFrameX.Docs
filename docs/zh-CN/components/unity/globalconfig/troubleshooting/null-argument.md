---
title: "参数为 null 怎么办"
order: 1
autoGenerated:
  source:
    repo: com.gameframex.unity.globalconfig
    commit: e9d07be9bf26f9cf8ac767be220c813116f78b84
    generator: OpenDeepWiki
---
# 参数为 null 怎么办

当调用 `GlobalConfigComponent` 的方法或访问响应对象字段时,经常会遇到 `ArgumentNullException` 或读到 `null` 值。本页说明每种 null 的来源、判断方式与修复办法。

## 快速诊断

| 症状 | 原因 | 修复 |
|------|------|------|
| 调用 `SetOriginalData(null)` 抛出 `ArgumentNullException: data` | 入参为 `null`,方法显式拒绝 | 调用前判断非空,或传入空串 `""` |
| 调用 `SetGlobalConfig(null)` 抛出 `ArgumentNullException: globalInfo` | 入参为 `null`,方法显式拒绝 | 先用反序列化得到非空对象再传入 |
| 读取 `ResponseGlobalInfo.CheckAppVersionUrl` 等字段得到 `null` | JSON 中没有该字段,反序列化结果默认为 `null` | 访问前判空;与服务器约定字段非空或在反序列化后填充默认值 |
| `RequestBase.Language / AppVersion / Platform` 等字段为 `null` | 请求对象属性未赋值 | 在发起请求前为 `RequestBase` 子类属性赋值 |

## 修复步骤

### 1. `SetOriginalData` 报 `ArgumentNullException: data`

`GlobalConfigComponent.SetOriginalData` 在入参为 `null` 时直接抛出异常。

```csharp
public void SetOriginalData(string data)
{
    if (data == null)
    {
        throw new ArgumentNullException(nameof(data));
    }
    m_originalData = data;
}
```

Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L126-L134)

修复:

```csharp
if (!string.IsNullOrEmpty(rawJson))
{
    component.SetOriginalData(rawJson);
}
```

### 2. `SetGlobalConfig` 报 `ArgumentNullException: globalInfo`

`SetGlobalConfig` 同样在入参为 `null` 时抛出异常。

```csharp
public void SetGlobalConfig(ResponseGlobalInfo globalInfo)
{
    if (globalInfo == null)
    {
        throw new ArgumentNullException(nameof(globalInfo));
    }
    m_responseGlobalInfo = globalInfo;
}
```

Source: [GlobalConfigComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/GlobalConfigComponent.cs#L160-L168)

修复:确保传入的是反序列化得到的对象,不是 `null`。如果是从网络获取,先做非空判断再赋值。

### 3. 响应对象字段为 `null`

`ResponseGlobalInfo`、`ResponseGameAppVersion`、`ResponseGameAssetPackageVersion` 的所有字符串属性默认值都是 `null`,只有服务端实际返回的字段才会有值。

```csharp
public class ResponseGlobalInfo
{
    public string CheckAppVersionUrl { get; set; }
    public string CheckResourceVersionUrl { get; set; }
    public string AOTCodeList { get; set; }
    public string Content { get; set; }
}
```

Source: [ResponseGlobalInfo.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGlobalInfo.cs#L6-L27)

修复:使用前判空,或与服务器约定必填字段。

```csharp
var info = component.GlobalInfo;
var url = info?.CheckAppVersionUrl;
if (!string.IsNullOrEmpty(url))
{
    // 使用 url
}
```

### 4. `RequestBase` 字段为 `null`

`RequestBase` 的所有属性都没有默认值,需要由调用方在发起请求前赋值。

```csharp
public abstract class RequestBase
{
    public string Language { get; set; }
    public string AppVersion { get; set; }
    public string Platform { get; set; }
    public string PackageName { get; set; }
    public string Channel { get; set; }
    public string SubChannel { get; set; }
}
```

Source: [RequestBase.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/RequestBase.cs#L6-L37)

修复:在构造请求对象时填齐所有字段。

## 验证

- 在调用 `SetOriginalData` / `SetGlobalConfig` 前用 `string.IsNullOrEmpty` 或 `!= null` 守卫,确认不再抛出 `ArgumentNullException`。
- 对响应字段使用 `?.` 和 `string.IsNullOrEmpty`,确认空值被正确处理而不是触发 `NullReferenceException`。
- 对 `RequestBase` 子类,遍历所有 `string` 属性确认无 `null`,再发起请求。
