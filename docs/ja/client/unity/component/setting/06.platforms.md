# プラットフォーム

ゲームサポート，の Unity プラットフォームと。ドキュメントプラットフォームのプラットフォームの。

## 

GameFrameX モジュール，をじての `SettingHelper`  Unity プラットフォーム。プラットフォームの API に，プロジェクトたのインターフェース `ISettingHelper`，コード，びし API たの。

プラットフォームのコアに：
- **インターフェース**：ののインターフェース
- **プラットフォーム**：コンパイルプラットフォームの
- ****：プラットフォームサポートの `SettingHelper`

## アーキテクチャ

```mermaid
flowchart TD
    subgraph Client["客户端コード"]
        GameLogic[ゲーム逻辑]
    end

    subgraph Core["コア层"]
        SC[SettingComponent]
        SM[SettingManager]
    end

    subgraph Abstract["抽象层"]
        ISh[ISettingHelper]
        SHB[SettingHelperBase]
    end

    subgraph Implementations["实现层"]
        PSSH[PlayerPrefsSettingHelper]
        DSH[DefaultSettingHelper]
        Custom[カスタマイズHelper]
    end

    subgraph Platforms["存储后端"]
        PP[Unity PlayerPrefs]
        TS[TTSDK 抖音存储]
        WX[微信存储]
        KS[快手存储]
        File[ローカルファイル]
    end

    GameLogic --> SC
    SC --> SM
    SM --> ISh
    ISh <|-- SHB
    SHB <|-- PSSH
    SHB <|-- DSH
    SHB <|-- Custom
    PSSH --> PP
    PSSH --> TS
    PSSH --> WX
    PSSH --> KS
    DSH --> File
```

### コアコンポーネント

| コンポーネント |  |
|------|------|
| SettingComponent | Unity コンポーネント，の |
| SettingManager | コアマネージャー， |
| SettingHelperBase | クラス， Helper のインターフェース |
| ISettingHelper | インターフェース， |
| PlayerPrefsSettingHelper | にづく Unity PlayerPrefs の，プラットフォームコード |
| DefaultSettingHelper | にづくローカルファイルの |

## プラットフォームサポート

### PlayerPrefsSettingHelper プラットフォーム

`PlayerPrefsSettingHelper` はデフォルトの，コンパイルの API。はプラットフォームの：

```csharp
// Source: Runtime/Setting/PlayerPrefsSettingHelper.cs#L78-L86
public override bool Save()
{
#if UNITY_WEBGL && (ENABLE_DOUYIN_MINI_GAME || ENABLE_WECHAT_MINI_GAME || ENABLE_KUAISHOU_MINI_GAME)
    return true;
#else
    UnityEngine.PlayerPrefs.Save();
    return true;
#endif
}
```

### サポートのプラットフォーム

| プラットフォーム |  |  |  |
|------|--------|----------|----------|
| Unity Editor | `UNITY_EDITOR` | UnityEngine.PlayerPrefs |  |
| ゲーム | `ENABLE_DOUYIN_MINI_GAME` | TTSDK.TTStorage |  API， |
| ゲーム | `ENABLE_WECHAT_MINI_GAME` | WeChatWASM.WXSDKManagerHandler |  API， |
| ゲーム | `ENABLE_KUAISHOU_MINI_GAME` | KSWASM.KSBase |  API， |
| プラットフォーム | デフォルト | UnityEngine.PlayerPrefs |  PlayerPrefs  |

### プラットフォーム

#### 

```csharp
// Source: Runtime/Setting/PlayerPrefsSettingHelper.cs#L262-L276
public override bool GetBool(string settingName, bool defaultValue)
{
#if UNITY_EDITOR
    return UnityEngine.PlayerPrefs.GetInt(settingName, defaultValue ? 1 : 0) != 0;
#endif
#if UNITY_WEBGL && ENABLE_DOUYIN_MINI_GAME
    return TTSDK.TTStorage.GetIntSync(settingName, defaultValue ? 1 : 0) != 0;
#elif UNITY_WEBGL && ENABLE_WECHAT_MINI_GAME
    return WeChatWASM.WXSDKManagerHandler.Instance.StorageGetIntSync(settingName, defaultValue ? 1 : 0) != 0;
#elif UNITY_WEBGL && ENABLE_KUAISHOU_MINI_GAME
    return KSWASM.KSBase.StorageGetIntSync(settingName, defaultValue ? 1 : 0) != 0;
#else
    return UnityEngine.PlayerPrefs.GetInt(settingName, defaultValue ? 1 : 0) != 0;
#endif
}
```

#### 

```csharp
// Source: Runtime/Setting/PlayerPrefsSettingHelper.cs#L518-L534
public override void SetString(string settingName, string value)
{
#if UNITY_EDITOR
    UnityEngine.PlayerPrefs.SetString(settingName, value);
    return;
#endif

#if UNITY_WEBGL && ENABLE_DOUYIN_MINI_GAME
    TTSDK.TTStorage.SetStringSync(settingName, value);
#elif UNITY_WEBGL && ENABLE_WECHAT_MINI_GAME
    WeChatWASM.WXSDKManagerHandler.Instance.StorageSetStringSync(settingName, value);
#elif UNITY_WEBGL && ENABLE_KUAISHOU_MINI_GAME
    KSWASM.KSBase.StorageSetStringSync(settingName, value);
#else
    UnityEngine.PlayerPrefs.SetString(settingName, value);
#endif
}
```

## 

### に SettingComponent  Helper

```csharp
// Source: Runtime/Setting/SettingComponent.cs#L50-L53
[SerializeField] private string m_SettingHelperTypeName = "GameFrameX.Setting.Runtime.PlayerPrefsSettingHelper";

[SerializeField] private SettingHelperBase m_CustomSettingHelper = null;
```

### パラメータ

| パラメータ | タイプ | デフォルト |  |
|------|------|--------|------|
| m_SettingHelperTypeName | string | "GameFrameX.Setting.Runtime.PlayerPrefsSettingHelper" | のタイプ，に |
| m_CustomSettingHelper | SettingHelperBase | null | カスタマイズ， |

### カスタマイズ

カスタマイズ， `SettingHelperBase`：

```csharp
namespace GameFrameX.Setting.Runtime
{
    /// <summary>
    /// カスタマイズゲーム設定辅助器例
    /// </summary>
    public class CustomSettingHelper : SettingHelperBase
    {
        public override int Count => /* 实现计数逻辑 */;

        public override bool Load() => /* 实现ロード逻辑 */;

        public override bool Save() => /* 实现保存逻辑 */;

        // 实现其他抽象メソッド...
    }
}
```

## デフォルトファイル (DefaultSettingHelper)

た `PlayerPrefsSettingHelper`，プロジェクトた `DefaultSettingHelper`，ローカルファイル：

```csharp
// Source: Runtime/Setting/DefaultSettingHelper.cs#L46-L52
public class DefaultSettingHelper : SettingHelperBase
{
    private const string SettingFileName = "GameFrameworkSetting.dat";
    private string m_FilePath = null;
    private DefaultSetting m_Settings = null;
    private DefaultSettingSerializer m_Serializer = null;
}
```

### パス

```csharp
// Source: Runtime/Setting/DefaultSettingHelper.cs#L511
m_FilePath = Utility.Path.GetRegularPath(Path.Combine(Application.persistentDataPath, SettingFileName));
```

ファイル `Application.persistentDataPath/GameFrameworkSetting.dat`。

### データ

`DefaultSettingHelper` ，：

- ：`GFS` (0x47, 0x46, 0x53)
- データ：， 7 

## プラットフォーム

| シーン |  |  |
|----------|----------|------|
| ゲーム (PC/Mac/Linux) | PlayerPrefsSettingHelper | シンプル， |
| Android/iOS  | PlayerPrefsSettingHelper | PlayerPrefs  |
| //ゲーム | PlayerPrefsSettingHelper | ，ゲーム |
| データ | DefaultSettingHelper | サポートの |
|  | DefaultSettingHelper + カスタマイズ | ファイル |
| /エディター | DefaultSettingHelper | データ，デバッグ |

## リンク

- [](./04.features) - たのコア
- [](./05.helper-implementation) - カスタマイズ Helper のガイド
- [API リファレンス](./07.api-reference) - SettingComponent と SettingManager の API
