# 字典管理

GameFrameX 本地化系統的核心功能之一，提供完整的字典（Dictionary）管理能力，允許開發者在執行時動態管理本地化字串資料。

## 概述

字典管理是本地化系統的基礎架構，負責儲存和管理所有本地化字串資料。GameFrameX 採用了清晰的分層架構：

- **LocalizationManager** - 核心字典管理器，負責字典的儲存和查詢
- **ILocalizationManager** - 字典管理介面，定義標準操作
- **LocalizationComponent** - Unity 組件封裝，提供編輯器配置和執行時訪問

字典管理採用 `Dictionary<string, string>` 內部儲存結構，使用 `StringComparer.Ordinal` 確保鍵的大小寫敏感和高效能比較。

> 原始碼引用：
> - [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L45-L65)
> - [ILocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/ILocalizationManager.cs#L40-L65)

## 架構

```mermaid
flowchart TD
    subgraph UnityLayer["Unity 層"]
        Client[客戶端程式碼]
        LC[LocalizationComponent]
    end

    subgraph ManagerLayer["管理器層"]
        ILM[ILocalizationManager]
        LM[LocalizationManager]
    end

    subgraph DataLayer["資料層"]
        Dict["Dictionary<string, string><br/>_dictionary"]
        LH[LocalizationHelperBase]
    end

    Client --> LC
    LC --> ILM
    ILM <|.. LM
    LM --> Dict
    LM --> LH
```

### 架構說明

1. **客戶端層**：透過 `LocalizationComponent` 組件訪問字典功能
2. **管理器層**：`LocalizationManager` 實現 `ILocalizationManager` 介面，提供完整的字典操作
3. **資料層**：使用 .NET 原生 `Dictionary` 儲存鍵值對，配合 `LocalizationHelperBase` 獲取系統語言

## 核心功能

### 新增字典

使用 `AddRawString` 方法向字典新增新的鍵值對。如果鍵已存在，則新增失敗並返回 `false`。

```csharp
// 透過 LocalizationComponent 新增字典
var localizationComponent = GameEntry.GetComponent<LocalizationComponent>();
bool success = localizationComponent.AddRawString("ui_welcome", "歡迎來到遊戲");
```
> Source: [LocalizationComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/LocalizationComponent.cs#L737-L740)

```csharp
// 透過 LocalizationManager 直接新增
m_LocalizationManager.AddRawString("ui_goodbye", "再見");
```
> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L899-L913)

**設計意圖**：新增操作在鍵已存在時返回 `false`，而不是覆蓋現有值，這是為了保護已載入的字典資料不被意外覆蓋。

### 移除字典

使用 `RemoveRawString` 方法移除指定的字典項，或使用 `RemoveAllRawStrings` 清空所有字典。

```csharp
// 移除指定鍵的字典
bool success = localizationComponent.RemoveRawString("ui_welcome");

// 清空所有字典
localizationComponent.RemoveAllRawStrings();
```
> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L920-L936)

**設計意圖**：提供單個移除和批次清空兩種操作，允許精細控制字典內容。

### 查詢字典

```csharp
// 檢查鍵是否存在
bool exists = localizationComponent.HasRawString("ui_welcome");

// 獲取字典值（不經過格式化）
string rawValue = localizationComponent.GetRawString("ui_welcome");

// 獲取字典值（帶錯誤處理，鍵不存在返回 "<NoKey>key" 格式）
string value = localizationComponent.GetString("ui_welcome");
```
> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L862-L891)

**設計意圖**：
- `HasRawString` 用於檢查鍵是否存在
- `GetRawString` 返回原始值，鍵不存在時返回空字串
- `GetString` 提供更友好的錯誤處理，不存在的鍵返回特定格式的提示資訊

### 獲取字典數量

```csharp
int count = localizationComponent.DictionaryCount;
```
> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L128-L131)

## 引數化字串

GameFrameX 支援在字典值中使用佔位符，並透過引數動態替換。系統提供了從 1 個到 16 個引數的過載方法。

```csharp
// 字典定義：ui_player_level = "玩家等級：{0}，經驗值：{1}"
string result = localizationComponent.GetString("ui_player_level", 10, 500);

// 結果："玩家等級：10，經驗值：500"
```
> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L186-L195)

支援泛型型別引數的方法允許傳入任意型別的引數：

```csharp
// 使用泛型方法指定引數型別
string result = localizationComponent.GetString<int, float>("ui_player_info", 5, 99.5f);
```
> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L233-L249)

**設計意圖**：提供豐富的引數化方法，支援從簡單到複雜的各種字串格式化場景，同時保證型別安全。

## 錯誤處理機制

| 場景 | 返回值 |
|------|--------|
| 鍵不存在（GetRawString） | 空字串 `""` |
| 鍵不存在（GetString） | `"<NoKey>{key}"` |
| 格式化錯誤 | `"<Error>{key},{value},{args},{exception}"` |
| 新增已存在的鍵 | `false` |
| 移除不存在的鍵 | `false` |

> Source: [LocalizationManager.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/Localization/LocalizationManager.cs#L170-L177)

## 字典載入事件

雖然當前版本的字典載入事件被註釋掉了，但框架保留了事件機制用於未來擴充套件：

```csharp
// 事件定義（預留）
// LoadDictionarySuccessEventArgs - 載入成功
// LoadDictionaryFailureEventArgs - 載入失敗  
// LoadDictionaryUpdateEventArgs - 載入進度更新
```
> Source: [LocalizationComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.localization/blob/main/Runtime/Localization/LocalizationComponent.cs#L180-L186)

## API 參考

### LocalizationManager 核心方法

| 方法 | 返回型別 | 說明 |
|------|----------|------|
| `AddRawString(key, value)` | `bool` | 新增字典，成功返回 `true`，鍵已存在返回 `false` |
| `RemoveRawString(key)` | `bool` | 移除字典，成功返回 `true`，鍵不存在返回 `false` |
| `RemoveAllRawStrings()` | `void` | 清空所有字典 |
| `GetRawString(key)` | `string` | 獲取原始字典值，鍵不存在返回空字串 |
| `GetString(key)` | `string` | 獲取字典值，鍵不存在返回 `"<NoKey>key"` |
| `GetString(key, params)` | `string` | 獲取格式化的字典值 |
| `HasRawString(key)` | `bool` | 檢查鍵是否存在 |
| `DictionaryCount` | `int` | 獲取字典數量 |

### LocalizationComponent 封裝方法

| 方法 | 說明 |
|------|------|
| `AddRawString(string key, string value)` | 新增字典條目 |
| `RemoveRawString(string key)` | 移除指定字典 |
| `RemoveAllRawStrings()` | 清空所有字典 |
| `GetRawString(string key)` | 獲取原始值 |
| `GetString(string key)` | 獲取字串（帶錯誤處理） |
| `HasRawString(string key)` | 檢查鍵是否存在 |
| `DictionaryCount` | 字典數量 |

## 最佳實踐

### 1. 批次初始化字典

```csharp
// 在遊戲啟動時批次載入字典
var localizationComponent = GameEntry.GetComponent<LocalizationComponent>();

// 批次新增字典
localizationComponent.AddRawString("ui_start", "開始遊戲");
localizationComponent.AddRawString("ui_settings", "設定");
localizationComponent.AddRawString("ui_exit", "退出遊戲");
localizationComponent.AddRawString("ui_loading", "載入中...{0}%");
```

### 2. 動態更新字典

```csharp
// 根據遊戲程序動態更新字典
public void UpdateGameTitle(string newTitle)
{
    var localizationComponent = GameEntry.GetComponent<LocalizationComponent>();
    // 先移除舊值
    localizationComponent.RemoveRawString("game_title");
    // 新增新值
    localizationComponent.AddRawString("game_title", newTitle);
}
```

### 3. 安全獲取字串

```csharp
// 使用 GetString 而不是 GetRawString 以獲得更好的錯誤反饋
var localizationComponent = GameEntry.GetComponent<LocalizationComponent>();
string text = localizationComponent.GetString("ui_not_exist");

// 如果鍵不存在，text 值為 "<NoKey>ui_not_exist"
// 而不是空字串，便於除錯
```

## 相關連結

- [獲取本地化字串](./4-core-features.get-string) - 詳細瞭解字串獲取方法
- [語言切換與事件](./4-core-features.language-switching) - 瞭解語言切換機制
- [API 參考](./5-api-reference) - 完整的 API 文件
- [配置](./6-configuration) - 系統配置說明
- [最佳實踐](./7-best-practices) - 開發最佳實踐
