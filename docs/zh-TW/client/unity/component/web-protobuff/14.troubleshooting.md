# 常見問題

本文件彙總了使用 GameFrameX Web ProtoBuff 組件過程中可能遇到的常見問題及其解決方案。

## 概述

Web ProtoBuff 組件是 GameFrameX 框架的網路模組，專門用於處理基於 Protocol Buffer 格式的 HTTP 請求。在使用過程中，開發者可能會遇到編譯錯誤、執行時異常、訊息序列化問題等。本文件旨在幫助開發者快速定位和解決這些問題。

---

## 編譯相關問題

### 1. Post 方法不可見

**問題描述：**
在程式碼中無法呼叫 `WebProtoBuffComponent.Post<T>()` 方法，IDE 提示方法不存在。

**原因分析：**
`Post<T>` 方法被包裹在編譯宏 `#if ENABLE_GAME_FRAME_X_WEB_PROTOBUF_NETWORK` 中。如果未定義此宏，方法將不可用。

**解決方案：**
在 Unity 編輯器中，開啟 **Edit > Project Settings > Player**，在 **Scripting Define Symbols** 中新增以下宏定義：

```
ENABLE_GAME_FRAME_X_WEB_PROTOBUF_NETWORK
```

> Source: [WebProtoBuffComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffComponent.cs#L92-L109)

---

### 2. 組件初始化失敗

**問題描述：**
執行遊戲時控制檯出現 "Web manager is invalid." 錯誤日誌。

**原因分析：**
`WebProtoBuffComponent.Awake()` 方法中透過 `GameFrameworkEntry.GetModule<IWebProtoBuffManager>()` 獲取管理器例項，如果返回 null 則會記錄致命錯誤。這通常是因為：

1. GameFrameX 核心框架未正確初始化
2. `WebProtoBuffManager` 模組未註冊
3. 程式集裁剪移除了必要的型別

**解決方案：**
1. 確保 GameFrameX 核心框架已正確安裝並初始化
2. 檢查 `GameFrameXWebProtoBuffCroppingHelper` 組件已新增到 GameObject
3. 如果使用了 IL2CPP 或程式集裁剪，確保新增了必要的 Preserve 屬性

```csharp
// 組件已自動新增 Preserve 屬性
[UnityEngine.Scripting.Preserve]
[RequireComponent(typeof(GameFrameXWebProtoBuffCroppingHelper))]
public sealed class WebProtoBuffComponent : GameFrameworkComponent
```

> Source: [WebProtoBuffComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffComponent.cs#L46-L90)

---

## 執行時錯誤

### 3. 請求超時

**問題描述：**
網路請求長時間無響應，最終丟擲超時異常。

**原因分析：**
組件預設超時時間為 5 秒。如果伺服器響應時間超過此值，會觸發超時錯誤。

WebGL 平臺使用 `UnityWebRequest.timeout`：
```csharp
unityWebRequest.timeout = (int)RequestTimeout.TotalSeconds;
```

其他平臺使用 `HttpWebRequest.Timeout`：
```csharp
request.Timeout = (int)RequestTimeout.TotalMilliseconds;
```

**解決方案：**
1. 透過 Inspector 調整超時時間（範圍 0-120 秒）
2. 在程式碼中動態設定超時：

```csharp
// 獲取組件引用並設定超時時間
var webComponent = GetComponent<WebProtoBuffComponent>();
webComponent.Timeout = 30f; // 設定為 30 秒
```

> Source: [WebProtoBuffComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffComponent.cs#L60-L71)

---

### 4. 網路錯誤

**問題描述：**
請求失敗，控制檯出現網路錯誤資訊。

**原因分析：**
WebGL 平臺透過 `UnityWebRequest.isNetworkError` 和 `UnityWebRequest.isHttpError` 檢測錯誤：

```csharp
if (unityWebRequest.isNetworkError || unityWebRequest.isHttpError || unityWebRequest.error != null)
{
    webData.Task.TrySetException(new Exception(unityWebRequest.error));
    return;
}
```

其他平臺會捕獲 `WebException`：

```csharp
catch (WebException e)
{
    if (e.Status == WebExceptionStatus.Timeout)
    {
        webData.Task.SetException(new TimeoutException(e.Message));
        return;
    }
    webData.Task.SetException(e);
}
```

**解決方案：**
1. 檢查網路連線是否正常
2. 確認伺服器地址 URL 是否正確
3. 檢查防火牆或代理設定
4. 檢視錯誤訊息確定具體原因

---

### 5. 響應訊息型別不匹配

**問題描述：**
請求成功但返回 null 或日誌顯示 "Response message type is invalid"。

**原因分析：**
組件會驗證響應訊息型別是否與請求的型別匹配：

```csharp
var messageType = ProtoMessageIdHandler.GetRespTypeById(messageObjectHttp.Id);
if (messageType != typeof(T))
{
    Log.Error($"Response message type is invalid. Expected '{typeof(T).FullName}', actual '{messageType.FullName}'.");
    return default;
}
```

這通常是因為：
1. 伺服器返回的訊息 ID 與預期不符
2. `ProtoMessageIdHandler` 未正確註冊訊息型別對映

**解決方案：**
1. 確保伺服器返回的訊息 ID 與請求/響應型別對映一致
2. 檢查 `ProtoMessageIdHandler` 是否正確初始化訊息型別

> Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L185-L197)

---

## 訊息定義問題

### 6. 訊息序列化失敗

**問題描述：**
傳送請求時出現序列化異常或伺服器無法解析請求資料。

**原因分析：**
組件使用 `SerializerHelper.Serialize()` 序列化訊息，請求體被包裝為 `MessageHttpObject`：

```csharp
MessageHttpObject messageHttpObject = new MessageHttpObject
{
    Id = id,
    UniqueId = message.UniqueId,
    Body = SerializerHelper.Serialize(message),
};
var sendData = SerializerHelper.Serialize(messageHttpObject);
```

訊息型別必須滿足以下條件：
1. 標記 `[ProtoContract]` 屬性
2. 繼承 `MessageObject` 基類
3. 所有屬性標記 `[ProtoMember]`

**解決方案：**
正確宣告訊息型別：

```csharp
using ProtoBuf;
using GameFrameX.Network.Runtime;

[ProtoContract]
public class LoginRequest : MessageObject
{
    [ProtoMember(1)]
    public string Username { get; set; }
    
    [ProtoMember(2)]
    public string Password { get; set; }
}

[ProtoContract]
public class LoginResponse : MessageObject, IResponseMessage
{
    [ProtoMember(1)]
    public bool Success { get; set; }
    
    [ProtoMember(2)]
    public string Token { get; set; }
}
```

> Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L210-L225)

---

### 7. 響應訊息介面未實現

**問題描述：**
編譯錯誤或執行時異常，提示響應型別需要實現特定介面。

**原因分析：**
`Post<T>` 方法的泛型約束要求型別必須同時繼承 `MessageObject` 並實現 `IResponseMessage` 介面：

```csharp
public Task<T> Post<T>(string url, GameFrameX.Network.Runtime.MessageObject message) 
    where T : GameFrameX.Network.Runtime.MessageObject, GameFrameX.Network.Runtime.IResponseMessage
```

**解決方案：**
確保響應類同時繼承兩個型別：

```csharp
[ProtoContract]
public class LoginResponse : MessageObject, IResponseMessage
{
    [ProtoMember(1)]
    public bool Success { get; set; }
}
```

> Source: [IWebProtoBuffManager.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/IWebProtoBuffManager.cs#L22-L24)

---

## 平臺相關問題

### 8. WebGL 平臺請求失敗

**問題描述：**
在 WebGL 平臺構建後網路請求無法正常工作。

**原因分析：**
WebGL 平臺使用 `UnityWebRequest` 而非 `HttpWebRequest`，兩者 API 和行為存在差異：

```csharp
#if UNITY_WEBGL
        UnityEngine.Networking.UnityWebRequest unityWebRequest;
        if (webData.IsGet)
        {
            unityWebRequest = UnityEngine.Networking.UnityWebRequest.Get(webData.URL);
        }
        else
        {
            unityWebRequest = UnityEngine.Networking.UnityWebRequest.Post(webData.URL, string.Empty);
        }
        
        unityWebRequest.SetRequestHeader("Content-Type", ProtoBufContentType);
        byte[] postData = webData.SendData;
        unityWebRequest.uploadHandler = new UnityEngine.Networking.UploadHandlerRaw(postData);
#else
        // 使用 HttpWebRequest
#endif
```

**解決方案：**
1. 確保 Unity 版本支援 WebGL 請求
2. 檢查瀏覽器控制檯是否有跨域錯誤
3. 伺服器需要配置正確的 CORS 頭
4. 確認 Content-Type 設定為 `application/x-protobuf`

> Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L85-L117)

---

### 9. 程式集裁剪問題

**問題描述：**
在 IL2CPP 打包後組件無法正常工作。

**原因分析：**
Unity 的程式集裁剪可能移除被認為是"未使用"的型別，導致執行時找不到必要的類。

**解決方案：**
組件已新增 `UnityEngine.Scripting.Preserve` 屬性確保不被裁剪：

```csharp
[DisallowMultipleComponent]
[AddComponentMenu("GameFrameX/Web ProtoBuff")]
[UnityEngine.Scripting.Preserve]
[RequireComponent(typeof(GameFrameXWebProtoBuffCroppingHelper))]
public sealed class WebProtoBuffComponent : GameFrameworkComponent
```

如果仍然遇到問題，可以手動新增連結器配置或使用以下程式碼強制保留型別：

```csharp
// 在啟動時呼叫，確保型別不被裁剪
void PreserveTypes()
{
    _ = typeof(WebProtoBuffComponent);
    _ = typeof(IWebProtoBuffManager);
    _ = typeof(WebProtoBuffManager);
}
```

> Source: [GameFrameXWebProtoBuffCroppingHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/GameFrameXWebProtoBuffCroppingHelper.cs#L6-L17)

---

## 除錯與日誌

### 10. 啟用除錯日誌

**問題描述：**
需要檢視請求和響應的詳細資訊進行除錯。

**原因分析：**
組件提供除錯日誌功能，但預設關閉。可透過以下宏啟用：

- `ENABLE_GAMEFRAMEX_WEB_SEND_LOG` - 傳送日誌
- `ENABLE_GAMEFRAMEX_WEB_RECEIVE_LOG` - 接收日誌

**解決方案：**
在 Player Settings 中新增以上宏定義之一或全部：

```
ENABLE_GAMEFRAMEX_WEB_SEND_LOG;ENABLE_GAMEFRAMEX_WEB_RECEIVE_LOG
```

日誌輸出示例：

```csharp
// 傳送日誌
Log.Debug($"傳送訊息 ID:[{messageId},{messageObject.UniqueId},{messageObject.GetType().Name}] 訊息內容:{...}");

// 接收日誌
Log.Debug($"接收訊息 ID:[{messageId},{messageObject.UniqueId},{messageObject.GetType().Name}] 訊息內容:{...}");
```

> Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L227-L241)

---

## 相關連結

- [外掛介紹](./1-overview.introduction)
- [功能特性](./1-overview.features)
- [安裝方式](./2-installation.install-methods)
- [環境要求](./2-installation.requirements)
- [快速入門](./3-getting-started.quick-start)
- [基礎示例](./3-getting-started.basic-example)
- [組件架構](./4-core-concepts.architecture)
- [訊息定義](./4-core-concepts.message-definition)
- [WebProtoBuffComponent API](./5-api-reference.webprotobuffcomponent)
- [IWebProtoBuffManager API](./5-api-reference.iwebprotobuffmanager)
- [組件配置](./6-configuration.component-config)
- [編譯宏定義](./6-configuration.compile-defines)
- [平臺說明](./7-platforms.platform-info)
