# 確認和移除任务

在 Timer 计时器コンポーネント中，除了追加定时任务外，还提供します確認任务是否存在および移除任务的機能。这些機能对于动态管理定时任务、避免重复追加或在特定条件下取消任务非常有用。

## 概要

TimerComponent 提供します两个コアメソッド用于確認和移除任务：

- **Exists** - 確認指定コールバック函数对应的任务是否存在
- **Remove** - 移除（取消）指定コールバック函数对应的任务

这两个メソッド都通过コールバック函数的参照（`Action<object>`）来标识和操作任务，因此〜する必要があります確認してください传入的是同一个デリゲートインスタンス。

## コア機能

### 確認任务是否存在

`Exists` メソッド用于確認指定的コールバック函数是否对应一个有效的定时任务。

```csharp
public bool Exists(Action<object> callback)
```

**実装原理：**

> Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L226-L243)

```csharp
public bool Exists(Action<object> callback)
{
    lock (Locker)
    {
        // 第一步：检查待添加列表
        if (_toAdd.ContainsKey(callback))
        {
            return true;
        }

        // 第二步：检查活跃任务列表
        if (_items.TryGetValue(callback, out var at))
        {
            // 只有未被标记为删除的任务才视为存在
            return !at.Deleted;
        }

        return false;
    }
}
```

**確認逻辑説明：**

1. **待追加列表（`_toAdd`）**：確認任务是否在等待追加队列中刚作成但尚未生效
2. **活跃任务列表（`_items`）**：確認任务是否在当前活跃的任务列表中
3. **削除标记（`Deleted`）**：只有未被标记为削除的任务才返す true

### 移除指定任务

`Remove` メソッド用于移除（取消）指定的定时任务。该メソッド采用延迟削除机制，不会立つまり从字典中移除，而是在下一帧 Update 时清理。

```csharp
public void Remove(Action<object> callback)
```

**実装原理：**

> Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L249-L264)

```csharp
public void Remove(Action<object> callback)
{
    lock (Locker)
    {
        // 如果任务还在待添加队列中，直接取消并归还到对象池
        if (_toAdd.TryGetValue(callback, out var t))
        {
            _toAdd.Remove(callback);
            ReturnToPool(t);
        }

        // 如果任务已在活跃列表中，标记为删除
        if (_items.TryGetValue(callback, out t))
        {
            t.Deleted = true;
        }
    }
}
```

**移除逻辑説明：**

1. **待追加队列**：もし任务尚未开始执行（还在 `_toAdd` 中），直接从队列中移除并归还对象池
2. **活跃列表**：もし任务〜中活跃列表中执行，将其标记为 `Deleted = true`
3. **延迟清理**：在下一帧 `Update()` 循环中，会自动清理所有被标记为 Deleted 的任务

## ワークフロー图

```mermaid
flowchart TD
    subgraph Exists流程["Exists 检查流程"]
        Start1[调用 Exists] --> CheckToAdd{检查 _toAdd}
        CheckToAdd -->|存在| ReturnTrue1[返回 true]
        CheckToAdd -->|不存在| CheckItems{检查 _items}
        CheckItems -->|存在且未删除| ReturnTrue2[返回 true]
        CheckItems -->|不存在或已删除| ReturnFalse[返回 false]
    end

    subgraph Remove流程["Remove 移除流程"]
        Start2[调用 Remove] --> CheckToAdd2{在 _toAdd 中?}
        CheckToAdd2 -->|是| RemoveFromToAdd[从 _toAdd 移除<br/>归还对象池]
        CheckToAdd2 -->|否| CheckItems2{在 _items 中?}
        RemoveFromToAdd --> CheckItems2
        CheckItems2 -->|是| MarkDeleted[标记 Deleted = true]
        CheckItems2 -->|否| EndRemove[结束]
        MarkDeleted --> Cleanup[下一帧 Update 时清理]
    end
```

## 使用例

### 基本用法

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class TimerExample : MonoBehaviour
{
    private TimerComponent _timer;

    private void Start()
    {
        _timer = GetComponent<TimerComponent>();

        // 定义一个回调方法
        void OnTick(object param)
        {
            Debug.Log($"Tick at {Time.time}");
        }

        // 添加一个定时任务，每秒执行一次
        _timer.Add(1.0f, 0, OnTick);

        // 检查任务是否存在
        bool exists = _timer.Exists(OnTick);
        Debug.Log($"任务是否存在: {exists}"); // 输出: True

        // 移除任务
        _timer.Remove(OnTick);

        // 再次检查
        exists = _timer.Exists(OnTick);
        Debug.Log($"任务是否存在: {exists}"); // 输出: False
    }
}
```

> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L72-L89)

### 动态管理任务

在实际应用中，〜の可能性があります〜する必要があります根据游戏ステータス动态追加和移除任务：

```csharp
public class GameTimerManager : MonoBehaviour
{
    private TimerComponent _timer;
    private Action<object> _gameLoopTask;

    private void Start()
    {
        _timer = GetComponent<TimerComponent>();
        _gameLoopTask = OnGameLoop;
    }

    // 暂停游戏时停止循环任务
    public void PauseGame()
    {
        if (_timer.Exists(_gameLoopTask))
        {
            _timer.Remove(_gameLoopTask);
            Debug.Log("游戏循环任务已暂停");
        }
    }

    // 恢复游戏时重新开始循环任务
    public void ResumeGame()
    {
        if (!_timer.Exists(_gameLoopTask))
        {
            _timer.AddUpdate(_gameLoopTask);
            Debug.Log("游戏循环任务已恢复");
        }
    }

    private void OnGameLoop(object param)
    {
        // 游戏循环逻辑
    }
}
```

### Lambda 表达式的注意事项

使用 Lambda 表达式时〜する必要があります特别注意，每次 Lambda 表达式都会作成新的デリゲートインスタンス，导致无法正确匹配和移除任务：

```csharp
// ❌ 错误示例：Lambda 表达式每次创建新实例
_timer.Add(1.0f, 5, (param) => Debug.Log("Tick"));
_timer.Exists((param) => Debug.Log("Tick")); // 返回 False，因为是不同的委托实例
_timer.Remove((param) => Debug.Log("Tick"));  // 无法移除，因为是不同的委托实例

// ✅ 正确示例：使用具名方法或保存委托引用
Action<object> myTick = (param) => Debug.Log("Tick");
_timer.Add(1.0f, 5, myTick);
_timer.Exists(myTick);  // 返回 True
_timer.Remove(myTick);   // 正确移除
```

## 线程安全性

`Exists` 和 `Remove` メソッド都使用 `lock (Locker)` 来保证线程安全。由于 TimerManager 的 Update メソッド也在锁内执行操作，这些メソッド〜できます安全地在任何线程上呼び出し。

> Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L38)

```csharp
private static readonly object Locker = new object();
```

## API リファレンス

### TimerComponent

#### `Exists(callback: Action<object>): bool`

確認指定的任务是否存在。

**パラメータ：**
- `callback` (Action<object>): 要確認的コールバック函数

**返す：** 
- `bool`: 任务存在且未标记为削除时返す true，否则返す false

#### `Remove(callback: Action<object>): void`

移除指定的任务。

**パラメータ：**
- `callback` (Action<object>): 要移除的コールバック函数

**説明：**
- もし任务在待追加队列中，会被立つまり移除
- もし任务在活跃列表中，会被标记为削除，在下一帧清理

### ITimerManager

> Source: [ITimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/ITimerManager.cs#L41-L52)

インターフェース定義完全相同：

```csharp
bool Exists(Action<object> callback);
void Remove(Action<object> callback);
```

## よくある問題

### 为什么 Remove 后 Exists 仍返す true？

もし在 Remove 呼び出し后立つまり呼び出し Exists，〜の可能性があります仍返す true，因为：
1. 任务被标记为 Deleted，但尚未在下一帧 Update 中清理
2. 解決策：在 Remove 后等待一帧，或使用其他机制確認

### Lambda 表达式无法匹配任务怎么办？

如前所述，每次 Lambda 表达式会作成新的デリゲートインスタンス。解決策是：
1. 使用具名メソッド代替 Lambda
2. 保存デリゲート参照并复用

### 多次呼び出し Remove 会出問題吗？

不会。Remove メソッド設計为幂等操作，多次呼び出し不会产生エラー。もし任务不存在，メソッド会静默返す。

## 関連リンク

- [Timer コンポーネント介绍](../1-overview.introduction)
- [機能特性](../1-overview.features)
- [追加定时任务](./4-core-functions.add-timer)
- [追加单次任务](./4-core-functions.add-once)
- [追加帧アップデート任务](./4-core-functions.add-update)
- [API リファレンス - TimerComponent](./5-api-reference.timer-component)
- [API リファレンス - ITimerManager](./5-api-reference.itimer-manager)
