# 安装方式

本文档介绍如何将 GameFrameX Coroutine 协程组件安装到您的 Unity 项目中。

## 概述

GameFrameX Coroutine 协程组件是一个 Unity 包，提供扩展了 Unity 内建协程管理功能的接口。该包允许开发者更容易地控制协程的生命周期，通过使用并发字典来跟踪执行的协程，为停止和清理协程提供更好的控制。

**包信息：**
| 属性 | 值 |
|------|-----|
| 包名 | com.gameframex.unity.coroutine |
| 版本 | 1.0.1 |
| Unity 最低版本 | 2017.1 |
| 分类 | Game Framework X |

## 安装前准备

### 依赖要求

在安装此包之前，请确保您的项目中已经安装了 **GameFrameX.Runtime** 包。该包是本组件的必需依赖项。

```json
// Runtime 程序集依赖关系
{
    "references": [
        "GameFrameX.Runtime"
    ]
}
```

> Source: [GameFrameX.Coroutine.Runtime.asmdef](https://github.com/GameFrameX/com.gameframex.unity.coroutine/blob/main/Runtime/GameFrameX.Coroutine.Runtime.asmdef#L3-L5)

## 安装方式

### 方式一：通过 manifest.json 安装

这种方式适合需要将包作为项目持久依赖的场景。

**操作步骤：**

1. 找到您 Unity 项目的 `Packages` 文件夹
2. 打开 `manifest.json` 文件
3. 在 `dependencies` 节点下添加以下内容：

```json
{
    "dependencies": {
        "com.gameframex.unity.coroutine": "https://github.com/GameFrameX/com.gameframex.unity.coroutine.git"
    }
}
```

4. 保存文件并返回 Unity，Unity 会自动下载并安装包

> Source: [README.md](https://github.com/GameFrameX/com.gameframex.unity.coroutine/blob/main/README.md#L76-L79)

### 方式二：通过 Package Manager 安装

这是最常用的方式，适合大多数开发场景。

**操作步骤：**

1. 打开 Unity 编辑器
2. 导航到 `Window` → `Package Manager`
3. 点击左上角的 `+` 按钮
4. 选择 `Add package from git URL...`
5. 在输入框中粘贴以下地址：

```
https://github.com/GameFrameX/com.gameframex.unity.coroutine.git
```

6. 点击 `Add` 按钮，Unity 会自动下载并安装包

> Source: [README.md](https://github.com/GameFrameX/com.gameframex.unity.coroutine/blob/main/README.md#L80)

### 方式三：手动安装到 Packages 文件夹

这种方式适合离线环境或者需要自定义包内容的场景。

**操作步骤：**

1. 克隆或下载本仓库：
   ```bash
   git clone https://github.com/GameFrameX/com.gameframex.unity.coroutine.git
   ```
   或者访问 GitHub 仓库下载 ZIP 包：
   https://github.com/GameFrameX/com.gameframex.unity.coroutine

2. 将下载的仓库内容复制到您 Unity 项目的 `Packages` 文件夹下

3. Unity 会自动识别并加载包

> Source: [README.md](https://github.com/GameFrameX/com.gameframex.unity.coroutine/blob/main/README.md#L82)

## 安装验证

安装完成后，可以通过以下方式验证是否安装成功：

### 方法一：检查 Package Manager

在 Unity 的 Package Manager 窗口中，确认 `GameFrameX Coroutine` 包已经出现在包列表中，并且版本号显示为 `1.0.1`。

### 方法二：创建组件测试

1. 在 Unity 场景中创建一个空 GameObject
2. 右键点击该 GameObject，选择 `Add Component`
3. 搜索 `Coroutine`，确认 `Coroutine Component` 出现在结果中
4. 添加组件到 GameObject

```csharp
// 验证代码示例
using GameFrameX.Coroutine.Runtime;
using UnityEngine;

public class InstallationVerification : MonoBehaviour
{
    private void Start()
    {
        // 添加协程组件
        CoroutineComponent coroutineComponent = gameObject.AddComponent<CoroutineComponent>();
        
        // 启动一个简单的协程
        coroutineComponent.StartCoroutine(TestCoroutine());
        
        Debug.Log("GameFrameX Coroutine 组件安装成功！");
    }
    
    private System.Collections.IEnumerator TestCoroutine()
    {
        yield return null;
    }
}
```

### 方法三：检查程序集定义

确认以下程序集定义文件已正确加载：

| 程序集 | 路径 |
|--------|------|
| GameFrameX.Coroutine.Runtime | Packages/com.gameframex.unity.coroutine/Runtime/ |
| GameFrameX.Coroutine.Editor | Packages/com.gameframex.unity.coroutine/Editor/ |

## 卸载方式

如需卸载此包，请根据安装方式选择对应的卸载方法：

| 安装方式 | 卸载方法 |
|----------|----------|
| manifest.json | 从 manifest.json 的 dependencies 中移除对应行 |
| Package Manager | 在 Package Manager 中选择包，点击 Remove |
| 手动安装 | 删除 Packages 文件夹下的对应目录 |

## 常见问题

### Q: 安装后出现编译错误

**解决方案：** 确保您的项目中已安装 `GameFrameX.Runtime` 包。这是本组件的必需依赖。

### Q: 无法通过 Git URL 安装

**解决方案：** 
- 检查网络连接是否正常
- 确认 Git URL 输入正确：`https://github.com/GameFrameX/com.gameframex.unity.coroutine.git`
- 尝试使用命令行方式安装：`git clone` 到 Packages 文件夹

### Q: 组件菜单不显示

**解决方案：** 
- 确认包已正确安装
- 尝试重新打开 Unity 项目
- 检查 Console 窗口是否有错误信息

## 相关链接

- [插件介绍](./1-overview.index)
- [使用指南](./3-getting-started.index)
- [API 参考](./4-api-reference.coroutine-component)
- [GitHub 仓库](https://github.com/GameFrameX/com.gameframex.unity.coroutine.git)
