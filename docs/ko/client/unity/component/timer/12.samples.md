# 사용 예시

이 장에서는 Timer 타이머 컴포넌트의 완전한 사용 예시를 제공하며, Unity 프로젝트에서 다양한 타이머 작업 기능을 실제로 적용하는 방법을 보여줍니다.

## 개요

Timer 컴포넌트는 완전한 타이머 작업 솔루션을 제공하며, 다음 타이머 모드를 지원합니다:
- **반복 타이머 작업**: 고정 간격으로 반복 실행
- **1회성 타이머 작업**: 지정 시간 지연 후 한 번 실행
- **프레임 업데이트 작업**: 매 프레임 실행 1회로, 지속 모니터링 시나리오에 적합

이러한 예시는 실제 소스 코드를 기반으로 작성되었으며, 프로젝트에서 직접 사용할 수 있습니다.

## 기본 사용 예시

### 반복 타이머 작업 추가

1초 간격으로 실행되고 총 5회 실행되는 타이머 작업을 생성하는 방법을 보여줍니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class TimerExample : MonoBehaviour
{
    private TimerComponent _timerComponent;

    private void Start()
    {
        // TimerComponent 컴포넌트 가져오기
        _timerComponent = GetComponent<TimerComponent>();
        
        // 타이머 작업 추가: 간격 1초, 5회 실행
        _timerComponent.Add(1f, 5, OnTimerTick, "Hello Timer");
    }

    private void OnTimerTick(object param)
    {
        Debug.Log($"타이머 작업 트리거, 매개변수: {param}");
    }
}
```
> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L37-L40)

### 1회성 타이머 작업 추가

`AddOnce` 메서드를 사용하여 지연 실행 1회 작업을 추가합니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class DelayedTaskExample : MonoBehaviour
{
    private TimerComponent _timerComponent;

    private void Start()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 5초 후 한 번 실행
        _timerComponent.AddOnce(5f, OnDelayedExecute, "지연 작업 완료");
    }

    private void OnDelayedExecute(object param)
    {
        Debug.Log($"지연 작업 실행: {param}");
        // 여기에서 지연 후 로직을 실행할 수 있습니다 (예: 결제 화면 표시 등)
    }
}
```
> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L48-L51)

### 프레임 업데이트 작업 추가

`AddUpdate`를 사용하여 매 프레임 실행 작업을 추가합니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class FrameUpdateExample : MonoBehaviour
{
    private TimerComponent _timerComponent;
    private int _frameCount = 0;

    private void Start()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 매 프레임 작업 실행
        _timerComponent.AddUpdate(OnFrameUpdate, null);
    }

    private void OnFrameUpdate(object param)
    {
        _frameCount++;
        if (_frameCount % 60 == 0) // 60프레임마다 한 번 출력
        {
            Debug.Log($"실행됨 {_frameCount} 프레임");
        }
    }
}
```
> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L57-L70)

## 작업 확인 및 제거

### 작업 존재 여부 확인

실제 응용에서는 특정 타이머 작업이 여전히 실행 중인지 확인해야 할 수 있습니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class CheckTimerExample : MonoBehaviour
{
    private TimerComponent _timerComponent;
    private Action<object> _myTimerCallback;

    private void Start()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 콜백 참조를 저장하여 나중에 확인 가능
        _myTimerCallback = OnTimerTick;
        
        // 타이머 작업 추가
        _timerComponent.Add(1f, 10, _myTimerCallback, null);
        
        // 작업 존재 여부 확인
        bool exists = _timerComponent.Exists(_myTimerCallback);
        Debug.Log($"작업 존재 여부: {exists}");
    }

    private void OnTimerTick(object param)
    {
        Debug.Log("타이머 작업 실행 중...");
    }
}
```
> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L77-L80)

### 지정 작업 제거

콜백 함수 참조를 통해 타이머 작업을 제거할 수 있습니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class RemoveTimerExample : MonoBehaviour
{
    private TimerComponent _timerComponent;
    private Action<object> _repeatTimerCallback;
    private bool _shouldStop = false;

    private void Start()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 제거할 콜백 생성
        _repeatTimerCallback = OnRepeatTick;
        
        // 무한 반복 타이머 작업 추가
        _timerComponent.Add(1f, 0, _repeatTimerCallback, null);
        
        // 10초 후 작업 제거
        _timerComponent.AddOnce(10f, OnStopTimer, null);
    }

    private void OnRepeatTick(object param)
    {
        if (_shouldStop) return;
        Debug.Log("반복 작업 실행 중...");
    }

    private void OnStopTimer(object param)
    {
        _shouldStop = true;
        // 타이머 작업 제거
        _timerComponent.Remove(_repeatTimerCallback);
        Debug.Log("타이머 작업 제거됨");
    }
}
```
> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L86-L89)

## 완전한 사용 예시

게임 개발에서 Timer 컴포넌트를 실제로 적용하는 방법을 보여주는 더 완전한 예시:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;
using UnityEngine.UI;

public class GameTimerController : MonoBehaviour
{
    [Header("UI 컴포넌트")]
    public Text timerText;
    public Button startButton;
    public Button stopButton;

    private TimerComponent _timerComponent;
    private int _remainingSeconds;
    private Action<object> _countdownCallback;
    private Action<object> _gameOverCallback;

    private void Awake()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 콜백 참조 초기화
        _countdownCallback = OnCountdownTick;
        _gameOverCallback = OnGameOver;
    }

    private void Start()
    {
        // 버튼 이벤트 바인딩
        startButton.onClick.AddListener(StartGame);
        stopButton.onClick.AddListener(StopGame);
        
        UpdateTimerDisplay(0);
    }

    private void StartGame()
    {
        // 카운트다운: 1초마다 실행, 총 60초
        _remainingSeconds = 60;
        _timerComponent.Add(1f, 60, _countdownCallback, null);
        
        // 게임 종료 시 트리거
        _timerComponent.AddOnce(60f, _gameOverCallback, "시간 종료!");
    }

    private void StopGame()
    {
        // 모든 타이머 작업 중지
        _timerComponent.Remove(_countdownCallback);
        _timerComponent.Remove(_gameOverCallback);
        Debug.Log("게임이 중지됨");
    }

    private void OnCountdownTick(object param)
    {
        _remainingSeconds--;
        UpdateTimerDisplay(_remainingSeconds);
    }

    private void OnGameOver(object param)
    {
        Debug.Log(param);
        UpdateTimerDisplay(0);
        // 게임 종료 처리 로직
    }

    private void UpdateTimerDisplay(int seconds)
    {
        if (timerText != null)
        {
            int minutes = seconds / 60;
            int secs = seconds % 60;
            timerText.text = $"{minutes:D2}:{secs:D2}";
        }
    }
}
```

### 작업 프로세스図

```mermaid
flowchart TD
    A[게임 시작] --> B{플레이어가 시작 클릭}
    B -->|게임 시작| C[카운트다운 작업 추가<br/>Add 1초, 60회]
    B -->|게임 중지| D[모든 작업 제거<br/>Remove]
    
    C --> E[매초 카운트다운 트리거]
    E --> F{남은 시간 > 0?"}
    F -->|예| G[UI 표시 업데이트]
    G --> E
    F -->|아니오| H[게임 종료 콜백 트리거<br/>AddOnce 60초 후]
    H --> I[게임 종료 처리]
    
    D --> J[모든 타이머 중지]
    
    style A fill:#f9f,stroke:#333
    style I fill:#9f9,stroke:#333
```

## 예외 처리

TimerManager는 전역 예외 포획 기능을 제공하여 타이머 작업의 예외가 게임 크래시를 발생시키는 것을 방지합니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class ExceptionHandlingExample : MonoBehaviour
{
    private TimerComponent _timerComponent;

    private void Start()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 예외 포획 활성화
        TimerManager.CatchCallbackExceptions = true;
        
        // 예외가 발생할 수 있는 타이머 작업 추가
        _timerComponent.Add(2f, 3, OnRiskyTask, null);
    }

    private void OnRiskyTask(object param)
    {
        // 발생할 수 있는 예외 시뮬레이션
        if (Random.value > 0.5f)
        {
            throw new System.Exception("시뮬레이션된 타이머 작업 예외");
        }
        Debug.Log("작업 안전하게 실행됨");
    }
}
```
> Source: [TimerManager.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerManager.cs#L43)

## 매개변수 전달 예시

모든 타이머 작업은 매개변수 전달을 지원하며, 콜백에서 수신할 수 있습니다:

```csharp
using GameFrameX.Timer.Runtime;
using UnityEngine;

public class ParameterExample : MonoBehaviour
{
    private TimerComponent _timerComponent;

    private void Start()
    {
        _timerComponent = GetComponent<TimerComponent>();
        
        // 사용자 정의 매개변수 객체 전달
        var gameData = new GameTimerData 
        { 
            levelId = 1, 
            score = 1000 
        };
        
        _timerComponent.Add(1f, 5, OnGameTimerTick, gameData);
    }

    private void OnGameTimerTick(object param)
    {
        if (param is GameTimerData data)
        {
            Debug.Log($"레벨: {data.levelId}, 점수: {data.score}");
            data.score += 100;
        }
    }
}

public class GameTimerData
{
    public int levelId;
    public int score;
}
```
> Source: [TimerComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.timer/blob/main/Runtime/Timer/TimerComponent.cs#L37-L40)

## 사용 시나리오 요약

| 시나리오 | 권장 메서드 | 설명 |
|------|----------|------|
| 정기 데이터 갱신 | Add | 고정 간격으로 반복 실행 |
| 지연 실행 | AddOnce | 지정 시간 지연 후 한 번 실행 |
| 매 프레임 감지 | AddUpdate | 입력 감지, 상태 모니터링에 적합 |
| 카운트다운 기능 | Add + AddOnce | 반복 표시 카운트다운, 종료 시 종료 콜백 트리거 |
| 스킬 쿨다운 | Add | 주기적으로 쿨다운 시간 확인 |
| 애니메이션 재생 | AddOnce | 지연 재생 또는 시퀀스 프레임 애니메이션 |

## 관련 링크

- [Timer 컴포넌트 소개](../1-overview.introduction)
- [기능 특성](../1-overview.features)
- [타이머 추가](../4-core-functions.add-timer)
- [1회성 작업 추가](../4-core-functions.add-once)
- [프레임 업데이트 작업 추가](../4-core-functions.add-update)
- [작업 확인 및 제거](../4-core-functions.exists-remove)
- [TimerComponent API 참고](../5-api-reference.timer-component)
- [ITimerManager API 참고](../5-api-reference.itimer-manager)
- [TimerManager API 참고](../5-api-reference.timer-manager)
- [자주 묻는 질문](../7-troubleshooting)