---
title: "如何定义自定义事件参数"
order: 1
autoGenerated:
  source:
    repo: com.gameframex.unity.event
    commit: a5f8cf0edf8e3925da36478adc798c737af4c682
    generator: OpenDeepWiki
---
# 如何定义自定义事件参数

定义自定义事件参数只需新建一个类继承 `GameEventArgs`，重写 `Id` 与 `Clear()`，并提供一个用 `ReferencePool.Acquire<T>()` 创建实例的静态 `Create` 工厂方法，之后即可通过 `EventComponent` 订阅与抛出该事件。

## 前置条件

- 工程中已安装 GameFrameX Event 包，场景中存在 `EventComponent` 组件（菜单 `GameFrameX/Event`，框架启动时会自动挂载）。
- 引用命名空间 `GameFrameX.Event.Runtime` 与 `GameFrameX.Runtime`。

## 操作步骤

1. 新建事件参数类，继承 `GameEventArgs`（它是 `BaseEventArgs` 的抽象子类，并带有防止代码裁剪的 `Preserve` 特性标记）：

```csharp
using GameFrameX.Runtime;

namespace GameFrameX.Event.Runtime
{
    /// <summary>
    /// 游戏逻辑事件基类。
    /// </summary>
    [UnityEngine.Scripting.Preserve]
    public abstract class GameEventArgs : BaseEventArgs
    {
    }
}
```

Source: [GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs#L30-L41)

2. 按照框架自带的 `EmptyEventArgs` 的写法实现你的事件参数：重写 `Id` 返回事件编号、重写 `Clear()` 还原字段、提供静态 `Create` 方法从引用池取对象。下面的 `PlayerLevelUpEventArgs` 是按同一模式编写的自定义示例：

```csharp
using GameFrameX.Runtime;

namespace GameFrameX.Event.Runtime
{
    /// <summary>
    /// 玩家升级事件参数
    /// </summary>
    public sealed class PlayerLevelUpEventArgs : GameEventArgs
    {
        public int OldLevel { get; private set; }
        public int NewLevel { get; private set; }

        /// <summary>
        /// 事件编号，默认用类型全名，订阅与抛出时使用同一字符串
        /// </summary>
        public override string Id
        {
            get { return typeof(PlayerLevelUpEventArgs).FullName; }
        }

        /// <summary>
        /// 引用池回收时会调用，必须把所有字段还原为默认值
        /// </summary>
        public override void Clear()
        {
            OldLevel = 0;
            NewLevel = 0;
        }

        /// <summary>
        /// 创建事件参数，从引用池获取实例
        /// </summary>
        public static PlayerLevelUpEventArgs Create(int oldLevel, int newLevel)
        {
            var eventArgs = ReferencePool.Acquire<PlayerLevelUpEventArgs>();
            eventArgs.OldLevel = oldLevel;
            eventArgs.NewLevel = newLevel;
            return eventArgs;
        }
    }
}
```

上面参考的框架原始实现如下，`Id`、`Clear()`、`Create` 三件套与 `ReferencePool.Acquire<T>()` 的用法都以此为准：

```csharp
public sealed class EmptyEventArgs : GameEventArgs
{
    private string _eventId = typeof(EmptyEventArgs).FullName;

    public override void Clear()
    {
        _eventId = typeof(EmptyEventArgs).FullName;
    }

    public override string Id
    {
        get { return _eventId; }
    }

    public static EmptyEventArgs Create(string eventId)
    {
        var eventArgs = ReferencePool.Acquire<EmptyEventArgs>();
        eventArgs.SetEventId(eventId);
        return eventArgs;
    }

    private void SetEventId(string eventId)
    {
        _eventId = eventId;
    }
}
```

Source: [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs#L7-L49)

3. 订阅事件。`EventComponent` 对外的订阅入口是 `CheckSubscribe`（不存在时才订阅，避免重复订阅）：

```csharp
// 签名：public void CheckSubscribe(string id, EventHandler<GameEventArgs> handler)
EventComponent.CheckSubscribe(typeof(PlayerLevelUpEventArgs).FullName, OnPlayerLevelUp);

private void OnPlayerLevelUp(object sender, GameEventArgs e)
{
    var args = (PlayerLevelUpEventArgs)e;
    Debug.Log($"升级：{args.OldLevel} -> {args.NewLevel}");
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L100-L111)

4. 抛出事件。`Fire` 线程安全、下一帧分发；`FireNow` 立即分发但非线程安全：

```csharp
// 签名：public void Fire(object sender, GameEventArgs e)
EventComponent.Fire(this, PlayerLevelUpEventArgs.Create(10, 11));

// 签名：public void FireNow(object sender, GameEventArgs e)
EventComponent.FireNow(this, PlayerLevelUpEventArgs.Create(10, 11));
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L142-L171)

5. 不再需要时取消订阅，事件编号必须与订阅时完全一致：

```csharp
// 签名：public void Unsubscribe(string id, EventHandler<GameEventArgs> handler)
EventComponent.Unsubscribe(typeof(PlayerLevelUpEventArgs).FullName, OnPlayerLevelUp);

// handler 可能未订阅时用安全版本，不存在时不执行任何操作
EventComponent.CheckUnsubscribe(typeof(PlayerLevelUpEventArgs).FullName, OnPlayerLevelUp);
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L113-L131)

## 常见变体

- **不带数据的事件**：不必新建类，直接使用框架内置的 `EmptyEventArgs`。`EventComponent` 提供了便捷重载 `Fire(object sender, string eventId)`，内部会自动 `EmptyEventArgs.Create(eventId)`，`eventId` 传 `null` 或空字符串会触发参数校验：

```csharp
public void Fire(object sender, string eventId)
{
    GameFrameworkGuard.NotNullOrEmpty(eventId, nameof(eventId));
    m_EventManager.Fire(sender, EmptyEventArgs.Create(eventId));
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L152-L161)

- **自定义事件编号字符串**：如果不想用 `typeof(X).FullName`，可像 `EmptyEventArgs` 那样把编号存为私有字段 `_eventId`，由 `Create(eventId)` 传入，一个类即可承载多个逻辑事件。

## 常见错误

| 现象 | 原因 | 修复 |
|------|------|------|
| 抛出事件后没有任何回调 | 订阅与抛出使用的 `Id` 字符串不一致 | 两处统一使用 `typeof(PlayerLevelUpEventArgs).FullName` |
| 事件回调里读到的数据是脏值 | `Clear()` 没有把全部字段还原，实例被引用池复用后残留旧值 | 在 `Clear()` 中重置所有字段 |
| 重复回调同一帧执行多次 | 多次 `Subscribe` 同一 handler | 改用 `CheckSubscribe`，先 `Check` 再订阅 |
| 对象销毁后回调仍触发、抛空引用 | 忘记 `Unsubscribe` | 在 `OnDestroy` 中调用 `Unsubscribe` 或 `CheckUnsubscribe` |
| 自定义参数类被代码裁剪掉 | 类未保留 | 加 `[UnityEngine.Scripting.Preserve]`，与 `GameEventArgs` 一致 |

## 背景

事件参数实例来自 `ReferencePool` 引用池（`ReferencePool.Acquire<T>()`），抛出分发后会被回收复用，因此 `Clear()` 必须把字段恢复默认值；这也是所有事件参数都通过静态 `Create` 创建、而不是 `new` 的原因。事件分发与线程语义由 `IEventManager` 管理，`Fire` 保证回调发生在主线程。

## 相关链接

- [GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs) — 事件参数基类
- [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs) — 框架内置空事件参数，自定义类的标准模板
- [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs) — 订阅 / 抛出事件的组件入口
- [IEventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/IEventManager.cs) — 事件管理器接口
