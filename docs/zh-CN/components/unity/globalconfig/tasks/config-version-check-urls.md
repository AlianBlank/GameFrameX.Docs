---
title: "如何配置应用与资源版本检查 URL"
order: 1
autoGenerated:
  source:
    repo: com.gameframex.unity.globalconfig
    commit: e9d07be9bf26f9cf8ac767be220c813116f78b84
    generator: OpenDeepWiki
---
# 如何配置应用与资源版本检查 URL

GlobalConfig 通过服务器下发的全局配置对象返回应用版本检测地址和资源版本检测地址，前端只需消费 `CheckAppVersionUrl` 与 `CheckResourceVersionUrl` 两个字段即可发起对应的版本检查请求。

## 前置条件

- 已安装 `com.gameframex.unity.globalconfig` 包并完成入口组件注册。
- 业务侧已部署好返回全局信息的接口,接口 JSON 中包含 `CheckAppVersionUrl` 与 `CheckResourceVersionUrl` 字段。
- 客户端能够访问该接口所在的主机服务器地址(参见 `ResponseGlobalInfo.HostServerUrl` 字段)。

## 操作步骤

1. 在服务器全局信息接口的响应 JSON 中,确保同时返回两个 URL 字段,字段名与客户端模型严格一致:

   ```json
   {
     "CheckAppVersionUrl": "https://your-cdn.example.com/api/app/version",
     "CheckResourceVersionUrl": "https://your-cdn.example.com/api/resource/version"
   }
   ```

2. 客户端解析该 JSON 后,直接读取 `ResponseGlobalInfo` 的两个字符串属性。两个属性均为 `string`,默认值为 `null`。

   ```csharp
   using GameFrameX.GlobalConfig.Runtime;

   var info = new ResponseGlobalInfo
   {
       CheckAppVersionUrl      = "https://your-cdn.example.com/api/app/version",
       CheckResourceVersionUrl = "https://your-cdn.example.com/api/resource/version",
       HostServerUrl           = "https://your-cdn.example.com",
       AOTCodeList             = "[]",
       Content                 = "{}"
   };
   ```

   Source: [Runtime/GlobalConfig/ResponseGlobalInfo.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGlobalInfo.cs#L1-L28)

3. 将得到的 URL 分别交给应用版本检查与资源版本检查模块,发起对应的请求对象:

   ```csharp
   // 应用版本检查请求
   var appVersionReq = new RequestGameAppVersion
   {
       AppVersion = Application.version,
       Platform   = Application.platform.ToString(),
       PackageName = Application.identifier,
       Channel    = "default",
       Language   = "zh-CN"
   };
   ```

   Source: [Runtime/GlobalConfig/RequestGameAppVersion.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/RequestGameAppVersion.cs)

4. 资源版本检查使用独立的请求对象,字段同样为平台、渠道、语言等基础信息:

   ```csharp
   var assetReq = new RequestGameAssetPackageVersion
   {
       AppVersion = Application.version,
       Platform   = Application.platform.ToString(),
       PackageName = Application.identifier,
       Channel    = "default",
       Language   = "zh-CN"
   };
   ```

   Source: [Runtime/GlobalConfig/RequestGameAssetPackageVersion.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/RequestGameAssetPackageVersion.cs)

5. 服务器在处理应用版本检查请求时返回 `ResponseGameAppVersion`,其中包含 `IsForce`、`IsUpgrade`、`AppDownloadUrl`、`UpdateAnnouncement`、`UpdateTitle` 等升级信息:

   ```csharp
   public class ResponseGameAppVersion
   {
       public bool   IsForce           { get; set; }
       public string AppDownloadUrl    { get; set; }
       public bool   IsUpgrade         { get; set; }
       public string UpdateAnnouncement{ get; set; }
       public string UpdateTitle       { get; set; }
   }
   ```

   Source: [Runtime/GlobalConfig/ResponseGameAppVersion.cs](https://github.com/GameFrameX/com.gameframex.unity.globalconfig/blob/main/Runtime/GlobalConfig/ResponseGameAppVersion.cs#L1-L33)

## 常见变体

- **同地址复用**:如果应用与资源版本检查共用同一网关,可以让两个 URL 返回相同地址,在网关内按 path 路由区分。
- **灰度分服**:不同渠道(`Channel`)使用不同 URL 时,在服务器端按请求体中的 `Channel` 字段返回对应地址即可,客户端代码保持不变。

## 常见错误

- **字段名拼错**:接口字段名必须为 `CheckAppVersionUrl`、`CheckResourceVersionUrl`,首字母大写、驼峰命名,否则 `ResponseGlobalInfo` 反序列化后两个属性都为 `null`。
- **HTTPS 未就绪**:URL 必须使用 HTTPS,否则在 Android 9+ 上会被默认网络策略拦截。
- **忘记配置主机地址**:`CheckAppVersionUrl` 实际只携带相对路径或子域,真正的接入点还需要 `ResponseGlobalInfo.HostServerUrl`,否则请求会找不到主机。

## 背景(可选)

`ResponseGlobalInfo` 是 GlobalConfig 包定义的最简 DTO,只承担"把地址告诉客户端"的职责。包内没有内置 HTTP 客户端,实际请求与缓存由上层业务模块基于 `RequestGameAppVersion` / `RequestGameAssetPackageVersion` 发起,与全局配置解耦,便于不同网络栈接入。