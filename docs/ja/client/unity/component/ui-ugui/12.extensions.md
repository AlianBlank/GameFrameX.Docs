# 扩展メソッド

提供便捷的扩展メソッド，增强 UGUI コンポーネント的機能，简化常见 UI 操作的代码编写。

## 概要

扩展メソッド是 GameFrameX UGUI 插件提供的一组静态扩展メソッド，を通じて C# 扩展メソッド语法为 Unity 内置的 UGUI コンポーネント添加便捷機能。这些扩展メソッド涵盖了 UI 布局、按钮イベント处理和图片异步加载等常见シーン，能够显著减少样板代码，開発効率の向上。

所有扩展メソッド都位于 `GameFrameX.UI.UGUI.Runtime` 名前空間下，使用 `[Preserve]` プロパティ标记以确保在 IL2CPP 等代码裁剪环境中不会丢失。

## 架构

扩展メソッド按照目标コンポーネント进行分クラス，主要分为三クラス：

```mermaid
graph TD
    subgraph Extensions["扩展メソッド模块"]
        RectTransformExt[RectTransform 扩展]
        ButtonExt[Button 扩展]
        ImageExt[Image 扩展]
    end
    
    subgraph Targets["扩展目标"]
        RT[UnityEngine.RectTransform]
        Button[UnityEngine.UI.Button]
        Image[UnityEngine.UI.Image]
    end
    
    RectTransformExt --> RT
    ButtonExt --> Button
    ImageExt --> Image
    
    subgraph Dependencies["依赖"]
        Asset[AssetComponent]
        GameEntry[GameEntry]
    end
    
    ImageExt --> Asset
    Asset --> GameEntry
```

### 扩展メソッド分クラス

| 扩展クラス | 目标コンポーネント | 主要機能 |
|--------|----------|----------|
| `RectTransformExtension` | `RectTransform` | 快速設定全屏布局 |
| `UGUIButtonExtension` | `Button.ButtonClickedEvent` | 简化按钮イベント绑定 |
| `UGUIImageExtension` | `UnityEngine.UI.Image` | 异步加载图标リソース |

## RectTransform 扩展

`RectTransformExtension` クラス为 `RectTransform` コンポーネント提供布局関連的扩展メソッド。

### MakeFullScreen

将当前 UI 对象設定为全屏显示。

```csharp
public static void MakeFullScreen(this RectTransform rectTransform)
```

**実装原理：**

该メソッドを通じて設定 RectTransform 的四个关键プロパティ来実装全屏效果：

1. `anchorMin = Vector2.zero` - 設定锚点最小值为 (0,0)，即父容器左下角
2. `anchorMax = Vector2.one` - 設定锚点最大值为 (1,1)，即父容器右上角
3. `anchoredPosition = Vector2.zero` - 重置锚点位置为零
4. `sizeDelta = Vector2.zero` - 設定尺寸增量为零，使元素完全填满父容器

> Source: [RectTransformExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/RectTransformExtension.cs#L56-L62)

**使用示例：**

```csharp
using GameFrameX.UI.UGUI.Runtime;

// 假设有一个 RectTransform 引用
RectTransform fullScreenPanel = someUIObject.GetComponent<RectTransform>();

// 一行代码設定为全屏
fullScreenPanel.MakeFullScreen();
```

## Button 扩展

`UGUIButtonExtension` クラス为 `Button.ButtonClickedEvent` 提供イベント处理関連的扩展メソッド，简化按钮点击イベント的绑定和管理。

### メソッド列表

| メソッド | 機能説明 |
|------|----------|
| `Add` | 添加按钮点击イベント监听器 |
| `Remove` | 移除按钮点击イベント监听器 |
| `Clear` | 清除所有按钮点击イベント监听器 |
| `Set` | 設定按钮点击イベント（替换现有イベント） |
| `Set(action, userData)` | 設定带用户数据的点击イベント |

### Add

添加按钮点击イベント监听器。

```csharp
public static void Add(this Button.ButtonClickedEvent self, UnityAction action)
```

**参数：**
- `self`: 按钮的点击イベント集合
- `action`: 点击时要执行的回调

> Source: [UGUIButtonExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIButtonExtension.cs#L52-L57)

### Remove

移除指定的按钮点击イベント监听器。

```csharp
public static void Remove(this Button.ButtonClickedEvent self, UnityAction action)
```

> Source: [UGUIButtonExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIButtonExtension.cs#L67-L72)

### Clear

清除所有按钮点击イベント监听器。

```csharp
public static void Clear(this Button.ButtonClickedEvent self)
```

> Source: [UGUIButtonExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIButtonExtension.cs#L81-L85)

### Set

設定按钮点击イベント，将原有イベント全部替换为新イベント。

```csharp
public static void Set(this Button.ButtonClickedEvent self, UnityAction action)
```

> Source: [UGUIButtonExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIButtonExtension.cs#L95-L101)

### Set (带用户数据)

設定带用户数据的按钮点击イベント。

```csharp
public static void Set(this Button.ButtonClickedEvent self, UnityAction<object> action, object userData)
```

**参数：**
- `self`: 按钮的点击イベント集合
- `action`: 带用户数据参数的回调函数
- `userData`: 传递给回调的用户自定义数据

> Source: [UGUIButtonExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIButtonExtension.cs#L112-L124)

**使用示例：**

```csharp
using GameFrameX.UI.UGUI.Runtime;
using UnityEngine.UI;
using UnityEngine;

// 取得按钮コンポーネント
Button button = GetComponent<Button>();

// 添加点击イベント
button.onClick.Add(() => {
    Debug.Log("按钮被点击");
});

// 使用带参数的イベント
button.onClick.Set((userData) => {
    Debug.Log($"按钮被点击，用户数据: {userData}");
}, "自定义数据");

// 清除所有イベント
button.onClick.Clear();
```

## Image 扩展

`UGUIImageExtension` クラス为 `UnityEngine.UI.Image` 提供异步加载图标的機能。

### SetIconAsync

异步設定图片图标。

```csharp
public static async Task SetIconAsync(this UnityEngine.UI.Image self, string icon)
```

**参数：**
- `self`: 目标图片コンポーネント
- `icon`: 图标リソース路径（相对于リソースディレクトリ）

**返回值：**
- 返回 `Task` 异步任务

**実装原理：**

1. を通じて `GameEntry.GetComponent<AssetComponent>()` 取得リソースコンポーネント
2. 使用 `LoadAssetAsync<Texture2D>` 异步加载纹理リソース
3. 将加载的 `Texture2D` 转换为 `Sprite` 对象
4. 将生成的 Sprite 赋值给 Image コンポーネント的 sprite プロパティ

> Source: [UGUIImageExtension.cs](https://github.com/GameFrameX/com.gameframex.unity.ui.ugui/blob/main/Runtime/UGUIImageExtension.cs#L55-L74)

**異常处理：**

该メソッド内置了 try-catch 異常处理机制，加载失败时会输出错误日志但不会抛出異常，确保 UI 不会因リソース加载失败而崩溃。

**使用示例：**

```csharp
using GameFrameX.UI.UGUI.Runtime;
using UnityEngine.UI;

// 取得 Image コンポーネント
Image iconImage = GetComponent<Image>();

// 异步加载并設定图标
await iconImage.SetIconAsync("icons/player_avatar");

// 可能配合 UIFormHelper 使用
await iconImage.SetIconAsync(formHelper.GetIconPath("item_001"));
```

## 常见使用シーン

### シーン一：作成全屏弹窗

```csharp
// 作成全屏半透明背景
GameObject bgObj = new GameObject("PopupBackground");
bgObj.transform.SetParent(canvas.transform);
RectTransform bgRT = bgObj.AddComponent<RectTransform>();
Image bgImage = bgObj.AddComponent<Image>();
bgImage.color = new Color(0, 0, 0, 0.5f);

// 設定为全屏
bgRT.MakeFullScreen();
```

### シーン二：批量設定按钮イベント

```csharp
// 批量绑定按钮イベント
Button[] buttons = GetComponentsInChildren<Button>();
foreach (var btn in buttons)
{
    btn.onClick.Set(() => OnButtonClick(btn.name));
}
```

### シーン三：异步加载物品图标

```csharp
// 在 UIForm 的加载フロー中异步設定图标
public async void LoadItemIcon(Image iconImage, string itemId)
{
    await iconImage.SetIconAsync($"icons/items/{itemId}");
}
```

## 関連リンク

- [UIImage コンポーネント](./5-components.uiimage) - UIImage コンポーネント文档
- [UGUI 基クラス](./4-core-concepts.ugui-base) - UGUI 基クラス介绍
- [UI 管理器](./4-core-concepts.ui-manager) - UI 管理器文档
- [表单辅助器](./4-core-concepts.form-helper) - 表单辅助器文档
