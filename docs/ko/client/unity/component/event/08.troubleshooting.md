# 자주 묻는 질문

본 문서는 GameFrameX Event 이벤트 시스템 플러그인 사용 시 자주 발생하는 일반적인 문제 및 해결책을 통합합니다.

## 개요

GameFrameX Event는 Game Framework 기반의 이벤트 시스템 플러그인으로, 타입 안전한 이벤트 구독, 배포 및 관리 기능을 제공합니다. 사용 과정에서 개발자는 일부 일반적인 문제에 직면할 수 있으며, 본 문서는 상세한 트러블슈팅 가이드 및 해결책을 제공합니다.

## 일반적인 문제 및 해결책

### 1. 이벤트 수신 불가

#### 증상
이벤트 배포 후 구독자가 이벤트 통지를 받지 못함.

#### 원인 분석
- 이벤트 ID 불일치
- 구독 타이밍이 너무 빠름 (이벤트 관리자가 아직 초기화되지 않음)
- 구독자가 파괴되었지만 구독 취소되지 않음
- 잘못된 이벤트 배포 방식 사용

#### 해결책

**이벤트 ID 일관성 확인:**

```csharp
// 배포 및 구독 시 동일한 이벤트 ID 사용 확인
public class GameStartEventArgs : GameEventArgs
{
    public override string Id => "game_start";  // 통합된 이벤트 ID 정의
}

// 구독 시
eventComponent.CheckSubscribe("game_start", OnGameStart);

// 배포 시
eventComponent.Fire(this, GameStartEventArgs.Create("game_start"));
```

> Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L98-L101)

**구독 타이밍 올바르게 설정:**

```csharp
public class MyComponent : MonoBehaviour
{
    private void Start()
    {
        // Awake 또는 Start에서 이벤트 구독,此时事件系统已初始化
        var eventComponent = GameEntry.GetComponent<EventComponent>();
        eventComponent.CheckSubscribe("game_start", OnGameStart);
    }

    private void OnDestroy()
    {
        // 컴포넌트 파괴 시 구독 취소하여 메모리 누수 방지
        var eventComponent = GameEntry.GetComponent<EventComponent>();
        if (eventComponent != null)
        {
            eventComponent.Unsubscribe("game_start", OnGameStart);
        }
    }

    private void OnGameStart(object sender, GameEventArgs e)
    {
        // 이벤트 처리
    }
}
```

> Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L82-L88)

---

### 2. 메모리 누수

#### 증상
게임 실행 시간이 길어질수록 메모리 점유가 지속적으로 증가하거나, 장면 전환 후 예외 발생.

#### 원인 분석
- 이벤트 구독 후 구독 취소하지 않음
- 이벤트 처리 함수가 대량의 객체 참조 보유
- 이벤트 파라미터 객체가 객체 풀에 올바르게 Released되지 않음

#### 해결책

**항상 적절한 위치에서 구독 취소:**

```csharp
public class PlayerController : MonoBehaviour
{
    private EventHandler<GameEventArgs> _onGameStartHandler;

    private void Awake()
    {
        //委托 참조를 保存하여后续 구독 취소에 사용
        _onGameStartHandler = OnGameStart;
    }

    private void Start()
    {
        var eventComponent = GameEntry.GetComponent<EventComponent>();
        eventComponent.CheckSubscribe("game_start", _onGameStartHandler);
    }

    private void OnDestroy()
    {
        // 중요: 파괴 시 구독 취소!
        var eventComponent = GameEntry.GetComponent<EventComponent>();
        if (eventComponent != null)
        {
            eventComponent.Unsubscribe("game_start", _onGameStartHandler);
        }
    }

    private void OnGameStart(object sender, GameEventArgs e)
    {
        // 이벤트 처리
    }
}
```

> Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L106-L114)

**객체 풀 올바르게 사용:**

```csharp
// 自定义 이벤트 파라미터 생성 시 객체 풀 사용
public class ScoreEventArgs : GameEventArgs
{
    public int Score { get; private set; }

    public static ScoreEventArgs Create(int score)
    {
        var args = ReferencePool.Acquire<ScoreEventArgs>();
        args.Score = score;
        return args;
    }

    public override void Clear()
    {
        Score = 0;
    }

    public override string Id => "score_changed";
}

// 이벤트 배포
eventComponent.Fire(this, ScoreEventArgs.Create(100));

// 주의: Fire 메서드는 다음 프레임에서 자동으로 파라미터를 객체 풀에 Released합니다
// FireNow를 사용하면 수동으로 Released해야 합니다
```

> Source: [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs#L32-L37)

---

### 3. 이벤트 시스템 초기화 실패

#### 증상
실행 시 "Event manager is invalid" 오류가 발생하거나 이벤트 기능이 완전히 사용 불가.

#### 원인 분석
- 필수 의존성 패키지 `GameFrameX.Runtime` 없음
- EventComponent가 장면에 올바르게 추가되지 않음
- GameFrameworkEntry가 올바르게 초기화되지 않음

#### 해결책

**의존성 패키지 설치:**

`GameFrameX.Runtime` 의존성 패키지가 설치되었는지 확인합니다. 다음 방식으로 설치할 수 있습니다:

```json
// manifest.json의 dependencies에 추가
{
  "com.gameframex.unity.runtime": "https://github.com/GameFrameX/com.gameframex.unity.runtime.git"
}
```

> Source: [GameFrameX.Event.Runtime.asmdef](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/GameFrameX.Event.Runtime.asmdef#L2-L5)

**EventComponent 올바르게 구성:**

```csharp
// 장면에 빈 객체 생성하여 EventComponent 추가
public class GameEntry : MonoBehaviour
{
    public static EventComponent EventComponent { get; private set; }

    private void Awake()
    {
        // 장면에 EventComponent가 있는지 확인
        EventComponent = gameObject.GetOrAddComponent<EventComponent>();
    }
}
```

> Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L43-L54)

---

### 4. 이벤트 배포 타이밍 문제

#### 증상
- 이벤트가 예상보다 먼저 또는 나중에 트리거됨
- 비동기 操作에서 이벤트 처리 순서가 올바르지 않음

#### 원인 분석
- `Fire()` 및 `FireNow()`의 차이 혼동
- 스레드 안전성 메커니즘에 대한 이해 부족

#### 해결책

**두 가지 배포 모드의 차이 이해:**

| 메서드 | 스레드 안전성 | 배포 타이밍 | 적용 시나리오 |
|------|---------|---------|---------|
| `Fire()` | 예 | 다음 프레임 | 대부분의情况, 특히跨스레드 호출 |
| `FireNow()` | 아니오 | 즉시 | 긴급 이벤트, 동기 콜백 |

```csharp
public class NetworkManager
{
    // 네트워크 콜백은 하위 스레드에서 호출될 수 있으므로 Fire를 사용하여 스레드 안전성 보장
    public void OnReceiveData(byte[] data)
    {
        // 하위 스레드에서 데이터 수신
        var eventComponent = GameEntry.GetComponent<EventComponent>();
        
        // Fire를 사용하여 메인 스레드 다음 프레임에서 처리
        eventComponent.Fire(this, new NetworkDataEventArgs(data));
    }

    // 로컬 동기 이벤트는 FireNow 사용 가능
    public void TriggerImmediateEvent()
    {
        var eventComponent = GameEntry.GetComponent<EventComponent>();
        
        // 즉시 배포, 즉각적인 피드백이 필요한 시나리오에 사용
        eventComponent.FireNow(this, new ImmediateEventArgs());
    }
}
```

> Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L127-L140)

---

### 5. 중복 구독 문제

#### 증상
同一 이벤트 처리 함수가 여러 번 호출되어 로직이 중복 실행됨.

#### 원인 분석
- `Start()`에서 반복적으로 구독 메서드 호출
- 구독 여부를 확인하지 않고再次 구독

#### 해결책

**CheckSubscribe 자동 확인 사용:**

```csharp
private void Start()
{
    var eventComponent = GameEntry.GetComponent<EventComponent>();
    
    // CheckSubscribe는 이미 구독되었는지 확인하여 중복 구독 방지
    eventComponent.CheckSubscribe("score_changed", OnScoreChanged);
}

//旧的 Subscribe 메서드 사용하지 마세요 (obsolete로 표시됨)
// eventComponent.Subscribe("score_changed", OnScoreChanged);  // 비권장
```

> Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L82-L88)

---

### 6. 自定义 이벤트 파라미터 문제

#### 증상
自定义 이벤트 파라미터가 올바르게 배포되거나 수신되지 않음.

#### 원인 분석
- `GameEventArgs`를 올바르게 상속하지 않음
- 필수 `Id` 속성 및 `Clear()` 메서드를 구현하지 않음
- 객체 풀을 사용하여 생명주기를 올바르게 관리하지 않음

#### 해결책

**自定义 이벤트 파라미터 올바르게 생성:**

```csharp
// 1. GameEventArgs 상속
public class PlayerDieEventArgs : GameEventArgs
{
    // 2. Id 속성 구현
    public override string Id => "player_die";
    
    // 3. 이벤트 데이터 추가
    public int PlayerId { get; private set; }
    public string Reason { get; private set; }

    // 4. 정적 Create 메서드를 사용하여 객체 풀 캡슐화
    public static PlayerDieEventArgs Create(int playerId, string reason)
    {
        var args = ReferencePool.Acquire<PlayerDieEventArgs>();
        args.PlayerId = playerId;
        args.Reason = reason;
        return args;
    }

    // 5. Clear 메서드를 구현하여 데이터 Released
    public override void Clear()
    {
        PlayerId = 0;
        Reason = null;
    }
}
```

> Source: [GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs#L16-L18)

> Source: [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs#L18-L25)

---

## 디버깅 기술

### 현재 이벤트 상태 查看

```csharp
// 이벤트 처리 함수 수查看
var eventComponent = GameEntry.GetComponent<EventComponent>();
Debug.Log($"이벤트 처리 함수 수: {eventComponent.EventHandlerCount}");
Debug.Log($"이벤트 타입 수: {eventComponent.EventCount}");

// 특정 이벤트 확인
int handlerCount = eventComponent.Count("game_start");
Debug.Log($"game_start 이벤트 처리 함수 수: {handlerCount}");
```

> Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L27-L38)

---

## 관련 링크

- [플러그인 소개](./01.overview.md)
- [설치 방식](./02.installation.md)
- [빠른 시작](./03.getting-started.md)
- [이벤트 시스템 아키텍처](./04.features.md)
- [EventComponent API 참고](./05.event-component.md)
- [EventManager API 참고](./06.event-manager.md)
- [GameEventArgs API 참고](./07.game-event-args.md)