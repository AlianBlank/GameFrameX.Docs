---
title: "事件收不到或不觸發怎麼辦"
order: 1
autoGenerated:
  source:
    repo: com.gameframex.unity.event
    commit: a5f8cf0edf8e3925da36478adc798c737af4c682
    generator: OpenDeepWiki
---
# 事件收不到或不觸發怎麼辦

訂閱了事件卻收不到回呼，通常是**訂閱與拋出的事件 id 不一致**、**Fire 的「下一幀分發」語意被誤解**，或**場景缺少 `EventComponent`** 所導致。對照下面的診斷表對號入座，再進入對應的修復步驟即可定位問題。

## 快速診斷

| 症狀 | 原因 | 修復 |
|------|------|------|
| 回呼從不執行 | 訂閱的 `id` 與 `Fire` 傳入的事件編號不是同一個字串（拼字、大小寫、一邊用 `typeof(X).FullName` 一邊用手寫字面值） | 統一使用常數或 `typeof(X).FullName`，見[修復步驟 1](#1-核對事件-id-完全一致) |
| 回呼從不執行，但 id 確認一致 | 訂閱發生在 `Fire` 之後，`EventPoolMode.AllowNoHandler` 下事件被靜默丟棄 | 先 `Subscribe` 再 `Fire`，見[修復步驟 2](#2-先訂閱再拋出並自檢訂閱是否成功) |
| 事件在 Fire 當幀收不到，下一幀才到 | `Fire` 執行緒安全但延遲到下一幀分發，屬於設計行為 | 需要立即回呼時改用 `FireNow`，見[修復步驟 3](#3-理解-fire-的下一幀分發語意) |
| 偶發丟失事件 | 拋出事件後同幀銷毀物件 / 遊戲暫停，`EventManager.Update` 未再執行，佇列中的事件未分發 | 避免拋出後立即銷毀訂閱者；或改用 `FireNow` |
| 主控台輸出 `Event manager is invalid.`，所有事件失效 | 場景中缺少 `EventComponent`，`Awake` 中取不到 `IEventManager` 模組 | 在場景掛上 GameFrameX/Event 元件，見[修復步驟 4](#4-確認場景中存在-eventcomponent) |
| 呼叫 `Fire(sender, "")` 或傳入 null 事件編號會直接拋出例外 | `GameFrameworkGuard.NotNullOrEmpty(eventId, nameof(eventId))` 參數驗證 | 傳入非空的事件編號字串 |
| 想輸出日誌排查無人訂閱的事件 | 未設定預設處理函式 | 呼叫 `SetDefaultHandler` 作為後備，見[修復步驟 5](#5-用預設處理函式作為後備排查無人訂閱的事件) |
| 取消訂閱時回報 handler 不存在 | `Unsubscribe` 傳入了未訂閱的 handler | 改用 `CheckUnsubscribe` |

## 修復步驟

### 1. 核對事件 id 完全一致

`Subscribe` 與 `Fire` 匹配的唯一依據是 `string` 型別的事件編號，兩側不一致時事件池不會報錯（`EventManager` 以 `EventPoolMode.AllowNoHandler | EventPoolMode.AllowMultiHandler` 模式建立），事件會被靜默丟棄：

```csharp
m_EventPool = new EventPool<GameEventArgs>(EventPoolMode.AllowNoHandler | EventPoolMode.AllowMultiHandler);
```

Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L46-L49)

建議做法：將事件編號收斂為同一份常數或 `typeof(X).FullName`，訂閱與拋出兩側引用同一來源。使用無參數事件時，`Fire(sender, eventId)` 內部會建立 `EmptyEventArgs` 並寫入該編號：

```csharp
public void Fire(object sender, string eventId)
{
    GameFrameworkGuard.NotNullOrEmpty(eventId, nameof(eventId));
    m_EventManager.Fire(sender, EmptyEventArgs.Create(eventId));
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L157-L161)

```csharp
public static EmptyEventArgs Create(string eventId)
{
    var eventArgs = ReferencePool.Acquire<EmptyEventArgs>();
    eventArgs.SetEventId(eventId);
    return eventArgs;
}
```

Source: [EmptyEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/EmptyEventArgs.cs#L33-L38)

請注意 `EmptyEventArgs` 回收時會將編號重設為 `typeof(EmptyEventArgs).FullName`，不要依賴事件參數物件在分發後仍保留自訂編號。

### 2. 先訂閱再拋出，並自檢訂閱是否成功

事件必須先訂閱後拋出。懷疑訂閱流程有問題時，可用 `Count(id)` / `Check(id, handler)` 直接確認事件池目前狀態：

```csharp
public int Count(string id)
{
    return m_EventManager.Count(id);
}

public bool Check(string id, EventHandler<GameEventArgs> handler)
{
    return m_EventManager.Check(id, handler);
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L84-L98)

重複初始化容易造成同一 handler 被訂閱多次（`AllowMultiHandler` 允許重複訂閱，會重複回呼）。框架提供了防重複版本：

```csharp
public void CheckSubscribe(string id, EventHandler<GameEventArgs> handler)
{
    if (!Check(id, handler))
    {
        m_EventManager.Subscribe(id, handler);
    }
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L105-L111)

對稱地，取消訂閱不存在的 handler 時可用 `CheckUnsubscribe`，它會在 handler 不存在時直接跳過：

```csharp
public void CheckUnsubscribe(string id, EventHandler<GameEventArgs> handler)
{
    m_EventManager.CheckUnsubscribe(id, handler);
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L128-L131)

### 3. 理解 Fire 的「下一幀」分發語意

`Fire` 是執行緒安全的，但事件進入佇列後會在 `EventManager.Update` 中於下一幀分發；`FireNow` 立即分發但非執行緒安全：

```csharp
/// 拋出事件，這個操作是執行緒安全的，即使不在主執行緒中拋出，也可保證在主執行緒中回呼事件處理函式，但事件會在拋出後的下一幀分發。
public void Fire(object sender, GameEventArgs e)
{
    m_EventPool.Fire(sender, e);
}

/// 拋出事件立即模式，這個操作不是執行緒安全的，事件會立刻分發。
public void FireNow(object sender, GameEventArgs e)
{
    m_EventPool.FireNow(sender, e);
}
```

Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L158-L175)

佇列中的事件依賴每幀的輪詢被消費：

```csharp
protected override void Update(float elapseSeconds, float realElapseSeconds)
{
    m_EventPool.Update(elapseSeconds, realElapseSeconds);
}
```

Source: [EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs#L81-L84)

由此可推出兩個排查重點：
1. 在 `Fire` 之後當幀就斷言回呼已執行會失敗——等下一幀或改用 `FireNow`。
2. 拋出事件後立刻銷毀訂閱物件、或遊戲暫停導致 `Update` 停止，佇列中的事件可能永遠不分發。

### 4. 確認場景中存在 EventComponent

`EventComponent` 在 `Awake` 中透過 `GameFrameworkEntry.GetModule<IEventManager>()` 取得模組，失敗會輸出 `Log.Fatal` 並直接傳回，之後所有事件 API 均不可用：

```csharp
protected override void Awake()
{
    ImplementationComponentType = Utility.Assembly.GetType(componentType);
    InterfaceComponentType = typeof(IEventManager);
    base.Awake();
    m_EventManager = GameFrameworkEntry.GetModule<IEventManager>();
    if (m_EventManager == null)
    {
        Log.Fatal("Event manager is invalid.");
        return;
    }
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L66-L77)

修復：在場景中新增 `GameFrameX/Event` 元件（`[AddComponentMenu("GameFrameX/Event")]`），並確認初始化順序滿足模組優先權（`EventManager.Priority` 為 7）。

### 5. 用預設處理函式作為後備排查無人訂閱的事件

設定預設處理函式後，所有沒有訂閱者的事件都會走到該回呼，適合在排查階段輸出「事件被拋出但無人處理」：

```csharp
public void SetDefaultHandler(EventHandler<GameEventArgs> handler)
{
    m_EventManager.SetDefaultHandler(handler);
}
```

Source: [EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs#L137-L140)

## 驗證

1. 訂閱一個只輸出日誌的 handler，記錄 `Count(eventId)`，期望傳回值 ≥ 1。
2. `Fire(sender, eventId)` 後等待下一幀（或直接 `FireNow`），確認日誌輸出。
3. `Check(eventId, handler)` 應傳回 `true`；執行 `CheckUnsubscribe` 後 `Count(eventId)` 應回到 0，且再次 `Fire` 時事件進入預設處理函式（若已設定）。
4. 在編輯器中選取掛有 `EventComponent` 的物件，可透過 Inspector 觀察事件與處理函式數量（`EventHandlerCount` / `EventCount`）。

## 相關連結

- 事件訂閱與拋出 API 速查：見《事件系統參考》
- 事件系統整體介紹與快速開始：見《事件系統概述》
- 原始碼入口：[EventComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventComponent.cs)、[EventManager.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/Event/EventManager.cs)、[GameEventArgs.cs](https://github.com/GameFrameX/com.gameframex.unity.event/blob/main/Runtime/EventArgs/GameEventArgs.cs#L34-L40)