---
title: "WebGL 與跨平台使用指南"
order: 4
autoGenerated:
  source:
    repo: com.gameframex.unity.web.protobuff
    commit: 161731e79e845768c916c7e8d385fa16c989d605
    generator: OpenDeepWiki
---
# WebGL 與跨平台使用指南

本頁說明如何在 WebGL 與其他平台(編輯器、Windows、行動裝置等)使用 Web ProtoBuff 元件傳送 ProtoBuf HTTP 請求：呼叫方使用同一套 `Post<T>` API,元件內部透過 `#if UNITY_WEBGL` 自動切換 `UnityWebRequest` 與 `HttpWebRequest`,無需為不同平台撰寫兩份程式碼。

## 前置條件

- 已安裝 `com.gameframex.unity.web.protobuff` 套件，及其相依性(GameFrameX、`GameFrameX.Network`、ProtoBuf 執行階段)。
- 請求與回應的訊息類別繼承自 `MessageObject`,回應類別額外實作 `IResponseMessage`。
- WebGL 建置使用 IL2CPP;套件內附 [GameFrameXWebProtoBuffCroppingHelper.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/GameFrameXWebProtoBuffCroppingHelper.cs) 參與建置裁剪處理。

## 操作步驟

1. **確認目標平台的巨集生效**。元件只在 `UNITY_WEBGL` 巨集定義時走 UnityWebRequest 路徑，切換到 WebGL 平台(File → Build Settings → Switch Platform)後巨集自動生效。

   ```csharp
   using System;
   using System.Collections.Generic;
   using System.Text;
   using GameFrameX.Runtime;
   #if UNITY_WEBGL
   using UnityEngine.Networking;
   #endif
   ```

   Source: [WebProtoBuffManager.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.cs#L1-L7)

2. **按平台需要調整逾時與並行上限**。建構時預設 `Timeout = 5`(秒)、`MaxConnectionPerServer = 8`;修改 `Timeout` 會同步更新 `RequestTimeout`。

   ```csharp
   [UnityEngine.Scripting.Preserve]
   public WebProtoBuffManager()
   {
       MaxConnectionPerServer = 8;
       Timeout = 5f;
   }

   public float Timeout
   {
       get { return m_Timeout; }
       set
       {
           m_Timeout = value;
           RequestTimeout = TimeSpan.FromSeconds(value);
       }
   }

   public int MaxConnectionPerServer { get; set; }

   public TimeSpan RequestTimeout { get; set; }
   ```

   Source: [WebProtoBuffManager.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.cs#L27-L54)

3. **直接呼叫 `Post<T>` 傳送請求**。這是跨平台統一入口，回應主體會按訊息 Id 驗證型別後反序列化為 `T`。

   ```csharp
   public async Task<T> Post<T>(string url, MessageObject message) where T : MessageObject, IResponseMessage
   {
       DebugSendLog(message);
       var webBufferResult = await PostInner(url, message);
       if (webBufferResult.IsNotNull())
       {
           var messageObjectHttp = SerializerHelper.Deserialize<MessageHttpObject>(webBufferResult.Result);
           if (messageObjectHttp.IsNotNull() && messageObjectHttp.Id != default)
           {
               var messageType = ProtoMessageIdHandler.GetRespTypeById(messageObjectHttp.Id);
               if (messageType != typeof(T))
               {
                   Log.Error($"Response message type is invalid. Expected '{typeof(T).FullName}', actual '{messageType.FullName}'.");
                   return default;
               }

               var messageObject = SerializerHelper.Deserialize<T>(messageObjectHttp.Body);
               DebugReceiveLog(messageObject);
               return messageObject;
           }
       }

       return default;
   }
   ```

   Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L182-L205)

   自訂範例(保持識別項與原始碼一致):

   ```csharp
   // message 為請求訊息物件;LoginResponse : MessageObject, IResponseMessage
   var response = await webProtoBuffManager.Post<LoginResponse>(url, message);
   ```

4. **無需額外處理 WebGL 差異**。請求進入佇列後，`UpdateProtoBuf` 每幀在並行上限內出隊並呼叫 `MakeProtoBufBytesRequest`,該方法內部按平台編譯：

   ```csharp
   void UpdateProtoBuf(float elapseSeconds, float realElapseSeconds)
   {
       lock (m_StringBuilder)
       {
           if (m_SendingProtoBufList.Count < MaxConnectionPerServer)
           {
               if (m_WaitingProtoBufQueue.Count > 0)
               {
                   var webProtoBufData = m_WaitingProtoBufQueue.Dequeue();

                   MakeProtoBufBytesRequest(webProtoBufData);

                   m_SendingProtoBufList.Add(webProtoBufData);
               }
           }
       }
   }
   ```

   Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L39-L55)

## 常見變體

- **WebGL 平台**：使用 `UnityEngine.Networking.UnityWebRequest` 發起請求，`Content-Type` 為 `application/x-protobuf`,逾時取 `RequestTimeout.TotalSeconds`(秒)，透過 `completed` 回呼驅動結果；注意 `timeout` 屬性是 `int`,因此 `Timeout` 建議設為整數秒，避免被截斷。

  ```csharp
  #if UNITY_WEBGL
              UnityEngine.Networking.UnityWebRequest unityWebRequest;
              if (webData.IsGet)
              {
                  unityWebRequest = UnityEngine.Networking.UnityWebRequest.Get(webData.URL);
              }
              else
              {
                  unityWebRequest = UnityEngine.Networking.UnityWebRequest.Post(webData.URL, string.Empty);
              }

              unityWebRequest.timeout = (int)RequestTimeout.TotalSeconds;
              unityWebRequest.SetRequestHeader("Content-Type", ProtoBufContentType);
              byte[] postData = webData.SendData;
              unityWebRequest.uploadHandler = new UnityEngine.Networking.UploadHandlerRaw(postData);

              var asyncOperation = unityWebRequest.SendWebRequest();
              asyncOperation.completed += (asyncOperation2) =>
              {
                  try
                  {
                      m_SendingProtoBufList.Remove(webData);
                      if (unityWebRequest.isNetworkError || unityWebRequest.isHttpError || unityWebRequest.error != null)
                      {
                          webData.Task.TrySetException(new Exception(unityWebRequest.error));
                          return;
                      }

                      webData.Task.SetResult(new WebBufferResult(webData.UserData, unityWebRequest.downloadHandler.data));
                  }
                  finally
                  {
                      unityWebRequest.Dispose();
                      ReferencePool.Release(webData);
                  }
              };
  #else
  ```

  Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L83-L119)

- **非 WebGL 平台**(編輯器、桌面、行動裝置)：走 `HttpWebRequest` 非同步串流實作，逾時單位是毫秒(`RequestTimeout.TotalMilliseconds`),並把 `WebExceptionStatus.Timeout` 轉為 `TimeoutException` 擲給上層：

  ```csharp
                  HttpWebRequest request = WebRequest.CreateHttp(webData.URL);
                  request.Method = webData.IsGet ? WebRequestMethods.Http.Get : WebRequestMethods.Http.Post;
                  request.Timeout = (int)RequestTimeout.TotalMilliseconds; // 設定請求逾時時間
                  request.ContentType = ProtoBufContentType;
  ```

  ```csharp
              catch (WebException e)
              {
                  // 攔截逾時例外
                  if (e.Status == WebExceptionStatus.Timeout)
                  {
                      webData.Task.SetException(new TimeoutException(e.Message));
                      return;
                  }

                  webData.Task.SetException(e);
              }
  ```

  Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L120-L155)

- **請求主體封裝**：對外只傳 `MessageObject`;`PostInner` 會自動包一層 `MessageHttpObject`(Id、UniqueId、Body)並序列化為二進位後再入隊：

  ```csharp
           var id = ProtoMessageIdHandler.GetReqMessageIdByType(message.GetType());
           MessageHttpObject messageHttpObject = new MessageHttpObject
           {
               Id = id,
               UniqueId = message.UniqueId,
               Body = SerializerHelper.Serialize(message),
           };
           var sendData = SerializerHelper.Serialize(messageHttpObject);
           var webData = WebProtoBufData.Create(url, sendData, uniTaskCompletionSource, userData);
           m_WaitingProtoBufQueue.Enqueue(webData);
  ```

  Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L214-L229)

## 常見錯誤

| 症狀 | 原因 | 修復 |
|------|------|------|
| `Response message type is invalid. Expected '...', actual '...'` 日誌且回傳 `default` | 伺服器回傳的訊息 Id 對應的回應型別與呼叫方傳入的 `T` 不一致 | 核對 `ProtoMessageIdHandler` 註冊的請求/回應對應與 `Post<T>` 的泛型參數 |
| WebGL 下請求無回應 | 目標 `Timeout` 含小數，被 `(int)RequestTimeout.TotalSeconds` 截斷為過小值(如 `0.5` → `0`) | 將 `Timeout` 設為整數秒 |
| 請求一直懸置 | 並行數已達 `MaxConnectionPerServer`(預設 8),後續請求滯留在 `m_WaitingProtoBufQueue` | 提高 `MaxConnectionPerServer`,或確保元件 `Update` 持續被驅動 |
| WebGL 主控台回報 `Unknown error` / CORS 類網路錯誤 | `unityWebRequest.isNetworkError || unityWebRequest.isHttpError || unityWebRequest.error != null` 分支觸發，例外文字即 `unityWebRequest.error` | 檢查伺服器 CORS 設定與 URL 可達性 |
| 非 WebGL 平台擲出 `TimeoutException` | 伺服器未在 `RequestTimeout`(毫秒精確度)內回應 | 調大 `Timeout` 或排查伺服器耗時 |

## 驗證

- 在 WebGL 與編輯器兩個平台分別傳送同一 `Post<T>` 請求，確認均能回傳反序列化後的 `T` 執行個體且 `UniqueId` 一致。
- 開啟 `ENABLE_GAMEFRAMEX_WEB_RECEIVE_LOG` 巨集後，可看到 `接收消息 ID:[...] 消息内容:...` 偵錯日誌(Source: [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs#L231-L237))。
- 故意把 `T` 換成錯誤型別，確認出現 `Response message type is invalid...` 報錯，證明型別驗證生效。

## 背景

元件按平台條件編譯雙實作：WebGL 因執行階段限制不能使用 `HttpWebRequest`,統一改走 `UnityWebRequest` 的 `completed` 回呼；其餘平台保留基於 `Task` 的串流 `HttpWebRequest` 實作。兩者結果都寫回同一個 `TaskCompletionSource<WebBufferResult>`,因此上層 `Post<T>` 無感知差異。URL 查詢參數、重複使用的 `StringBuilder` 與並行上限在 [WebProtoBuffManager.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.cs) 中定義。

## 相關連結

- [WebProtoBuffManager.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.cs) — 逾時、並行與 URL 處理
- [WebProtoBuffManager.ProtoBuf.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffManager.ProtoBuf.cs) — 跨平台請求實作與 `Post<T>` 入口
- [IWebProtoBuffManager.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/IWebProtoBuffManager.cs) — 元件介面
- [WebProtoBuffComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/Runtime/Web/WebProtoBuffComponent.cs) — Unity 元件封裝
- [README.zh-CN.md](https://github.com/GameFrameX/com.gameframex.unity.web.protobuff/blob/main/README.zh-CN.md) — 套件總覽