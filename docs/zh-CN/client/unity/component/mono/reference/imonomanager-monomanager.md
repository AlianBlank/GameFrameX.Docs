---
title: "IMonoManager / MonoManager 接口速查"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.mono
    commit: 97cf18c5e3948f45682b584e853c69a2a57bbebf
    generator: OpenDeepWiki
---
# IMonoManager / MonoManager 接口速查

本页收录 `IMonoManager` 接口与 `MonoManager` 实现类的成员签名、调用语义、异常与默认值,供快速查询。

## IMonoManager 接口成员

| 成员 | 签名 | 说明 |
|------|------|------|
| `FixedUpdate()` | `void FixedUpdate()` | 在固定的帧率下调用。 |
| `Update(float elapseSeconds, float realElapseSeconds)` | `void Update(float, float)` | 每帧调用,参数为流逝时间与真实流逝时间。 |
| `LateUpdate()` | `void LateUpdate()` | 在所有 `Update` 函数调用后每帧调用。 |
| `OnDestroy()` | `void OnDestroy()` | 当 MonoBehaviour 将被销毁时调用。 |
| `OnApplicationFocus(bool focusStatus)` | `void OnApplicationFocus(bool)` | 应用程序失去或获得焦点时调用。 |
| `OnApplicationPause(bool pauseStatus)` | `void OnApplicationPause(bool)` | 应用程序暂停或恢复时调用。 |
| `AddLateUpdateListener(Action<float, float>)` | `void AddLateUpdateListener(Action<float, float>)` | 添加一个在 `LateUpdate` 期间调用的监听器。 |
| `RemoveLateUpdateListener(Action<float, float>)` | `void RemoveLateUpdateListener(Action<float, float>)` | 从 `LateUpdate` 中移除一个监听器。 |
| `AddFixedUpdateListener(Action<float, float>)` | `void AddFixedUpdateListener(Action<float, float>)` | 添加一个在 `FixedUpdate` 期间调用的监听器。 |
| `RemoveFixedUpdateListener(Action<float, float>)` | `void RemoveFixedUpdateListener(Action<float, float>)` | 从 `FixedUpdate` 中移除一个监听器。 |
| `AddUpdateListener(Action<float, float>)` | `void AddUpdateListener(Action<float, float>)` | 添加一个在 `Update` 期间调用的监听器。 |
| `RemoveUpdateListener(Action<float, float>)` | `void RemoveUpdateListener(Action<float, float>)` | 从 `Update` 中移除一个监听器。 |
| `AddDestroyListener(Action)` | `void AddDestroyListener(Action)` | 添加一个在 `Destroy` 期间调用的监听器。 |
| `RemoveDestroyListener(Action)` | `void RemoveDestroyListener(Action)` | 从 `Destroy` 中移除一个监听器。 |
| `AddOnApplicationPauseListener(Action<bool>)` | `void AddOnApplicationPauseListener(Action<bool>)` | 添加一个在 `OnApplicationPause` 期间调用的监听器。 |
| `RemoveOnApplicationPauseListener(Action<bool>)` | `void RemoveOnApplicationPauseListener(Action<bool>)` | 从 `OnApplicationPause` 中移除一个监听器。 |
| `AddOnApplicationFocusListener(Action<bool>)` | `void AddOnApplicationFocusListener(Action<bool>)` | 添加一个在 `OnApplicationFocus` 期间调用的监听器。 |
| `RemoveOnApplicationFocusListener(Action<bool>)` | `void RemoveOnApplicationFocusListener(Action<bool>)` | 从 `OnApplicationFocus` 中移除一个监听器。 |

接口源码: [IMonoManager.cs](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/IMonoManager.cs#L36-L146)

## MonoManager 类成员

`MonoManager` 是 `IMonoManager` 的实现,继承自 `GameFrameworkModule`,命名空间 `GameFrameX.Mono.Runtime`。

| 成员 | 类型 | 说明 |
|------|------|------|
| 类签名 | `public sealed class MonoManager : GameFrameworkModule, IMonoManager` | 标记 `[UnityEngine.Scripting.Preserve]` 以避免被代码裁剪。 |
| `FixedUpdate()` | `public void` | 调用 `_fixedUpdate` 队列,使用 `Time.fixedDeltaTime` 与 `Time.fixedUnscaledDeltaTime`,外层包装 `Profiler.BeginSample("FixedUpdate")`。 |
| `LateUpdate()` | `public void` | 调用 `_lateUpdate` 队列,使用 `Time.deltaTime` 与 `Time.unscaledDeltaTime`。 |
| `OnDestroy()` | `public void` | 调用 `_destroy` 队列。 |
| `OnApplicationFocus(bool focusStatus)` | `public void` | 调用 `_onApplicationFocus` 队列。 |
| `OnApplicationPause(bool pauseStatus)` | `public void` | 调用 `_onApplicationPause` 队列。 |

实现源码: [MonoManager.cs](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/MonoManager.cs#L43-L57)

## 监听器管理(AddXxxListener / RemoveXxxListener)

所有 `Add*` / `Remove*` 方法具备一致的契约:

- 入参为 `null` 时,抛出 `ArgumentNullException(nameof(action))`。
- 修改内部 `LinkedList` 时统一在 `private static readonly object Lock` 上加锁。
- `Add*` 使用 `AddLast` 追加到链表末尾;`Remove*` 使用 `LinkedList.Remove(action)`。

| 方法 | 链表 | 回调签名 |
|------|------|----------|
| `AddLateUpdateListener` / `RemoveLateUpdateListener` | `_lateUpdate` | `Action<float, float>`(elapseSeconds, fixedElapseSeconds) |
| `AddFixedUpdateListener` / `RemoveFixedUpdateListener` | `_fixedUpdate` | `Action<float, float>`(elapseSeconds, fixedElapseSeconds) |
| `AddUpdateListener` / `RemoveUpdateListener` | `_updateQueue` | `Action<float, float>`(elapseSeconds, realElapseSeconds) |
| `AddDestroyListener` / `RemoveDestroyListener` | `_destroy` | `Action` |
| `AddOnApplicationPauseListener` / `RemoveOnApplicationPauseListener` | `_onApplicationPause` | `Action<bool>` |
| `AddOnApplicationFocusListener` / `RemoveOnApplicationFocusListener` | `_onApplicationFocus` | `Action<bool>` |

实现参考: [MonoManager.cs#L108-L306](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/MonoManager.cs#L108-L306)

## 调用与异常速查

| 行为 | 来源 | 说明 |
|------|------|------|
| 入参校验 | `MonoManager.Add*` / `Remove*` | `action == null` 时抛出 `ArgumentNullException`。 |
| 线程安全 | `private static readonly object Lock` | 所有读写链表的操作均加锁。 |
| 队列派发 | `QueueInvoking` 私有静态方法族 | 在锁内拷贝快照到数组,避免遍历时被并发修改;外层包裹 `Profiler.BeginSample`。 |
| 单个监听器异常隔离 | `QueueInvoking` 中的 `try / catch (Exception e)` | 捕获后调用 `Log.Error(e)`,不影响后续监听器调用。 |

实现参考: [MonoManager.cs#L308-L356](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/MonoManager.cs#L308-L356)

## 相关链接

- 接口源文件: [IMonoManager.cs](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/IMonoManager.cs)
- 实现源文件: [MonoManager.cs](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/MonoManager.cs)
- 相关类型: [MonoComponent.cs](https://github.com/GameFrameX/com.gameframex.unity.mono/blob/main/Runtime/Mono/MonoComponent.cs)