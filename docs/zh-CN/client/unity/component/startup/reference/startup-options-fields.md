---
title: "StartupOptions 配置字段参考"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.startup
    commit: 9823e4a6e6fa554c76d3b88e46a90357a7c58184
    generator: OpenDeepWiki
---
# StartupOptions 配置字段参考

`StartupOptions` 是启动流程的 ScriptableObject 配置资产，承载所有可变的运行参数：资源模式、URL 主备列表、热更入口、HTTP 公共参数、UI 资源路径等。运行时通过此 SO 注入配置，启动流程按字段值初始化各阶段。

## 字段速查表

| 字段名 | 类型 | 默认值 | 说明 |
|--------|------|--------|------|
| `GameFrameXApiKey` | `string` | `""` | GameFrameX 管理后台的租户 ID |
| `GameFrameXTenantSecret` | `string` | `""` | GameFrameX 管理后台的租户密钥 |
| `GameFrameXAppId` | `string` | `""` | GameFrameX 管理后台的应用 ID |
| `GameFrameXAppSecret` | `string` | `""` | GameFrameX 管理后台的应用密钥 |
| `GamePlayMode` | `EPlayMode` | `EditorSimulateMode` | 资源运行模式（YooAsset），启动前同步到 Asset 组件 |
| `GlobalInfoUrls` | `string[]` | 空数组 | 全局信息接口 URL 主备列表，按顺序尝试；空数组视为配置错误 |
| `MaxAttemptsPerUrl` | `int` | `3` | 每个 URL 内部的总尝试次数上限（含初次） |
| `RetryDelayMs` | `int` | `3000` | 重试之间的延迟毫秒数 |
| `SkipRemoteStartupRequests` | `bool` | `false` | 跳过远程启动请求（全局信息、App 版本、资源包版本） |
| `HotfixAssemblyName` | `string` | `"Unity.Hotfix"` | Hotfix 程序集名，传给 `IHotfixLauncher` |
| `HotfixEntryTypeName` | `string` | `"Hotfix.HotfixLauncher"` | Hotfix 入口类型全名 |
| `HotfixEntryMethodName` | `string` | `"Main"` | Hotfix 入口方法名 |
| `PackageName` | `string` | `""` | HTTP 公共参数中的应用包名（如 `com.company.game`） |
| `Channel` | `string` | `""` | HTTP 公共参数中的渠道标识 |
| `SubChannel` | `string` | `""` | HTTP 公共参数中的子渠道标识 |
| `LauncherUIResName` | `string` | `"UI/UILauncher"` | 启动 UI 的资源路径，传给 `IStartupUIHandler` |

字段均通过 C# 特性标注 Header 与 Tooltip，分组如下：

- **Admin**：管理后台鉴权四元组。
- **Asset**：资源运行模式。
- **Network**：URL 主备列表与重试策略。
- **Hotfix**：热更入口三件套（程序集 / 类型 / 方法）。
- **HTTP Base Params**：HTTP 公共参数。
- **UI**：启动 UI 资源路径。

## 字段定义

来源：[StartupOptions.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Startup/StartupOptions.cs#L1-L52)

```csharp
using UnityEngine;
using YooAsset;

namespace GameFrameX.Startup.Runtime
{
    [CreateAssetMenu(menuName = "GameFrameX/Startup Options", fileName = "StartupOptions")]
    public sealed class StartupOptions : ScriptableObject
    {
        [Header("Admin")] [Tooltip("GameFrameX 管理后台的租户ID")]
        public string GameFrameXApiKey = "";

        [Tooltip("GameFrameX 管理后台的租户密钥")] public string GameFrameXTenantSecret = "";
        [Tooltip("GameFrameX 管理后台的应用ID")] public string GameFrameXAppId = "";
        [Tooltip("GameFrameX 管理后台的应用密钥")] public string GameFrameXAppSecret = "";

        [Header("Asset")] [Tooltip("资源运行模式。启动流程会在加载资源前同步到 Asset 组件。")]
        public EPlayMode GamePlayMode = EPlayMode.EditorSimulateMode;

        [Header("Network")] [Tooltip("全局信息接口 URL 主备列表，按顺序尝试。空数组视为配置错误。")]
        public string[] GlobalInfoUrls = new string[0];

        [Tooltip("每个 URL 内部的总尝试次数上限（含初次）。")] public int MaxAttemptsPerUrl = 3;
        [Tooltip("重试之间的延迟毫秒数。")] public int RetryDelayMs = 3000;

        [Tooltip("是否跳过远程启动请求（全局信息、App 版本、资源包版本）。WebGL 单机等无后端场景启用，直接进入本地资源初始化。")]
        public bool SkipRemoteStartupRequests = false;

        [Header("Hotfix")] [Tooltip("Hotfix 程序集名，传给 IHotfixLauncher 使用。")]
        public string HotfixAssemblyName = "Unity.Hotfix";
        [Tooltip("Hotfix 入口类型全名。")] public string HotfixEntryTypeName = "Hotfix.HotfixLauncher";
        [Tooltip("Hotfix 入口方法名。")] public string HotfixEntryMethodName = "Main";

        [Header("HTTP Base Params")] [Tooltip("HTTP 公共参数中的应用包名（如 com.company.game）。运行时覆盖默认值。")]
        public string PackageName = string.Empty;
        [Tooltip("HTTP 公共参数中的渠道标识。运行时覆盖默认值。")] public string Channel = string.Empty;
        [Tooltip("HTTP 公共参数中的子渠道标识。运行时覆盖默认值。")]
        public string SubChannel = string.Empty;

        [Header("UI")]
        [Tooltip("启动 UI 的资源路径，传给 IStartupUIHandler。")]
        public string LauncherUIResName = "UI/UILauncher";
    }
}
```

## 创建与编辑

通过菜单 `Assets / Create / GameFrameX/Startup Options` 即可生成 `StartupOptions.asset`。Inspector 提供 `GlobalInfoUrls` 的可拖拽排序列表，其余字段使用默认 PropertyField 绘制，每个字段下方显示 Tooltip 说明。

来源：[StartupOptionsInspector.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Editor/StartupOptionsInspector.cs#L1-L54)

```csharp
[CustomEditor(typeof(StartupOptions))]
public sealed class StartupOptionsInspector : UnityEditor.Editor
{
    private ReorderableList _urlList;
    private SerializedProperty _urlListProperty;

    private void OnEnable()
    {
        _urlListProperty = serializedObject.FindProperty(nameof(StartupOptions.GlobalInfoUrls));
        _urlList = new ReorderableList(serializedObject, _urlListProperty, true, true, true, true)
        {
            drawHeaderCallback = rect => EditorGUI.LabelField(rect, "Global Info URLs (Primary-Backup Order)"),
            drawElementCallback = (rect, index, isActive, isFocused) =>
            {
                var element = _urlListProperty.GetArrayElementAtIndex(index);
                EditorGUI.PropertyField(rect, element, GUIContent.none);
            }
        };
    }
    // ...
}
```

## 字段使用约束

| 约束 | 涉及字段 | 影响 |
|------|----------|------|
| `GlobalInfoUrls` 留空会被视为配置错误 | `GlobalInfoUrls` | 启动流程直接走失败路径，不会尝试请求 |
| `MaxAttemptsPerUrl` 包含初次请求 | `MaxAttemptsPerUrl` | 值为 1 表示只尝试一次 |
| `SkipRemoteStartupRequests` 为 true 时跳过所有远程接口 | 该字段 | WebGL 单机或纯本地资源场景使用，跳过全局信息、App 版本、资源包版本 |
| `HotfixAssemblyName` / `HotfixEntryTypeName` / `HotfixEntryMethodName` 必须与程序集内真实符号一致 | Hotfix 三件套 | 任意一个写错会导致 `IHotfixLauncher` 反射失败 |

## 相关链接

- [StartupOptions.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Startup/StartupOptions.cs)
- [StartupOptionsInspector.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Editor/StartupOptionsInspector.cs)
- [StartupOptionsTests.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Tests/Runtime/StartupOptionsTests.cs)
- [UrlFailoverRunner.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Failover/UrlFailoverRunner.cs)（`GlobalInfoUrls` 主备与重试消费者）
- [DefaultStartupHttpParamsProvider.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Http/DefaultStartupHttpParamsProvider.cs)（HTTP 公共参数消费者）
- [IHotfixLauncher.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Hotfix/IHotfixLauncher.cs)（Hotfix 入口消费者）