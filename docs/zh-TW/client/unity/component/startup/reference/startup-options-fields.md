---
title: "StartupOptions 設定欄位參考"
order: 2
autoGenerated:
  source:
    repo: com.gameframex.unity.startup
    commit: 9823e4a6e6fa554c76d3b88e46a90357a7c58184
    generator: OpenDeepWiki
---
# StartupOptions 設定欄位參考

`StartupOptions` 是啟動流程的 ScriptableObject 設定資產，包含資源模式、URL 主/備清單、Hotfix 進入點、HTTP 公用參數、UI 資源路徑等所有可變的執行時期參數。執行時期會透過此 SO 注入設定，啟動流程依據欄位值初始化各個步驟。

## 欄位快速參考表

| 欄位名稱 | 類型 | 預設值 | 說明 |
|--------|------|--------|------|
| `GameFrameXApiKey` | `string` | `""` | GameFrameX 管理後台的租戶 ID |
| `GameFrameXTenantSecret` | `string` | `""` | GameFrameX 管理後台的租戶金鑰 |
| `GameFrameXAppId` | `string` | `""` | GameFrameX 管理後台的應用程式 ID |
| `GameFrameXAppSecret` | `string` | `""` | GameFrameX 管理後台的應用程式金鑰 |
| `GamePlayMode` | `EPlayMode` | `EditorSimulateMode` | 資源執行模式（YooAsset），啟動前會同步至 Asset 元件 |
| `GlobalInfoUrls` | `string[]` | 空陣列 | 全域資訊介面 URL 主/備清單，依序嘗試；空陣列視為設定錯誤 |
| `MaxAttemptsPerUrl` | `int` | `3` | 每個 URL 內部的總嘗試次數上限（含首次嘗試） |
| `RetryDelayMs` | `int` | `3000` | 重試之間的延遲時間（毫秒） |
| `SkipRemoteStartupRequests` | `bool` | `false` | 跳過遠端啟動請求（全域資訊、App 版本、資源包版本） |
| `HotfixAssemblyName` | `string` | `"Unity.Hotfix"` | Hotfix 組件名稱，傳遞給 `IHotfixLauncher` |
| `HotfixEntryTypeName` | `string` | `"Hotfix.HotfixLauncher"` | Hotfix 進入類型的完整名稱 |
| `HotfixEntryMethodName` | `string` | `"Main"` | Hotfix 進入方法名稱 |
| `PackageName` | `string` | `""` | HTTP 公用參數中的應用程式套件名稱（例如 `com.company.game`） |
| `Channel` | `string` | `""` | HTTP 公用參數中的渠道識別碼 |
| `SubChannel` | `string` | `""` | HTTP 公用參數中的子渠道識別碼 |
| `LauncherUIResName` | `string` | `"UI/UILauncher"` | 啟動 UI 的資源路徑，傳遞給 `IStartupUIHandler` |

所有欄位皆以 C# Attribute 指定了 Header 與 Tooltip，分組如下：

- **Admin**：管理後台驗證四元組。
- **Asset**：資源執行模式。
- **Network**：URL 主/備清單與重試策略。
- **Hotfix**：Hotfix 進入三件套（組件 / 類型 / 方法）。
- **HTTP Base Params**：HTTP 公用參數。
- **UI**：啟動 UI 資源路徑。

## 欄位定義

來源：[StartupOptions.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Startup/StartupOptions.cs#L1-L52)

```csharp
using UnityEngine;
using YooAsset;

namespace GameFrameX.Startup.Runtime
{
    [CreateAssetMenu(menuName = "GameFrameX/Startup Options", fileName = "StartupOptions")]
    public sealed class StartupOptions : ScriptableObject
    {
        [Header("Admin")] [Tooltip("GameFrameX 管理后台的租户ID")]
        public string GameFrameXApiKey = "";

        [Tooltip("GameFrameX 管理后台的租户密钥")] public string GameFrameXTenantSecret = "";
        [Tooltip("GameFrameX 管理后台的应用ID")] public string GameFrameXAppId = "";
        [Tooltip("GameFrameX 管理后台的应用密钥")] public string GameFrameXAppSecret = "";

        [Header("Asset")] [Tooltip("资源运行模式。启动流程会在加载资源前同步到 Asset 组件。")]
        public EPlayMode GamePlayMode = EPlayMode.EditorSimulateMode;

        [Header("Network")] [Tooltip("全局信息接口 URL 主备列表，按顺序尝试。空数组视为配置错误。")]
        public string[] GlobalInfoUrls = new string[0];

        [Tooltip("每个 URL 内部的总尝试次数上限（含初次）。")] public int MaxAttemptsPerUrl = 3;
        [Tooltip("重试之间的延迟毫秒数。")] public int RetryDelayMs = 3000;

        [Tooltip("是否跳过远程启动请求（全局信息、App 版本、资源包版本）。WebGL 单机等无后端场景启用，直接进入本地资源初始化。")]
        public bool SkipRemoteStartupRequests = false;

        [Header("Hotfix")] [Tooltip("Hotfix 程序集名，传给 IHotfixLauncher 使用。")]
        public string HotfixAssemblyName = "Unity.Hotfix";
        [Tooltip("Hotfix 入口类型全名。")] public string HotfixEntryTypeName = "Hotfix.HotfixLauncher";
        [Tooltip("Hotfix 入口方法名。")] public string HotfixEntryMethodName = "Main";

        [Header("HTTP Base Params")] [Tooltip("HTTP 公共参数中的应用包名（如 com.company.game）。运行时覆盖默认值。")]
        public string PackageName = string.Empty;
        [Tooltip("HTTP 公共参数中的渠道标识。运行时覆盖默认值。")] public string Channel = string.Empty;
        [Tooltip("HTTP 公共参数中的子渠道标识。运行时覆盖默认值。")]
        public string SubChannel = string.Empty;

        [Header("UI")]
        [Tooltip("启动 UI 的资源路径，传给 IStartupUIHandler。")]
        public string LauncherUIResName = "UI/UILauncher";
    }
}
```

## 建立與編輯

可透過選單 `Assets / Create / GameFrameX/Startup Options` 建立 `StartupOptions.asset`。Inspector 針對 `GlobalInfoUrls` 提供可拖曳重新排序的清單，其餘欄位以預設 PropertyField 繪製，並在各欄位下方顯示 Tooltip 說明。

來源：[StartupOptionsInspector.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Editor/StartupOptionsInspector.cs#L1-L54)

```csharp
[CustomEditor(typeof(StartupOptions))]
public sealed class StartupOptionsInspector : UnityEditor.Editor
{
    private ReorderableList _urlList;
    private SerializedProperty _urlListProperty;

    private void OnEnable()
    {
        _urlListProperty = serializedObject.FindProperty(nameof(StartupOptions.GlobalInfoUrls));
        _urlList = new ReorderableList(serializedObject, _urlListProperty, true, true, true, true)
        {
            drawHeaderCallback = rect => EditorGUI.LabelField(rect, "Global Info URLs (Primary-Backup Order)"),
            drawElementCallback = (rect, index, isActive, isFocused) =>
            {
                var element = _urlListProperty.GetArrayElementAtIndex(index);
                EditorGUI.PropertyField(rect, element, GUIContent.none);
            }
        };
    }
    // ...
}
```

## 欄位使用限制

| 限制 | 相關欄位 | 影響 |
|------|----------|------|
| `GlobalInfoUrls` 留空視為設定錯誤 | `GlobalInfoUrls` | 啟動流程會直接進入失敗路徑，不會嘗試任何請求 |
| `MaxAttemptsPerUrl` 包含首次請求 | `MaxAttemptsPerUrl` | 值為 1 時表示僅嘗試一次 |
| `SkipRemoteStartupRequests` 為 true 時會跳過所有遠端介面 | 該欄位 | 用於 WebGL 單機或純本地資源情境，會跳過全域資訊、App 版本、資源包版本 |
| `HotfixAssemblyName` / `HotfixEntryTypeName` / `HotfixEntryMethodName` 必須與組件內的實際符號一致 | Hotfix 三件套 | 只要其中一個輸入錯誤，`IHotfixLauncher` 的反射就會失敗 |

## 相關連結

- [StartupOptions.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Startup/StartupOptions.cs)
- [StartupOptionsInspector.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Editor/StartupOptionsInspector.cs)
- [StartupOptionsTests.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Tests/Runtime/StartupOptionsTests.cs)
- [UrlFailoverRunner.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Failover/UrlFailoverRunner.cs)（`GlobalInfoUrls` 主/備與重試的消費者）
- [DefaultStartupHttpParamsProvider.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Http/DefaultStartupHttpParamsProvider.cs)（HTTP 公用參數消費者）
- [IHotfixLauncher.cs](https://github.com/GameFrameX/com.gameframex.unity.startup/blob/main/Runtime/Hotfix/IHotfixLauncher.cs)（Hotfix 進入的消費者）